# Nodes

Nodes are named PFEM installations or example node instances.

Profiles define a node shape. Nodes identify an actual configured participant.

Example:

```text
profile_id = field-radio-node
node_id    = field-radio-node-example
```

The profile says what kind of node it is. The node manifest says who this node is
inside a local/federated pattern instance.
