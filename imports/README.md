# Import Records

Import records say what happened after an exchange receipt accepted a payload.

An exchange receipt says the exchange layer accepted or rejected a bundle/message.
An import record says the accepted payload was staged, applied, skipped, failed, or superseded in the receiver's local PFEM repository.
