# Sources

Sources are named origins of raw evidence.

Adapters describe how input is translated. Sources describe where a raw record
came from.

Examples:

```text
source_id = manual-entry
adapter_id = manual-observer-report
```

A source is not necessarily a hardware sensor. It can be a person-entered form,
file import, vendor feed, replay log, synthetic generator, or configured site system.
