# Routing

Routing policy describes where PFEM records, actions, bundles, and summaries should go.

Routing is not transport. It does not send anything by itself.

It answers:

```text
Given this kind of thing, with this scope/priority/handling, which node/profile should receive it?
```

Current file:

```text
routing/routing-policy.json
```
