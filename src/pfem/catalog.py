"""PFEM catalog."""

from __future__ import annotations

from pathlib import Path
from typing import Any

from pfem.action import load_action_policy, load_action_records
from pfem.apply_receipt import load_apply_receipts
from pfem.adapter_runtime import load_adapter_registry
from pfem.audit import load_audit_events
from pfem.conflict_record import load_conflict_records
from pfem.bundle import load_exchange_bundle
from pfem.capability_runtime import load_capability_manifest
from pfem.delivery import load_delivery_channel_registry
from pfem.delivery_job import load_delivery_jobs
from pfem.dispatch import load_dispatch_policy
from pfem.dispatch_decision import load_dispatch_decisions
from pfem.disposition_record import load_disposition_records
from pfem.disposition_receipt import load_disposition_receipts
from pfem.custody_record import load_custody_records
from pfem.custody_verification_receipt import load_custody_verification_receipts
from pfem.custody_transfer_record import load_custody_transfer_records
from pfem.custody_transfer_verification_receipt import load_custody_transfer_verification_receipts
from pfem.custody_closeout_record import load_custody_closeout_records
from pfem.custody_chain_record import load_custody_chain_records
from pfem.custody_chain_verification_receipt import load_custody_chain_verification_receipts
from pfem.custody_ledger_record import load_custody_ledger_records
from pfem.custody_ledger_verification_receipt import load_custody_ledger_verification_receipts
from pfem.custody_release_request import load_custody_release_requests
from pfem.custody_release_approval import load_custody_release_approvals
from pfem.custody_release_receipt import load_custody_release_receipts
from pfem.custody_release_verification_receipt import load_custody_release_verification_receipts
from pfem.custody_release_closeout_record import load_custody_release_closeout_records
from pfem.custody_release_chain_record import load_custody_release_chain_records
from pfem.custody_release_chain_verification_receipt import load_custody_release_chain_verification_receipts
from pfem.custody_lifecycle_record import load_custody_lifecycle_records
from pfem.custody_lifecycle_verification_receipt import load_custody_lifecycle_verification_receipts
from pfem.custody_lifecycle_closeout_record import load_custody_lifecycle_closeout_records
from pfem.archive_manifest_record import load_archive_manifest_records
from pfem.archive_receipt import load_archive_receipts
from pfem.archive_verification_receipt import load_archive_verification_receipts
from pfem.archive_closeout_record import load_archive_closeout_records
from pfem.archive_chain_record import load_archive_chain_records
from pfem.archive_chain_verification_receipt import load_archive_chain_verification_receipts
from pfem.archive_index_record import load_archive_index_records
from pfem.archive_index_verification_receipt import load_archive_index_verification_receipts
from pfem.archive_index_closeout_record import load_archive_index_closeout_records
from pfem.archive_lifecycle_record import load_archive_lifecycle_records
from pfem.archive_lifecycle_verification_receipt import load_archive_lifecycle_verification_receipts
from pfem.archive_lifecycle_closeout_record import load_archive_lifecycle_closeout_records
from pfem.preservation_record import load_preservation_records
from pfem.preservation_verification_receipt import load_preservation_verification_receipts
from pfem.preservation_closeout_record import load_preservation_closeout_records
from pfem.preservation_chain_record import load_preservation_chain_records
from pfem.preservation_chain_verification_receipt import load_preservation_chain_verification_receipts
from pfem.retention_review_record import load_retention_review_records
from pfem.retention_review_verification_receipt import load_retention_review_verification_receipts
from pfem.retention_decision_record import load_retention_decision_records
from pfem.retention_decision_approval import load_retention_decision_approvals
from pfem.retention_action_receipt import load_retention_action_receipts
from pfem.retention_action_verification_receipt import load_retention_action_verification_receipts
from pfem.retention_action_closeout_record import load_retention_action_closeout_records
from pfem.retention_chain_record import load_retention_chain_records
from pfem.retention_chain_verification_receipt import load_retention_chain_verification_receipts
from pfem.retention_lifecycle_record import load_retention_lifecycle_records
from pfem.retention_lifecycle_verification_receipt import load_retention_lifecycle_verification_receipts
from pfem.retention_lifecycle_closeout_record import load_retention_lifecycle_closeout_records
from pfem.retention_ledger_record import load_retention_ledger_records
from pfem.retention_ledger_verification_receipt import load_retention_ledger_verification_receipts
from pfem.retention_ledger_closeout_record import load_retention_ledger_closeout_records
from pfem.retention_policy_compliance_record import load_retention_policy_compliance_records
from pfem.retention_policy_compliance_verification_receipt import load_retention_policy_compliance_verification_receipts
from pfem.retention_obligation_record import load_retention_obligation_records
from pfem.retention_obligation_verification_receipt import load_retention_obligation_verification_receipts
from pfem.retention_schedule_record import load_retention_schedule_records
from pfem.retention_schedule_verification_receipt import load_retention_schedule_verification_receipts
from pfem.retention_schedule_closeout_record import load_retention_schedule_closeout_records
from pfem.retention_cycle_record import load_retention_cycle_records
from pfem.retention_cycle_verification_receipt import load_retention_cycle_verification_receipts
from pfem.retention_cycle_closeout_record import load_retention_cycle_closeout_records
from pfem.retention_hold_record import load_retention_hold_records
from pfem.retention_hold_verification_receipt import load_retention_hold_verification_receipts
from pfem.retention_hold_closeout_record import load_retention_hold_closeout_records
from pfem.retention_status_snapshot_record import load_retention_status_snapshot_records
from pfem.retention_status_snapshot_verification_receipt import load_retention_status_snapshot_verification_receipts
from pfem.retention_rollup_record import load_retention_rollup_records
from pfem.retention_rollup_verification_receipt import load_retention_rollup_verification_receipts
from pfem.retention_rollup_closeout_record import load_retention_rollup_closeout_records
from pfem.retention_report_record import load_retention_report_records
from pfem.retention_report_verification_receipt import load_retention_report_verification_receipts
from pfem.retention_report_closeout_record import load_retention_report_closeout_records
from pfem.retention_publication_record import load_retention_publication_records
from pfem.retention_publication_verification_receipt import load_retention_publication_verification_receipts
from pfem.retention_publication_closeout_record import load_retention_publication_closeout_records
from pfem.retention_dashboard_snapshot_record import load_retention_dashboard_snapshot_records
from pfem.retention_dashboard_snapshot_verification_receipt import load_retention_dashboard_snapshot_verification_receipts
from pfem.retention_dashboard_snapshot_closeout_record import load_retention_dashboard_snapshot_closeout_records
from pfem.retention_summary_record import load_retention_summary_records
from pfem.retention_summary_verification_receipt import load_retention_summary_verification_receipts
from pfem.retention_summary_closeout_record import load_retention_summary_closeout_records
from pfem.retention_export_record import load_retention_export_records
from pfem.retention_export_verification_receipt import load_retention_export_verification_receipts
from pfem.retention_export_closeout_record import load_retention_export_closeout_records
from pfem.retention_handoff_record import load_retention_handoff_records
from pfem.retention_handoff_verification_receipt import load_retention_handoff_verification_receipts
from pfem.retention_handoff_closeout_record import load_retention_handoff_closeout_records
from pfem.retention_acceptance_record import load_retention_acceptance_records
from pfem.retention_acceptance_verification_receipt import load_retention_acceptance_verification_receipts
from pfem.retention_acceptance_closeout_record import load_retention_acceptance_closeout_records
from pfem.retention_package_record import load_retention_package_records
from pfem.retention_package_verification_receipt import load_retention_package_verification_receipts
from pfem.retention_package_closeout_record import load_retention_package_closeout_records
from pfem.retention_finalization_record import load_retention_finalization_records
from pfem.retention_finalization_verification_receipt import load_retention_finalization_verification_receipts
from pfem.retention_finalization_closeout_record import load_retention_finalization_closeout_records
from pfem.retention_terminal_status_record import load_retention_terminal_status_records
from pfem.retention_terminal_status_verification_receipt import load_retention_terminal_status_verification_receipts
from pfem.retention_terminal_status_closeout_record import load_retention_terminal_status_closeout_records
from pfem.retention_certificate_record import load_retention_certificate_records
from pfem.retention_certificate_verification_receipt import load_retention_certificate_verification_receipts
from pfem.retention_certificate_closeout_record import load_retention_certificate_closeout_records
from pfem.retention_registry_record import load_retention_registry_records
from pfem.retention_registry_verification_receipt import load_retention_registry_verification_receipts
from pfem.retention_registry_closeout_record import load_retention_registry_closeout_records
from pfem.retention_closure_record import load_retention_closure_records
from pfem.retention_closure_verification_receipt import load_retention_closure_verification_receipts
from pfem.retention_closure_closeout_record import load_retention_closure_closeout_records
from pfem.retention_completion_record import load_retention_completion_records
from pfem.retention_completion_verification_receipt import load_retention_completion_verification_receipts
from pfem.retention_completion_closeout_record import load_retention_completion_closeout_records
from pfem.retention_attestation_record import load_retention_attestation_records
from pfem.retention_attestation_verification_receipt import load_retention_attestation_verification_receipts
from pfem.retention_attestation_closeout_record import load_retention_attestation_closeout_records
from pfem.retention_seal_record import load_retention_seal_records
from pfem.retention_seal_verification_receipt import load_retention_seal_verification_receipts
from pfem.retention_seal_closeout_record import load_retention_seal_closeout_records
from pfem.retention_notarization_record import load_retention_notarization_records
from pfem.retention_notarization_verification_receipt import load_retention_notarization_verification_receipts
from pfem.retention_notarization_closeout_record import load_retention_notarization_closeout_records
from pfem.retention_archive_anchor_record import load_retention_archive_anchor_records
from pfem.retention_archive_anchor_verification_receipt import load_retention_archive_anchor_verification_receipts
from pfem.retention_archive_anchor_closeout_record import load_retention_archive_anchor_closeout_records
from pfem.retention_endcap_record import load_retention_endcap_records
from pfem.retention_endcap_verification_receipt import load_retention_endcap_verification_receipts
from pfem.retention_endcap_closeout_record import load_retention_endcap_closeout_records
from pfem.retention_final_index_record import load_retention_final_index_records
from pfem.retention_final_index_verification_receipt import load_retention_final_index_verification_receipts
from pfem.retention_final_index_closeout_record import load_retention_final_index_closeout_records
from pfem.retention_master_ledger_record import load_retention_master_ledger_records
from pfem.retention_master_ledger_verification_receipt import load_retention_master_ledger_verification_receipts
from pfem.retention_master_ledger_closeout_record import load_retention_master_ledger_closeout_records
from pfem.retention_terminal_manifest_record import load_retention_terminal_manifest_records
from pfem.retention_terminal_manifest_verification_receipt import load_retention_terminal_manifest_verification_receipts
from pfem.retention_terminal_manifest_closeout_record import load_retention_terminal_manifest_closeout_records
from pfem.retention_repository_release_record import load_retention_repository_release_records
from pfem.retention_repository_release_verification_receipt import load_retention_repository_release_verification_receipts
from pfem.retention_repository_release_closeout_record import load_retention_repository_release_closeout_records
from pfem.retention_deployment_release_record import load_retention_deployment_release_records
from pfem.retention_deployment_release_verification_receipt import load_retention_deployment_release_verification_receipts
from pfem.retention_deployment_release_closeout_record import load_retention_deployment_release_closeout_records
from pfem.retention_availability_notice_record import load_retention_availability_notice_records
from pfem.retention_availability_notice_verification_receipt import load_retention_availability_notice_verification_receipts
from pfem.retention_availability_notice_closeout_record import load_retention_availability_notice_closeout_records
from pfem.retention_release_acknowledgement_record import load_retention_release_acknowledgement_records
from pfem.retention_release_acknowledgement_verification_receipt import load_retention_release_acknowledgement_verification_receipts
from pfem.retention_release_acknowledgement_closeout_record import load_retention_release_acknowledgement_closeout_records
from pfem.retention_release_confirmation_record import load_retention_release_confirmation_records
from pfem.retention_release_confirmation_verification_receipt import load_retention_release_confirmation_verification_receipts
from pfem.retention_release_confirmation_closeout_record import load_retention_release_confirmation_closeout_records
from pfem.retention_distribution_package_record import load_retention_distribution_package_records
from pfem.retention_distribution_package_verification_receipt import load_retention_distribution_package_verification_receipts
from pfem.retention_distribution_package_closeout_record import load_retention_distribution_package_closeout_records
from pfem.retention_distribution_manifest_record import load_retention_distribution_manifest_records
from pfem.retention_distribution_manifest_verification_receipt import load_retention_distribution_manifest_verification_receipts
from pfem.retention_distribution_manifest_closeout_record import load_retention_distribution_manifest_closeout_records
from pfem.retention_access_publication_record import load_retention_access_publication_records
from pfem.retention_access_publication_verification_receipt import load_retention_access_publication_verification_receipts
from pfem.retention_access_publication_closeout_record import load_retention_access_publication_closeout_records
from pfem.retention_access_grant_record import load_retention_access_grant_records
from pfem.retention_access_grant_verification_receipt import load_retention_access_grant_verification_receipts
from pfem.retention_access_grant_closeout_record import load_retention_access_grant_closeout_records
from pfem.retention_access_ledger_record import load_retention_access_ledger_records
from pfem.retention_access_ledger_verification_receipt import load_retention_access_ledger_verification_receipts
from pfem.retention_access_ledger_closeout_record import load_retention_access_ledger_closeout_records
from pfem.retention_retrieval_catalog_record import load_retention_retrieval_catalog_records
from pfem.retention_retrieval_catalog_verification_receipt import load_retention_retrieval_catalog_verification_receipts
from pfem.retention_retrieval_catalog_closeout_record import load_retention_retrieval_catalog_closeout_records
from pfem.retention_retrieval_endpoint_record import load_retention_retrieval_endpoint_records
from pfem.retention_retrieval_endpoint_verification_receipt import load_retention_retrieval_endpoint_verification_receipts
from pfem.retention_retrieval_endpoint_closeout_record import load_retention_retrieval_endpoint_closeout_records
from pfem.retention_retrieval_token_record import load_retention_retrieval_token_records
from pfem.retention_retrieval_token_verification_receipt import load_retention_retrieval_token_verification_receipts
from pfem.retention_retrieval_token_closeout_record import load_retention_retrieval_token_closeout_records
from pfem.retention_consumer_receipt_record import load_retention_consumer_receipt_records
from pfem.retention_consumer_receipt_verification_receipt import load_retention_consumer_receipt_verification_receipts
from pfem.retention_consumer_receipt_closeout_record import load_retention_consumer_receipt_closeout_records
from pfem.retention_publication_rollup_record import load_retention_publication_rollup_records
from pfem.retention_publication_rollup_verification_receipt import load_retention_publication_rollup_verification_receipts
from pfem.retention_publication_rollup_closeout_record import load_retention_publication_rollup_closeout_records
from pfem.retention_distribution_receipt_record import load_retention_distribution_receipt_records
from pfem.retention_distribution_receipt_verification_receipt import load_retention_distribution_receipt_verification_receipts
from pfem.retention_distribution_receipt_closeout_record import load_retention_distribution_receipt_closeout_records
from pfem.retention_access_audit_snapshot_record import load_retention_access_audit_snapshot_records
from pfem.retention_access_audit_snapshot_verification_receipt import load_retention_access_audit_snapshot_verification_receipts
from pfem.retention_access_audit_snapshot_closeout_record import load_retention_access_audit_snapshot_closeout_records
from pfem.retention_release_health_snapshot_record import load_retention_release_health_snapshot_records
from pfem.retention_release_health_snapshot_verification_receipt import load_retention_release_health_snapshot_verification_receipts
from pfem.retention_release_health_snapshot_closeout_record import load_retention_release_health_snapshot_closeout_records
from pfem.retention_release_usage_summary_record import load_retention_release_usage_summary_records
from pfem.retention_release_usage_summary_verification_receipt import load_retention_release_usage_summary_verification_receipts
from pfem.retention_release_usage_summary_closeout_record import load_retention_release_usage_summary_closeout_records
from pfem.retention_retention_exposure_report_record import load_retention_retention_exposure_report_records
from pfem.retention_retention_exposure_report_verification_receipt import load_retention_retention_exposure_report_verification_receipts
from pfem.retention_retention_exposure_report_closeout_record import load_retention_retention_exposure_report_closeout_records
from pfem.retention_release_closeout_summary_record import load_retention_release_closeout_summary_records
from pfem.retention_release_closeout_summary_verification_receipt import load_retention_release_closeout_summary_verification_receipts
from pfem.retention_release_closeout_summary_closeout_record import load_retention_release_closeout_summary_closeout_records
from pfem.retention_public_record_index_record import load_retention_public_record_index_records
from pfem.retention_public_record_index_verification_receipt import load_retention_public_record_index_verification_receipts
from pfem.retention_public_record_index_closeout_record import load_retention_public_record_index_closeout_records
from pfem.retention_final_release_bundle_record import load_retention_final_release_bundle_records
from pfem.retention_final_release_bundle_verification_receipt import load_retention_final_release_bundle_verification_receipts
from pfem.retention_final_release_bundle_closeout_record import load_retention_final_release_bundle_closeout_records
from pfem.retention_terminal_access_notice_record import load_retention_terminal_access_notice_records
from pfem.retention_terminal_access_notice_verification_receipt import load_retention_terminal_access_notice_verification_receipts
from pfem.retention_terminal_access_notice_closeout_record import load_retention_terminal_access_notice_closeout_records
from pfem.retention_release_acceptance_record import load_retention_release_acceptance_records
from pfem.retention_release_acceptance_verification_receipt import load_retention_release_acceptance_verification_receipts
from pfem.retention_release_acceptance_closeout_record import load_retention_release_acceptance_closeout_records
from pfem.retention_access_completion_record import load_retention_access_completion_records
from pfem.retention_access_completion_verification_receipt import load_retention_access_completion_verification_receipts
from pfem.retention_access_completion_closeout_record import load_retention_access_completion_closeout_records
from pfem.retention_publication_certificate_record import load_retention_publication_certificate_records
from pfem.retention_publication_certificate_verification_receipt import load_retention_publication_certificate_verification_receipts
from pfem.retention_publication_certificate_closeout_record import load_retention_publication_certificate_closeout_records
from pfem.retention_distribution_closure_notice_record import load_retention_distribution_closure_notice_records
from pfem.retention_distribution_closure_notice_verification_receipt import load_retention_distribution_closure_notice_verification_receipts
from pfem.retention_distribution_closure_notice_closeout_record import load_retention_distribution_closure_notice_closeout_records
from pfem.retention_public_access_register_record import load_retention_public_access_register_records
from pfem.retention_public_access_register_verification_receipt import load_retention_public_access_register_verification_receipts
from pfem.retention_public_access_register_closeout_record import load_retention_public_access_register_closeout_records
from pfem.retention_release_access_index_record import load_retention_release_access_index_records
from pfem.retention_release_access_index_verification_receipt import load_retention_release_access_index_verification_receipts
from pfem.retention_release_access_index_closeout_record import load_retention_release_access_index_closeout_records
from pfem.retention_release_access_verification_summary_record import load_retention_release_access_verification_summary_records
from pfem.retention_release_access_verification_summary_verification_receipt import load_retention_release_access_verification_summary_verification_receipts
from pfem.retention_release_access_verification_summary_closeout_record import load_retention_release_access_verification_summary_closeout_records
from pfem.retention_release_access_closeout_summary_record import load_retention_release_access_closeout_summary_records
from pfem.retention_release_access_closeout_summary_verification_receipt import load_retention_release_access_closeout_summary_verification_receipts
from pfem.retention_release_access_closeout_summary_closeout_record import load_retention_release_access_closeout_summary_closeout_records
from pfem.retention_archive_availability_rollup_record import load_retention_archive_availability_rollup_records
from pfem.retention_archive_availability_rollup_verification_receipt import load_retention_archive_availability_rollup_verification_receipts
from pfem.retention_archive_availability_rollup_closeout_record import load_retention_archive_availability_rollup_closeout_records
from pfem.retention_retrieval_readiness_snapshot_record import load_retention_retrieval_readiness_snapshot_records
from pfem.retention_retrieval_readiness_snapshot_verification_receipt import load_retention_retrieval_readiness_snapshot_verification_receipts
from pfem.retention_retrieval_readiness_snapshot_closeout_record import load_retention_retrieval_readiness_snapshot_closeout_records
from pfem.retention_consumer_availability_notice_record import load_retention_consumer_availability_notice_records
from pfem.retention_consumer_availability_notice_verification_receipt import load_retention_consumer_availability_notice_verification_receipts
from pfem.retention_consumer_availability_notice_closeout_record import load_retention_consumer_availability_notice_closeout_records
from pfem.retention_public_release_receipt_record import load_retention_public_release_receipt_records
from pfem.retention_public_release_receipt_verification_receipt import load_retention_public_release_receipt_verification_receipts
from pfem.retention_public_release_receipt_closeout_record import load_retention_public_release_receipt_closeout_records
from pfem.retention_release_exception_register_record import load_retention_release_exception_register_records
from pfem.retention_release_exception_register_verification_receipt import load_retention_release_exception_register_verification_receipts
from pfem.retention_release_exception_register_closeout_record import load_retention_release_exception_register_closeout_records
from pfem.retention_release_exception_summary_record import load_retention_release_exception_summary_records
from pfem.retention_release_exception_summary_verification_receipt import load_retention_release_exception_summary_verification_receipts
from pfem.retention_release_exception_summary_closeout_record import load_retention_release_exception_summary_closeout_records
from pfem.retention_release_metrics_snapshot_record import load_retention_release_metrics_snapshot_records
from pfem.retention_release_metrics_snapshot_verification_receipt import load_retention_release_metrics_snapshot_verification_receipts
from pfem.retention_release_metrics_snapshot_closeout_record import load_retention_release_metrics_snapshot_closeout_records
from pfem.retention_release_terminal_report_record import load_retention_release_terminal_report_records
from pfem.retention_release_terminal_report_verification_receipt import load_retention_release_terminal_report_verification_receipts
from pfem.retention_release_terminal_report_closeout_record import load_retention_release_terminal_report_closeout_records
from pfem.retention_final_publication_notice_record import load_retention_final_publication_notice_records
from pfem.retention_final_publication_notice_verification_receipt import load_retention_final_publication_notice_verification_receipts
from pfem.retention_final_publication_notice_closeout_record import load_retention_final_publication_notice_closeout_records
from pfem.retention_publication_completion_record import load_retention_publication_completion_records
from pfem.retention_publication_completion_verification_receipt import load_retention_publication_completion_verification_receipts
from pfem.retention_publication_completion_closeout_record import load_retention_publication_completion_closeout_records
from pfem.retention_publication_closure_certificate_record import load_retention_publication_closure_certificate_records
from pfem.retention_publication_closure_certificate_verification_receipt import load_retention_publication_closure_certificate_verification_receipts
from pfem.retention_publication_closure_certificate_closeout_record import load_retention_publication_closure_certificate_closeout_records
from pfem.retention_terminal_public_index_record import load_retention_terminal_public_index_records
from pfem.retention_terminal_public_index_verification_receipt import load_retention_terminal_public_index_verification_receipts
from pfem.retention_terminal_public_index_closeout_record import load_retention_terminal_public_index_closeout_records
from pfem.retention_terminal_public_digest_record import load_retention_terminal_public_digest_records
from pfem.retention_terminal_public_digest_verification_receipt import load_retention_terminal_public_digest_verification_receipts
from pfem.retention_terminal_public_digest_closeout_record import load_retention_terminal_public_digest_closeout_records
from pfem.retention_terminal_public_archive_copy_record import load_retention_terminal_public_archive_copy_records
from pfem.retention_terminal_public_archive_copy_verification_receipt import load_retention_terminal_public_archive_copy_verification_receipts
from pfem.retention_terminal_public_archive_copy_closeout_record import load_retention_terminal_public_archive_copy_closeout_records
from pfem.retention_terminal_public_audit_snapshot_record import load_retention_terminal_public_audit_snapshot_records
from pfem.retention_terminal_public_audit_snapshot_verification_receipt import load_retention_terminal_public_audit_snapshot_verification_receipts
from pfem.retention_terminal_public_audit_snapshot_closeout_record import load_retention_terminal_public_audit_snapshot_closeout_records
from pfem.retention_terminal_public_metrics_snapshot_record import load_retention_terminal_public_metrics_snapshot_records
from pfem.retention_terminal_public_metrics_snapshot_verification_receipt import load_retention_terminal_public_metrics_snapshot_verification_receipts
from pfem.retention_terminal_public_metrics_snapshot_closeout_record import load_retention_terminal_public_metrics_snapshot_closeout_records
from pfem.retention_terminal_public_release_summary_record import load_retention_terminal_public_release_summary_records
from pfem.retention_terminal_public_release_summary_verification_receipt import load_retention_terminal_public_release_summary_verification_receipts
from pfem.retention_terminal_public_release_summary_closeout_record import load_retention_terminal_public_release_summary_closeout_records
from pfem.retention_terminal_distribution_summary_record import load_retention_terminal_distribution_summary_records
from pfem.retention_terminal_distribution_summary_verification_receipt import load_retention_terminal_distribution_summary_verification_receipts
from pfem.retention_terminal_distribution_summary_closeout_record import load_retention_terminal_distribution_summary_closeout_records
from pfem.retention_terminal_access_summary_record import load_retention_terminal_access_summary_records
from pfem.retention_terminal_access_summary_verification_receipt import load_retention_terminal_access_summary_verification_receipts
from pfem.retention_terminal_access_summary_closeout_record import load_retention_terminal_access_summary_closeout_records
from pfem.retention_terminal_retrieval_summary_record import load_retention_terminal_retrieval_summary_records
from pfem.retention_terminal_retrieval_summary_verification_receipt import load_retention_terminal_retrieval_summary_verification_receipts
from pfem.retention_terminal_retrieval_summary_closeout_record import load_retention_terminal_retrieval_summary_closeout_records
from pfem.retention_terminal_consumer_summary_record import load_retention_terminal_consumer_summary_records
from pfem.retention_terminal_consumer_summary_verification_receipt import load_retention_terminal_consumer_summary_verification_receipts
from pfem.retention_terminal_consumer_summary_closeout_record import load_retention_terminal_consumer_summary_closeout_records
from pfem.retention_terminal_exception_summary_record import load_retention_terminal_exception_summary_records
from pfem.retention_terminal_exception_summary_verification_receipt import load_retention_terminal_exception_summary_verification_receipts
from pfem.retention_terminal_exception_summary_closeout_record import load_retention_terminal_exception_summary_closeout_records
from pfem.retention_terminal_archive_report_record import load_retention_terminal_archive_report_records
from pfem.retention_terminal_archive_report_verification_receipt import load_retention_terminal_archive_report_verification_receipts
from pfem.retention_terminal_archive_report_closeout_record import load_retention_terminal_archive_report_closeout_records
from pfem.retention_terminal_release_annex_record import load_retention_terminal_release_annex_records
from pfem.retention_terminal_release_annex_verification_receipt import load_retention_terminal_release_annex_verification_receipts
from pfem.retention_terminal_release_annex_closeout_record import load_retention_terminal_release_annex_closeout_records
from pfem.retention_terminal_publication_bundle_record import load_retention_terminal_publication_bundle_records
from pfem.retention_terminal_publication_bundle_verification_receipt import load_retention_terminal_publication_bundle_verification_receipts
from pfem.retention_terminal_publication_bundle_closeout_record import load_retention_terminal_publication_bundle_closeout_records
from pfem.retention_publication_terminal_status_record import load_retention_publication_terminal_status_records
from pfem.doctor import find_repo_root
from pfem.example_runtime import load_example_registry
from pfem.exchange import load_exchange_receipts
from pfem.handling import load_handling_policy
from pfem.inbox import load_inbox_items
from pfem.import_record import load_import_records
from pfem.intake_decision import load_intake_decisions
from pfem.merge_decision import load_merge_decisions
from pfem.integrity import load_integrity_manifest
from pfem.node_runtime import load_node_registry
from pfem.outbox import load_outbox_items
from pfem.playbook import load_playbooks
from pfem.policy import load_sharing_policy
from pfem.profile_runtime import load_profile_registry
from pfem.quality import load_quality_assessments, load_quality_policy
from pfem.reconciliation import load_reconciliation_records
from pfem.recovery_point import load_recovery_points
from pfem.snapshot_manifest import load_snapshot_manifests
from pfem.snapshot_verification_receipt import load_snapshot_verification_receipts
from pfem.retention import load_retention_policy
from pfem.restore_plan import load_restore_plans
from pfem.restore_approval import load_restore_approvals
from pfem.restore_receipt import load_restore_receipts
from pfem.restore_verification_receipt import load_restore_verification_receipts
from pfem.restore_closeout_record import load_restore_closeout_records
from pfem.review import load_review_records
from pfem.routing import load_routing_policy
from pfem.source_runtime import load_source_registry
from pfem.state_checkpoint import load_state_checkpoints
from pfem.state_transition import load_state_transitions
from pfem.transport import load_transport_adapter_registry
from pfem.transport_receipt import load_transport_receipts


def _capability_rows(root: Path) -> list[dict[str, Any]]:
    return [{"capability_id": m.capability_id, "capability_kind": m.capability_kind, "path": str(p.relative_to(root))} for p in sorted((root / "capabilities").rglob("*.capability.yaml")) for m in [load_capability_manifest(p)]] if (root / "capabilities").exists() else []


def _adapter_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "adapters" / "adapter-registry.json"
    return [] if not p.exists() else [{"adapter_id": e.adapter_id, "adapter_kind": e.adapter_kind, "status": e.status, "path": e.path} for e in load_adapter_registry(p).adapters]


def _profile_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "profiles" / "profile-registry.json"
    return [] if not p.exists() else [{"profile_id": e.profile_id, "profile_kind": e.profile_kind, "status": e.status, "path": e.path} for e in load_profile_registry(p).profiles]


def _node_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "nodes" / "node-registry.json"
    return [] if not p.exists() else [{"node_id": e.node_id, "profile_id": e.profile_id, "status": e.status, "path": e.path} for e in load_node_registry(p).nodes]


def _source_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "sources" / "source-registry.json"
    return [] if not p.exists() else [{"source_id": e.source_id, "source_kind": e.source_kind, "adapter_id": e.adapter_id, "node_id": e.node_id, "status": e.status} for e in load_source_registry(p).sources]


def _example_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "examples" / "example-registry.json"
    return [] if not p.exists() else [{"example_id": e.example_id, "profile_id": e.profile_id, "runnable": e.runnable, "status": e.status, "path": e.path} for e in load_example_registry(p).examples]


def _review_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "review" / "review-records.json"
    return [] if not p.exists() else [{"review_id": r.review_id, "review_gate": r.review_gate, "decision": r.decision, "sharing_scope": r.sharing_scope or ""} for r in load_review_records(p)]


def _audit_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "audit" / "audit-journal.json"
    return [] if not p.exists() else [{"audit_id": e.audit_id, "event_kind": e.event_kind, "actor_ref": e.actor_ref} for e in load_audit_events(p)]


def _bundle_rows(root: Path) -> list[dict[str, Any]]:
    return [{"bundle_id": b.bundle_id, "bundle_kind": b.bundle_kind, "sharing_scope": b.sharing_scope, "path": str(p.relative_to(root))} for p in sorted((root / "bundles").glob("**/*.bundle.json")) for b in [load_exchange_bundle(p)]] if (root / "bundles").exists() else []


def _exchange_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "exchange" / "exchange-receipts.json"
    return [] if not p.exists() else [{"exchange_receipt_id": r.exchange_receipt_id, "receipt_kind": r.receipt_kind, "bundle_id": r.bundle_id, "decision": r.decision, "intake_decision_id": r.intake_decision_id or ""} for r in load_exchange_receipts(p)]


def _reconciliation_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "reconciliation" / "reconciliation-records.json"
    return [] if not p.exists() else [{"reconciliation_id": r.reconciliation_id, "reconciliation_kind": r.reconciliation_kind, "decision": r.decision, "result_state": r.result_state} for r in load_reconciliation_records(p)]


def _quality_assessment_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "quality" / "quality-assessments.json"
    return [] if not p.exists() else [{"quality_assessment_id": q.quality_assessment_id, "confidence_level": q.confidence_level, "flags": ",".join(q.quality_flags)} for q in load_quality_assessments(p)]


def _quality_policy_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "quality" / "quality-policy.json"
    return [] if not p.exists() else [{"confidence_level": level.confidence_level, "rank": level.rank, "display_name": level.display_name} for level in load_quality_policy(p).confidence_levels]


def _action_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "action" / "action-records.json"
    return [] if not p.exists() else [{"action_id": a.action_id, "action_kind": a.action_kind, "priority": a.priority, "action_state": a.action_state} for a in load_action_records(p)]


def _action_policy_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "action" / "action-policy.json"
    return [] if not p.exists() else [{"action_kind": kind.action_kind, "display_name": kind.display_name} for kind in load_action_policy(p).action_kinds]


def _playbook_rows(root: Path) -> list[dict[str, Any]]:
    return [{"playbook_id": playbook.playbook_id, "playbook_kind": playbook.playbook_kind, "status": playbook.status, "steps": len(playbook.steps)} for _, playbook in load_playbooks(root)]


def _dispatch_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "dispatch" / "dispatch-policy.json"
    return [] if not p.exists() else [{"dispatch_rule_id": r.dispatch_rule_id, "enabled": r.enabled, "max_attempts": r.max_attempts, "retry_delay_seconds": r.retry_delay_seconds} for r in load_dispatch_policy(p).rules]


def _dispatch_decision_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "dispatch" / "dispatch-decisions.json"
    return [] if not p.exists() else [{"dispatch_decision_id": d.dispatch_decision_id, "delivery_job_id": d.delivery_job_id, "decision": d.decision, "reason_code": d.reason_code} for d in load_dispatch_decisions(p)]


def _outbox_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "outbox" / "outbox-items.json"
    return [] if not p.exists() else [{"outbox_item_id": o.outbox_item_id, "delivery_job_id": o.delivery_job_id, "outbox_state": o.outbox_state, "item_kind": o.item_kind} for o in load_outbox_items(p)]


def _inbox_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "inbox" / "inbox-items.json"
    return [] if not p.exists() else [{"inbox_item_id": i.inbox_item_id, "transport_receipt_id": i.transport_receipt_id, "inbox_state": i.inbox_state, "item_kind": i.item_kind} for i in load_inbox_items(p)]


def _import_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "imports" / "import-records.json"
    return [] if not p.exists() else [{"import_record_id": r.import_record_id, "exchange_receipt_id": r.exchange_receipt_id, "import_state": r.import_state, "import_kind": r.import_kind} for r in load_import_records(p)]


def _conflict_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "conflicts" / "conflict-records.json"
    return [] if not p.exists() else [{"conflict_record_id": r.conflict_record_id, "import_record_id": r.import_record_id, "conflict_state": r.conflict_state, "severity": r.severity} for r in load_conflict_records(p)]


def _apply_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "apply" / "apply-receipts.json"
    return [] if not p.exists() else [{"apply_receipt_id": r.apply_receipt_id, "merge_decision_id": r.merge_decision_id, "apply_state": r.apply_state, "receipt_kind": r.receipt_kind} for r in load_apply_receipts(p)]


def _state_checkpoint_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "state" / "state-checkpoints.json"
    return [] if not p.exists() else [{"state_checkpoint_id": c.state_checkpoint_id, "node_id": c.node_id, "checkpoint_state": c.checkpoint_state, "included_refs": len(c.included_refs)} for c in load_state_checkpoints(p)]


def _state_transition_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "state" / "state-transitions.json"
    return [] if not p.exists() else [{"state_transition_id": t.state_transition_id, "to_state_checkpoint_id": t.to_state_checkpoint_id, "transition_state": t.transition_state, "changed_refs": len(t.changed_refs)} for t in load_state_transitions(p)]


def _snapshot_manifest_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "snapshots" / "snapshot-manifests.json"
    return [] if not p.exists() else [{"snapshot_manifest_id": m.snapshot_manifest_id, "state_checkpoint_id": m.state_checkpoint_id, "snapshot_state": m.snapshot_state, "items": len(m.items)} for m in load_snapshot_manifests(p)]


def _snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "snapshots" / "snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"snapshot_verification_receipt_id": r.snapshot_verification_receipt_id, "snapshot_manifest_id": r.snapshot_manifest_id, "verification_state": r.verification_state, "checked_items": len(r.checked_item_refs)} for r in load_snapshot_verification_receipts(p)]


def _recovery_point_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "recovery" / "recovery-points.json"
    return [] if not p.exists() else [{"recovery_point_id": r.recovery_point_id, "snapshot_verification_receipt_id": r.snapshot_verification_receipt_id, "recovery_state": r.recovery_state, "restore_scope": r.restore_scope} for r in load_recovery_points(p)]


def _restore_plan_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "restore" / "restore-plans.json"
    return [] if not p.exists() else [{"restore_plan_id": r.restore_plan_id, "recovery_point_id": r.recovery_point_id, "plan_state": r.plan_state, "restore_scope": r.restore_scope} for r in load_restore_plans(p)]


def _restore_approval_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "restore" / "restore-approvals.json"
    return [] if not p.exists() else [{"restore_approval_id": a.restore_approval_id, "restore_plan_id": a.restore_plan_id, "approval_state": a.approval_state, "approved_scope": a.approved_scope} for a in load_restore_approvals(p)]


def _restore_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "restore" / "restore-receipts.json"
    return [] if not p.exists() else [{"restore_receipt_id": r.restore_receipt_id, "restore_plan_id": r.restore_plan_id, "restore_state": r.restore_state, "restore_scope": r.restore_scope} for r in load_restore_receipts(p)]


def _restore_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "restore" / "restore-verification-receipts.json"
    return [] if not p.exists() else [{"restore_verification_receipt_id": r.restore_verification_receipt_id, "restore_receipt_id": r.restore_receipt_id, "verification_state": r.verification_state, "checked_refs": len(r.checked_refs)} for r in load_restore_verification_receipts(p)]


def _restore_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "restore" / "restore-closeout-records.json"
    return [] if not p.exists() else [{"restore_closeout_record_id": r.restore_closeout_record_id, "restore_verification_receipt_id": r.restore_verification_receipt_id, "closeout_state": r.closeout_state, "outcome": r.outcome} for r in load_restore_closeout_records(p)]


def _disposition_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "disposition" / "disposition-records.json"
    return [] if not p.exists() else [{"disposition_record_id": r.disposition_record_id, "source_closeout_ref": r.source_closeout_ref, "disposition_state": r.disposition_state, "actions": len(r.actions)} for r in load_disposition_records(p)]


def _disposition_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "disposition" / "disposition-receipts.json"
    return [] if not p.exists() else [{"disposition_receipt_id": r.disposition_receipt_id, "disposition_record_id": r.disposition_record_id, "receipt_state": r.receipt_state, "executed_actions": len(r.executed_actions)} for r in load_disposition_receipts(p)]


def _custody_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-records.json"
    return [] if not p.exists() else [{"custody_record_id": r.custody_record_id, "disposition_receipt_id": r.disposition_receipt_id, "custody_state": r.custody_state, "custody_location_kind": r.custody_location_kind} for r in load_custody_records(p)]


def _custody_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-verification-receipts.json"
    return [] if not p.exists() else [{"custody_verification_receipt_id": r.custody_verification_receipt_id, "custody_record_id": r.custody_record_id, "verification_state": r.verification_state, "checked_refs": len(r.checked_refs)} for r in load_custody_verification_receipts(p)]


def _custody_transfer_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-transfer-records.json"
    return [] if not p.exists() else [{"custody_transfer_record_id": r.custody_transfer_record_id, "custody_verification_receipt_id": r.custody_verification_receipt_id, "transfer_state": r.transfer_state, "transferred_refs": len(r.transferred_refs)} for r in load_custody_transfer_records(p)]


def _custody_transfer_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-transfer-verification-receipts.json"
    return [] if not p.exists() else [{"custody_transfer_verification_receipt_id": r.custody_transfer_verification_receipt_id, "custody_transfer_record_id": r.custody_transfer_record_id, "verification_state": r.verification_state, "checked_refs": len(r.checked_refs)} for r in load_custody_transfer_verification_receipts(p)]


def _custody_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-closeout-records.json"
    return [] if not p.exists() else [{"custody_closeout_record_id": r.custody_closeout_record_id, "custody_transfer_verification_receipt_id": r.custody_transfer_verification_receipt_id, "closeout_state": r.closeout_state, "outcome": r.outcome} for r in load_custody_closeout_records(p)]


def _custody_chain_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-chain-records.json"
    return [] if not p.exists() else [{"custody_chain_record_id": r.custody_chain_record_id, "terminal_ref": r.terminal_ref, "chain_state": r.chain_state, "chain_refs": len(r.chain_refs)} for r in load_custody_chain_records(p)]


def _custody_chain_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody" / "custody-chain-verification-receipts.json"
    return [] if not p.exists() else [{"custody_chain_verification_receipt_id": r.custody_chain_verification_receipt_id, "custody_chain_record_id": r.custody_chain_record_id, "verification_state": r.verification_state, "checked_chain_refs": len(r.checked_chain_refs)} for r in load_custody_chain_verification_receipts(p)]


def _custody_ledger_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-ledger-records.json"
    return [] if not p.exists() else [{"custody_ledger_record_id": r["custody_ledger_record_id"], "custody_chain_record_id": r["custody_chain_record_id"], "ledger_state": r["ledger_state"], "entry_refs": len(r["entry_refs"])} for r in load_custody_ledger_records(p)]


def _custody_ledger_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-ledger-verification-receipts.json"
    return [] if not p.exists() else [{"custody_ledger_verification_receipt_id": r["custody_ledger_verification_receipt_id"], "custody_ledger_record_id": r["custody_ledger_record_id"], "verification_state": r["verification_state"], "checked_entry_refs": len(r["checked_entry_refs"])} for r in load_custody_ledger_verification_receipts(p)]


def _custody_release_request_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-requests.json"
    return [] if not p.exists() else [{"custody_release_request_id": r["custody_release_request_id"], "custody_ledger_record_id": r["custody_ledger_record_id"], "request_state": r["request_state"], "requested_refs": len(r["requested_refs"])} for r in load_custody_release_requests(p)]


def _custody_release_approval_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-approvals.json"
    return [] if not p.exists() else [{"custody_release_approval_id": r["custody_release_approval_id"], "custody_release_request_id": r["custody_release_request_id"], "approval_state": r["approval_state"], "approved_refs": len(r["approved_refs"])} for r in load_custody_release_approvals(p)]


def _custody_release_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-receipts.json"
    return [] if not p.exists() else [{"custody_release_receipt_id": r["custody_release_receipt_id"], "custody_release_approval_id": r["custody_release_approval_id"], "release_state": r["release_state"], "released_refs": len(r["released_refs"])} for r in load_custody_release_receipts(p)]


def _custody_release_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-verification-receipts.json"
    return [] if not p.exists() else [{"custody_release_verification_receipt_id": r["custody_release_verification_receipt_id"], "custody_release_receipt_id": r["custody_release_receipt_id"], "verification_state": r["verification_state"], "checked_released_refs": len(r["checked_released_refs"])} for r in load_custody_release_verification_receipts(p)]


def _custody_release_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-closeout-records.json"
    return [] if not p.exists() else [{"custody_release_closeout_record_id": r["custody_release_closeout_record_id"], "custody_release_verification_receipt_id": r["custody_release_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_custody_release_closeout_records(p)]


def _custody_release_chain_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-chain-records.json"
    return [] if not p.exists() else [{"custody_release_chain_record_id": r["custody_release_chain_record_id"], "terminal_ref": r["terminal_ref"], "chain_state": r["chain_state"], "chain_refs": len(r["chain_refs"])} for r in load_custody_release_chain_records(p)]


def _custody_release_chain_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-release-chain-verification-receipts.json"
    return [] if not p.exists() else [{"custody_release_chain_verification_receipt_id": r["custody_release_chain_verification_receipt_id"], "custody_release_chain_record_id": r["custody_release_chain_record_id"], "verification_state": r["verification_state"], "checked_chain_refs": len(r["checked_chain_refs"])} for r in load_custody_release_chain_verification_receipts(p)]


def _custody_lifecycle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-lifecycle-records.json"
    return [] if not p.exists() else [{"custody_lifecycle_record_id": r["custody_lifecycle_record_id"], "lifecycle_state": r["lifecycle_state"], "final_outcome": r["final_outcome"], "lifecycle_refs": len(r["lifecycle_refs"])} for r in load_custody_lifecycle_records(p)]


def _custody_lifecycle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-lifecycle-verification-receipts.json"
    return [] if not p.exists() else [{"custody_lifecycle_verification_receipt_id": r["custody_lifecycle_verification_receipt_id"], "custody_lifecycle_record_id": r["custody_lifecycle_record_id"], "verification_state": r["verification_state"], "checked_lifecycle_refs": len(r["checked_lifecycle_refs"])} for r in load_custody_lifecycle_verification_receipts(p)]


def _custody_lifecycle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "custody/custody-lifecycle-closeout-records.json"
    return [] if not p.exists() else [{"custody_lifecycle_closeout_record_id": r["custody_lifecycle_closeout_record_id"], "custody_lifecycle_verification_receipt_id": r["custody_lifecycle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_custody_lifecycle_closeout_records(p)]


def _archive_manifest_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-manifest-records.json"
    return [] if not p.exists() else [{"archive_manifest_record_id": r["archive_manifest_record_id"], "custody_lifecycle_closeout_record_id": r["custody_lifecycle_closeout_record_id"], "manifest_state": r["manifest_state"], "manifested_refs": len(r["manifested_refs"])} for r in load_archive_manifest_records(p)]


def _archive_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-receipts.json"
    return [] if not p.exists() else [{"archive_receipt_id": r["archive_receipt_id"], "archive_manifest_record_id": r["archive_manifest_record_id"], "archive_state": r["archive_state"], "archived_refs": len(r["archived_refs"])} for r in load_archive_receipts(p)]


def _archive_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-verification-receipts.json"
    return [] if not p.exists() else [{"archive_verification_receipt_id": r["archive_verification_receipt_id"], "archive_receipt_id": r["archive_receipt_id"], "verification_state": r["verification_state"], "checked_archived_refs": len(r["checked_archived_refs"])} for r in load_archive_verification_receipts(p)]


def _archive_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-closeout-records.json"
    return [] if not p.exists() else [{"archive_closeout_record_id": r["archive_closeout_record_id"], "archive_verification_receipt_id": r["archive_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_archive_closeout_records(p)]


def _archive_chain_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-chain-records.json"
    return [] if not p.exists() else [{"archive_chain_record_id": r["archive_chain_record_id"], "terminal_ref": r["terminal_ref"], "chain_state": r["chain_state"], "chain_refs": len(r["chain_refs"])} for r in load_archive_chain_records(p)]


def _archive_chain_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-chain-verification-receipts.json"
    return [] if not p.exists() else [{"archive_chain_verification_receipt_id": r["archive_chain_verification_receipt_id"], "archive_chain_record_id": r["archive_chain_record_id"], "verification_state": r["verification_state"], "checked_chain_refs": len(r["checked_chain_refs"])} for r in load_archive_chain_verification_receipts(p)]


def _archive_index_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-index-records.json"
    return [] if not p.exists() else [{"archive_index_record_id": r["archive_index_record_id"], "archive_chain_record_id": r["archive_chain_record_id"], "index_state": r["index_state"], "indexed_refs": len(r["indexed_refs"])} for r in load_archive_index_records(p)]


def _archive_index_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-index-verification-receipts.json"
    return [] if not p.exists() else [{"archive_index_verification_receipt_id": r["archive_index_verification_receipt_id"], "archive_index_record_id": r["archive_index_record_id"], "verification_state": r["verification_state"], "checked_indexed_refs": len(r["checked_indexed_refs"])} for r in load_archive_index_verification_receipts(p)]


def _archive_index_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-index-closeout-records.json"
    return [] if not p.exists() else [{"archive_index_closeout_record_id": r["archive_index_closeout_record_id"], "archive_index_verification_receipt_id": r["archive_index_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_archive_index_closeout_records(p)]


def _archive_lifecycle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-lifecycle-records.json"
    return [] if not p.exists() else [{"archive_lifecycle_record_id": r["archive_lifecycle_record_id"], "archive_index_closeout_record_id": r["archive_index_closeout_record_id"], "lifecycle_state": r["lifecycle_state"], "lifecycle_refs": len(r["lifecycle_refs"])} for r in load_archive_lifecycle_records(p)]


def _archive_lifecycle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-lifecycle-verification-receipts.json"
    return [] if not p.exists() else [{"archive_lifecycle_verification_receipt_id": r["archive_lifecycle_verification_receipt_id"], "archive_lifecycle_record_id": r["archive_lifecycle_record_id"], "verification_state": r["verification_state"], "checked_lifecycle_refs": len(r["checked_lifecycle_refs"])} for r in load_archive_lifecycle_verification_receipts(p)]


def _archive_lifecycle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "archive/archive-lifecycle-closeout-records.json"
    return [] if not p.exists() else [{"archive_lifecycle_closeout_record_id": r["archive_lifecycle_closeout_record_id"], "archive_lifecycle_verification_receipt_id": r["archive_lifecycle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_archive_lifecycle_closeout_records(p)]


def _preservation_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "preservation/preservation-records.json"
    return [] if not p.exists() else [{"preservation_record_id": r["preservation_record_id"], "archive_lifecycle_closeout_record_id": r["archive_lifecycle_closeout_record_id"], "preservation_state": r["preservation_state"], "preserved_refs": len(r["preserved_refs"])} for r in load_preservation_records(p)]


def _preservation_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "preservation/preservation-verification-receipts.json"
    return [] if not p.exists() else [{"preservation_verification_receipt_id": r["preservation_verification_receipt_id"], "preservation_record_id": r["preservation_record_id"], "verification_state": r["verification_state"], "checked_preserved_refs": len(r["checked_preserved_refs"])} for r in load_preservation_verification_receipts(p)]


def _preservation_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "preservation/preservation-closeout-records.json"
    return [] if not p.exists() else [{"preservation_closeout_record_id": r["preservation_closeout_record_id"], "preservation_verification_receipt_id": r["preservation_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_preservation_closeout_records(p)]


def _preservation_chain_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "preservation/preservation-chain-records.json"
    return [] if not p.exists() else [{"preservation_chain_record_id": r["preservation_chain_record_id"], "terminal_ref": r["terminal_ref"], "chain_state": r["chain_state"], "chain_refs": len(r["chain_refs"])} for r in load_preservation_chain_records(p)]


def _preservation_chain_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "preservation/preservation-chain-verification-receipts.json"
    return [] if not p.exists() else [{"preservation_chain_verification_receipt_id": r["preservation_chain_verification_receipt_id"], "preservation_chain_record_id": r["preservation_chain_record_id"], "verification_state": r["verification_state"], "checked_chain_refs": len(r["checked_chain_refs"])} for r in load_preservation_chain_verification_receipts(p)]


def _retention_review_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-review-records.json"
    return [] if not p.exists() else [{"retention_review_record_id": r["retention_review_record_id"], "preservation_chain_verification_receipt_id": r["preservation_chain_verification_receipt_id"], "review_state": r["review_state"], "recommendation": r["recommendation"]} for r in load_retention_review_records(p)]


def _retention_review_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-review-verification-receipts.json"
    return [] if not p.exists() else [{"retention_review_verification_receipt_id": r["retention_review_verification_receipt_id"], "retention_review_record_id": r["retention_review_record_id"], "verification_state": r["verification_state"], "checked_reviewed_refs": len(r["checked_reviewed_refs"])} for r in load_retention_review_verification_receipts(p)]


def _retention_decision_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-decision-records.json"
    return [] if not p.exists() else [{"retention_decision_record_id": r["retention_decision_record_id"], "retention_review_verification_receipt_id": r["retention_review_verification_receipt_id"], "decision_state": r["decision_state"], "decision": r["decision"]} for r in load_retention_decision_records(p)]


def _retention_decision_approval_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-decision-approvals.json"
    return [] if not p.exists() else [{"retention_decision_approval_id": r["retention_decision_approval_id"], "retention_decision_record_id": r["retention_decision_record_id"], "approval_state": r["approval_state"], "approved_decision": r["approved_decision"]} for r in load_retention_decision_approvals(p)]


def _retention_action_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-action-receipts.json"
    return [] if not p.exists() else [{"retention_action_receipt_id": r["retention_action_receipt_id"], "retention_decision_approval_id": r["retention_decision_approval_id"], "action_state": r["action_state"], "action": r["action"]} for r in load_retention_action_receipts(p)]


def _retention_action_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-action-verification-receipts.json"
    return [] if not p.exists() else [{"retention_action_verification_receipt_id": r["retention_action_verification_receipt_id"], "retention_action_receipt_id": r["retention_action_receipt_id"], "verification_state": r["verification_state"], "checked_action_refs": len(r["checked_action_refs"])} for r in load_retention_action_verification_receipts(p)]


def _retention_action_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-action-closeout-records.json"
    return [] if not p.exists() else [{"retention_action_closeout_record_id": r["retention_action_closeout_record_id"], "retention_action_verification_receipt_id": r["retention_action_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_action_closeout_records(p)]


def _retention_chain_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-chain-records.json"
    return [] if not p.exists() else [{"retention_chain_record_id": r["retention_chain_record_id"], "terminal_ref": r["terminal_ref"], "chain_state": r["chain_state"], "chain_refs": len(r["chain_refs"])} for r in load_retention_chain_records(p)]


def _retention_chain_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-chain-verification-receipts.json"
    return [] if not p.exists() else [{"retention_chain_verification_receipt_id": r["retention_chain_verification_receipt_id"], "retention_chain_record_id": r["retention_chain_record_id"], "verification_state": r["verification_state"], "checked_chain_refs": len(r["checked_chain_refs"])} for r in load_retention_chain_verification_receipts(p)]


def _retention_lifecycle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-lifecycle-records.json"
    return [] if not p.exists() else [{"retention_lifecycle_record_id": r["retention_lifecycle_record_id"], "retention_chain_record_id": r["retention_chain_record_id"], "lifecycle_state": r["lifecycle_state"], "lifecycle_refs": len(r["lifecycle_refs"])} for r in load_retention_lifecycle_records(p)]


def _retention_lifecycle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-lifecycle-verification-receipts.json"
    return [] if not p.exists() else [{"retention_lifecycle_verification_receipt_id": r["retention_lifecycle_verification_receipt_id"], "retention_lifecycle_record_id": r["retention_lifecycle_record_id"], "verification_state": r["verification_state"], "checked_lifecycle_refs": len(r["checked_lifecycle_refs"])} for r in load_retention_lifecycle_verification_receipts(p)]


def _retention_lifecycle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-lifecycle-closeout-records.json"
    return [] if not p.exists() else [{"retention_lifecycle_closeout_record_id": r["retention_lifecycle_closeout_record_id"], "retention_lifecycle_verification_receipt_id": r["retention_lifecycle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_lifecycle_closeout_records(p)]


def _retention_ledger_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-ledger-records.json"
    return [] if not p.exists() else [{"retention_ledger_record_id": r["retention_ledger_record_id"], "retention_lifecycle_closeout_record_id": r["retention_lifecycle_closeout_record_id"], "ledger_state": r["ledger_state"], "entry_refs": len(r["entry_refs"])} for r in load_retention_ledger_records(p)]


def _retention_ledger_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-ledger-verification-receipts.json"
    return [] if not p.exists() else [{"retention_ledger_verification_receipt_id": r["retention_ledger_verification_receipt_id"], "retention_ledger_record_id": r["retention_ledger_record_id"], "verification_state": r["verification_state"], "checked_entry_refs": len(r["checked_entry_refs"])} for r in load_retention_ledger_verification_receipts(p)]


def _retention_ledger_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-ledger-closeout-records.json"
    return [] if not p.exists() else [{"retention_ledger_closeout_record_id": r["retention_ledger_closeout_record_id"], "retention_ledger_verification_receipt_id": r["retention_ledger_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_ledger_closeout_records(p)]


def _retention_policy_compliance_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-policy-compliance-records.json"
    return [] if not p.exists() else [{"retention_policy_compliance_record_id": r["retention_policy_compliance_record_id"], "retention_ledger_verification_receipt_id": r["retention_ledger_verification_receipt_id"], "compliance_state": r["compliance_state"], "compliance_scope": r["compliance_scope"]} for r in load_retention_policy_compliance_records(p)]


def _retention_policy_compliance_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-policy-compliance-verification-receipts.json"
    return [] if not p.exists() else [{"retention_policy_compliance_verification_receipt_id": r["retention_policy_compliance_verification_receipt_id"], "retention_policy_compliance_record_id": r["retention_policy_compliance_record_id"], "verification_state": r["verification_state"], "checked_compliance_refs": len(r["checked_compliance_refs"])} for r in load_retention_policy_compliance_verification_receipts(p)]


def _retention_obligation_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-obligation-records.json"
    return [] if not p.exists() else [{"retention_obligation_record_id": r["retention_obligation_record_id"], "retention_policy_compliance_verification_receipt_id": r["retention_policy_compliance_verification_receipt_id"], "obligation_state": r["obligation_state"], "obligation_scope": r["obligation_scope"]} for r in load_retention_obligation_records(p)]


def _retention_obligation_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-obligation-verification-receipts.json"
    return [] if not p.exists() else [{"retention_obligation_verification_receipt_id": r["retention_obligation_verification_receipt_id"], "retention_obligation_record_id": r["retention_obligation_record_id"], "verification_state": r["verification_state"], "checked_obligation_refs": len(r["checked_obligation_refs"])} for r in load_retention_obligation_verification_receipts(p)]


def _retention_schedule_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-schedule-records.json"
    return [] if not p.exists() else [{"retention_schedule_record_id": r["retention_schedule_record_id"], "retention_obligation_record_id": r["retention_obligation_record_id"], "schedule_state": r["schedule_state"], "scheduled_action": r["scheduled_action"]} for r in load_retention_schedule_records(p)]


def _retention_schedule_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-schedule-verification-receipts.json"
    return [] if not p.exists() else [{"retention_schedule_verification_receipt_id": r["retention_schedule_verification_receipt_id"], "retention_schedule_record_id": r["retention_schedule_record_id"], "verification_state": r["verification_state"], "checked_schedule_refs": len(r["checked_schedule_refs"])} for r in load_retention_schedule_verification_receipts(p)]


def _retention_schedule_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-schedule-closeout-records.json"
    return [] if not p.exists() else [{"retention_schedule_closeout_record_id": r["retention_schedule_closeout_record_id"], "retention_schedule_verification_receipt_id": r["retention_schedule_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_schedule_closeout_records(p)]


def _retention_cycle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-cycle-records.json"
    return [] if not p.exists() else [{"retention_cycle_record_id": r["retention_cycle_record_id"], "retention_schedule_closeout_record_id": r["retention_schedule_closeout_record_id"], "cycle_state": r["cycle_state"], "cycle_refs": len(r["cycle_refs"])} for r in load_retention_cycle_records(p)]


def _retention_cycle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-cycle-verification-receipts.json"
    return [] if not p.exists() else [{"retention_cycle_verification_receipt_id": r["retention_cycle_verification_receipt_id"], "retention_cycle_record_id": r["retention_cycle_record_id"], "verification_state": r["verification_state"], "checked_cycle_refs": len(r["checked_cycle_refs"])} for r in load_retention_cycle_verification_receipts(p)]


def _retention_cycle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-cycle-closeout-records.json"
    return [] if not p.exists() else [{"retention_cycle_closeout_record_id": r["retention_cycle_closeout_record_id"], "retention_cycle_verification_receipt_id": r["retention_cycle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_cycle_closeout_records(p)]


def _retention_hold_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-hold-records.json"
    return [] if not p.exists() else [{"retention_hold_record_id": r["retention_hold_record_id"], "retention_cycle_closeout_record_id": r["retention_cycle_closeout_record_id"], "hold_state": r["hold_state"], "hold_scope": r["hold_scope"]} for r in load_retention_hold_records(p)]


def _retention_hold_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-hold-verification-receipts.json"
    return [] if not p.exists() else [{"retention_hold_verification_receipt_id": r["retention_hold_verification_receipt_id"], "retention_hold_record_id": r["retention_hold_record_id"], "verification_state": r["verification_state"], "checked_hold_refs": len(r["checked_hold_refs"])} for r in load_retention_hold_verification_receipts(p)]


def _retention_hold_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-hold-closeout-records.json"
    return [] if not p.exists() else [{"retention_hold_closeout_record_id": r["retention_hold_closeout_record_id"], "retention_hold_verification_receipt_id": r["retention_hold_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_hold_closeout_records(p)]


def _retention_status_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-status-snapshot-records.json"
    return [] if not p.exists() else [{"retention_status_snapshot_record_id": r["retention_status_snapshot_record_id"], "retention_hold_closeout_record_id": r["retention_hold_closeout_record_id"], "snapshot_state": r["snapshot_state"], "retention_status": r["retention_status"]} for r in load_retention_status_snapshot_records(p)]


def _retention_status_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-status-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_status_snapshot_verification_receipt_id": r["retention_status_snapshot_verification_receipt_id"], "retention_status_snapshot_record_id": r["retention_status_snapshot_record_id"], "verification_state": r["verification_state"], "checked_snapshot_refs": len(r["checked_snapshot_refs"])} for r in load_retention_status_snapshot_verification_receipts(p)]


def _retention_rollup_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-rollup-records.json"
    return [] if not p.exists() else [{"retention_rollup_record_id": r["retention_rollup_record_id"], "retention_status_snapshot_record_id": r["retention_status_snapshot_record_id"], "rollup_state": r["rollup_state"], "rollup_scope": r["rollup_scope"]} for r in load_retention_rollup_records(p)]


def _retention_rollup_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-rollup-verification-receipts.json"
    return [] if not p.exists() else [{"retention_rollup_verification_receipt_id": r["retention_rollup_verification_receipt_id"], "retention_rollup_record_id": r["retention_rollup_record_id"], "verification_state": r["verification_state"], "checked_rollup_refs": len(r["checked_rollup_refs"])} for r in load_retention_rollup_verification_receipts(p)]


def _retention_rollup_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-rollup-closeout-records.json"
    return [] if not p.exists() else [{"retention_rollup_closeout_record_id": r["retention_rollup_closeout_record_id"], "retention_rollup_verification_receipt_id": r["retention_rollup_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_rollup_closeout_records(p)]


def _retention_report_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-report-records.json"
    return [] if not p.exists() else [{"retention_report_record_id": r["retention_report_record_id"], "retention_rollup_closeout_record_id": r["retention_rollup_closeout_record_id"], "report_state": r["report_state"], "report_scope": r["report_scope"]} for r in load_retention_report_records(p)]


def _retention_report_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-report-verification-receipts.json"
    return [] if not p.exists() else [{"retention_report_verification_receipt_id": r["retention_report_verification_receipt_id"], "retention_report_record_id": r["retention_report_record_id"], "verification_state": r["verification_state"], "checked_report_refs": len(r["checked_report_refs"])} for r in load_retention_report_verification_receipts(p)]


def _retention_report_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-report-closeout-records.json"
    return [] if not p.exists() else [{"retention_report_closeout_record_id": r["retention_report_closeout_record_id"], "retention_report_verification_receipt_id": r["retention_report_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_report_closeout_records(p)]


def _retention_publication_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-records.json"
    return [] if not p.exists() else [{"retention_publication_record_id": r["retention_publication_record_id"], "retention_report_closeout_record_id": r["retention_report_closeout_record_id"], "publication_state": r["publication_state"], "publication_scope": r["publication_scope"]} for r in load_retention_publication_records(p)]


def _retention_publication_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-verification-receipts.json"
    return [] if not p.exists() else [{"retention_publication_verification_receipt_id": r["retention_publication_verification_receipt_id"], "retention_publication_record_id": r["retention_publication_record_id"], "verification_state": r["verification_state"], "checked_publication_refs": len(r["checked_publication_refs"])} for r in load_retention_publication_verification_receipts(p)]


def _retention_publication_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-closeout-records.json"
    return [] if not p.exists() else [{"retention_publication_closeout_record_id": r["retention_publication_closeout_record_id"], "retention_publication_verification_receipt_id": r["retention_publication_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_publication_closeout_records(p)]


def _retention_dashboard_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-dashboard-snapshot-records.json"
    return [] if not p.exists() else [{"retention_dashboard_snapshot_record_id": r["retention_dashboard_snapshot_record_id"], "retention_publication_closeout_record_id": r["retention_publication_closeout_record_id"], "snapshot_state": r["snapshot_state"], "dashboard_status": r["dashboard_status"]} for r in load_retention_dashboard_snapshot_records(p)]


def _retention_dashboard_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-dashboard-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_dashboard_snapshot_verification_receipt_id": r["retention_dashboard_snapshot_verification_receipt_id"], "retention_dashboard_snapshot_record_id": r["retention_dashboard_snapshot_record_id"], "verification_state": r["verification_state"], "checked_dashboard_refs": len(r["checked_dashboard_refs"])} for r in load_retention_dashboard_snapshot_verification_receipts(p)]


def _retention_dashboard_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-dashboard-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_dashboard_snapshot_closeout_record_id": r["retention_dashboard_snapshot_closeout_record_id"], "retention_dashboard_snapshot_verification_receipt_id": r["retention_dashboard_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_dashboard_snapshot_closeout_records(p)]


def _retention_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-summary-records.json"
    return [] if not p.exists() else [{"retention_summary_record_id": r["retention_summary_record_id"], "retention_dashboard_snapshot_closeout_record_id": r["retention_dashboard_snapshot_closeout_record_id"], "summary_state": r["summary_state"], "summary_scope": r["summary_scope"]} for r in load_retention_summary_records(p)]


def _retention_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_summary_verification_receipt_id": r["retention_summary_verification_receipt_id"], "retention_summary_record_id": r["retention_summary_record_id"], "verification_state": r["verification_state"], "checked_summary_refs": len(r["checked_summary_refs"])} for r in load_retention_summary_verification_receipts(p)]


def _retention_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_summary_closeout_record_id": r["retention_summary_closeout_record_id"], "retention_summary_verification_receipt_id": r["retention_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_summary_closeout_records(p)]


def _retention_export_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-export-records.json"
    return [] if not p.exists() else [{"retention_export_record_id": r["retention_export_record_id"], "retention_summary_closeout_record_id": r["retention_summary_closeout_record_id"], "export_state": r["export_state"], "export_scope": r["export_scope"]} for r in load_retention_export_records(p)]


def _retention_export_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-export-verification-receipts.json"
    return [] if not p.exists() else [{"retention_export_verification_receipt_id": r["retention_export_verification_receipt_id"], "retention_export_record_id": r["retention_export_record_id"], "verification_state": r["verification_state"], "checked_export_refs": len(r["checked_export_refs"])} for r in load_retention_export_verification_receipts(p)]


def _retention_export_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-export-closeout-records.json"
    return [] if not p.exists() else [{"retention_export_closeout_record_id": r["retention_export_closeout_record_id"], "retention_export_verification_receipt_id": r["retention_export_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_export_closeout_records(p)]


def _retention_handoff_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-handoff-records.json"
    return [] if not p.exists() else [{"retention_handoff_record_id": r["retention_handoff_record_id"], "retention_export_closeout_record_id": r["retention_export_closeout_record_id"], "handoff_state": r["handoff_state"], "handoff_scope": r["handoff_scope"]} for r in load_retention_handoff_records(p)]


def _retention_handoff_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-handoff-verification-receipts.json"
    return [] if not p.exists() else [{"retention_handoff_verification_receipt_id": r["retention_handoff_verification_receipt_id"], "retention_handoff_record_id": r["retention_handoff_record_id"], "verification_state": r["verification_state"], "checked_handoff_refs": len(r["checked_handoff_refs"])} for r in load_retention_handoff_verification_receipts(p)]


def _retention_handoff_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-handoff-closeout-records.json"
    return [] if not p.exists() else [{"retention_handoff_closeout_record_id": r["retention_handoff_closeout_record_id"], "retention_handoff_verification_receipt_id": r["retention_handoff_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_handoff_closeout_records(p)]


def _retention_acceptance_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-acceptance-records.json"
    return [] if not p.exists() else [{"retention_acceptance_record_id": r["retention_acceptance_record_id"], "retention_handoff_closeout_record_id": r["retention_handoff_closeout_record_id"], "acceptance_state": r["acceptance_state"], "acceptance_scope": r["acceptance_scope"]} for r in load_retention_acceptance_records(p)]


def _retention_acceptance_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-acceptance-verification-receipts.json"
    return [] if not p.exists() else [{"retention_acceptance_verification_receipt_id": r["retention_acceptance_verification_receipt_id"], "retention_acceptance_record_id": r["retention_acceptance_record_id"], "verification_state": r["verification_state"], "checked_acceptance_refs": len(r["checked_acceptance_refs"])} for r in load_retention_acceptance_verification_receipts(p)]


def _retention_acceptance_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-acceptance-closeout-records.json"
    return [] if not p.exists() else [{"retention_acceptance_closeout_record_id": r["retention_acceptance_closeout_record_id"], "retention_acceptance_verification_receipt_id": r["retention_acceptance_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_acceptance_closeout_records(p)]


def _retention_package_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-package-records.json"
    return [] if not p.exists() else [{"retention_package_record_id": r["retention_package_record_id"], "retention_acceptance_closeout_record_id": r["retention_acceptance_closeout_record_id"], "package_state": r["package_state"], "package_scope": r["package_scope"]} for r in load_retention_package_records(p)]


def _retention_package_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-package-verification-receipts.json"
    return [] if not p.exists() else [{"retention_package_verification_receipt_id": r["retention_package_verification_receipt_id"], "retention_package_record_id": r["retention_package_record_id"], "verification_state": r["verification_state"], "checked_package_refs": len(r["checked_package_refs"])} for r in load_retention_package_verification_receipts(p)]


def _retention_package_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-package-closeout-records.json"
    return [] if not p.exists() else [{"retention_package_closeout_record_id": r["retention_package_closeout_record_id"], "retention_package_verification_receipt_id": r["retention_package_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_package_closeout_records(p)]


def _retention_finalization_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-finalization-records.json"
    return [] if not p.exists() else [{"retention_finalization_record_id": r["retention_finalization_record_id"], "retention_package_closeout_record_id": r["retention_package_closeout_record_id"], "finalization_state": r["finalization_state"], "finalization_scope": r["finalization_scope"]} for r in load_retention_finalization_records(p)]


def _retention_finalization_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-finalization-verification-receipts.json"
    return [] if not p.exists() else [{"retention_finalization_verification_receipt_id": r["retention_finalization_verification_receipt_id"], "retention_finalization_record_id": r["retention_finalization_record_id"], "verification_state": r["verification_state"], "checked_finalization_refs": len(r["checked_finalization_refs"])} for r in load_retention_finalization_verification_receipts(p)]


def _retention_finalization_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-finalization-closeout-records.json"
    return [] if not p.exists() else [{"retention_finalization_closeout_record_id": r["retention_finalization_closeout_record_id"], "retention_finalization_verification_receipt_id": r["retention_finalization_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_finalization_closeout_records(p)]


def _retention_terminal_status_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-status-records.json"
    return [] if not p.exists() else [{"retention_terminal_status_record_id": r["retention_terminal_status_record_id"], "retention_finalization_closeout_record_id": r["retention_finalization_closeout_record_id"], "terminal_state": r["terminal_state"], "retention_status": r["retention_status"]} for r in load_retention_terminal_status_records(p)]


def _retention_terminal_status_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-status-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_status_verification_receipt_id": r["retention_terminal_status_verification_receipt_id"], "retention_terminal_status_record_id": r["retention_terminal_status_record_id"], "verification_state": r["verification_state"], "checked_terminal_refs": len(r["checked_terminal_refs"])} for r in load_retention_terminal_status_verification_receipts(p)]


def _retention_terminal_status_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-status-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_status_closeout_record_id": r["retention_terminal_status_closeout_record_id"], "retention_terminal_status_verification_receipt_id": r["retention_terminal_status_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_status_closeout_records(p)]


def _retention_certificate_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-certificate-records.json"
    return [] if not p.exists() else [{"retention_certificate_record_id": r["retention_certificate_record_id"], "retention_terminal_status_closeout_record_id": r["retention_terminal_status_closeout_record_id"], "certificate_state": r["certificate_state"], "certificate_scope": r["certificate_scope"]} for r in load_retention_certificate_records(p)]


def _retention_certificate_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-certificate-verification-receipts.json"
    return [] if not p.exists() else [{"retention_certificate_verification_receipt_id": r["retention_certificate_verification_receipt_id"], "retention_certificate_record_id": r["retention_certificate_record_id"], "verification_state": r["verification_state"], "checked_certificate_refs": len(r["checked_certificate_refs"])} for r in load_retention_certificate_verification_receipts(p)]


def _retention_certificate_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-certificate-closeout-records.json"
    return [] if not p.exists() else [{"retention_certificate_closeout_record_id": r["retention_certificate_closeout_record_id"], "retention_certificate_verification_receipt_id": r["retention_certificate_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_certificate_closeout_records(p)]


def _retention_registry_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-registry-records.json"
    return [] if not p.exists() else [{"retention_registry_record_id": r["retention_registry_record_id"], "retention_certificate_closeout_record_id": r["retention_certificate_closeout_record_id"], "registry_state": r["registry_state"], "registry_scope": r["registry_scope"]} for r in load_retention_registry_records(p)]


def _retention_registry_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-registry-verification-receipts.json"
    return [] if not p.exists() else [{"retention_registry_verification_receipt_id": r["retention_registry_verification_receipt_id"], "retention_registry_record_id": r["retention_registry_record_id"], "verification_state": r["verification_state"], "checked_registry_refs": len(r["checked_registry_refs"])} for r in load_retention_registry_verification_receipts(p)]


def _retention_registry_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-registry-closeout-records.json"
    return [] if not p.exists() else [{"retention_registry_closeout_record_id": r["retention_registry_closeout_record_id"], "retention_registry_verification_receipt_id": r["retention_registry_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_registry_closeout_records(p)]


def _retention_closure_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-closure-records.json"
    return [] if not p.exists() else [{"retention_closure_record_id": r["retention_closure_record_id"], "retention_registry_closeout_record_id": r["retention_registry_closeout_record_id"], "closure_state": r["closure_state"], "closure_scope": r["closure_scope"]} for r in load_retention_closure_records(p)]


def _retention_closure_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-closure-verification-receipts.json"
    return [] if not p.exists() else [{"retention_closure_verification_receipt_id": r["retention_closure_verification_receipt_id"], "retention_closure_record_id": r["retention_closure_record_id"], "verification_state": r["verification_state"], "checked_closure_refs": len(r["checked_closure_refs"])} for r in load_retention_closure_verification_receipts(p)]


def _retention_closure_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-closure-closeout-records.json"
    return [] if not p.exists() else [{"retention_closure_closeout_record_id": r["retention_closure_closeout_record_id"], "retention_closure_verification_receipt_id": r["retention_closure_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_closure_closeout_records(p)]


def _retention_completion_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-completion-records.json"
    return [] if not p.exists() else [{"retention_completion_record_id": r["retention_completion_record_id"], "retention_closure_closeout_record_id": r["retention_closure_closeout_record_id"], "completion_state": r["completion_state"], "completion_scope": r["completion_scope"]} for r in load_retention_completion_records(p)]


def _retention_completion_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-completion-verification-receipts.json"
    return [] if not p.exists() else [{"retention_completion_verification_receipt_id": r["retention_completion_verification_receipt_id"], "retention_completion_record_id": r["retention_completion_record_id"], "verification_state": r["verification_state"], "checked_completion_refs": len(r["checked_completion_refs"])} for r in load_retention_completion_verification_receipts(p)]


def _retention_completion_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-completion-closeout-records.json"
    return [] if not p.exists() else [{"retention_completion_closeout_record_id": r["retention_completion_closeout_record_id"], "retention_completion_verification_receipt_id": r["retention_completion_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_completion_closeout_records(p)]


def _retention_attestation_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-attestation-records.json"
    return [] if not p.exists() else [{"retention_attestation_record_id": r["retention_attestation_record_id"], "retention_completion_closeout_record_id": r["retention_completion_closeout_record_id"], "attestation_state": r["attestation_state"], "attestation_scope": r["attestation_scope"]} for r in load_retention_attestation_records(p)]


def _retention_attestation_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-attestation-verification-receipts.json"
    return [] if not p.exists() else [{"retention_attestation_verification_receipt_id": r["retention_attestation_verification_receipt_id"], "retention_attestation_record_id": r["retention_attestation_record_id"], "verification_state": r["verification_state"], "checked_attestation_refs": len(r["checked_attestation_refs"])} for r in load_retention_attestation_verification_receipts(p)]


def _retention_attestation_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-attestation-closeout-records.json"
    return [] if not p.exists() else [{"retention_attestation_closeout_record_id": r["retention_attestation_closeout_record_id"], "retention_attestation_verification_receipt_id": r["retention_attestation_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_attestation_closeout_records(p)]


def _retention_seal_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-seal-records.json"
    return [] if not p.exists() else [{"retention_seal_record_id": r["retention_seal_record_id"], "retention_attestation_closeout_record_id": r["retention_attestation_closeout_record_id"], "seal_state": r["seal_state"], "seal_scope": r["seal_scope"]} for r in load_retention_seal_records(p)]


def _retention_seal_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-seal-verification-receipts.json"
    return [] if not p.exists() else [{"retention_seal_verification_receipt_id": r["retention_seal_verification_receipt_id"], "retention_seal_record_id": r["retention_seal_record_id"], "verification_state": r["verification_state"], "checked_seal_refs": len(r["checked_seal_refs"])} for r in load_retention_seal_verification_receipts(p)]


def _retention_seal_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-seal-closeout-records.json"
    return [] if not p.exists() else [{"retention_seal_closeout_record_id": r["retention_seal_closeout_record_id"], "retention_seal_verification_receipt_id": r["retention_seal_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_seal_closeout_records(p)]


def _retention_notarization_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-notarization-records.json"
    return [] if not p.exists() else [{"retention_notarization_record_id": r["retention_notarization_record_id"], "retention_seal_closeout_record_id": r["retention_seal_closeout_record_id"], "notarization_state": r["notarization_state"], "notarization_scope": r["notarization_scope"]} for r in load_retention_notarization_records(p)]


def _retention_notarization_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-notarization-verification-receipts.json"
    return [] if not p.exists() else [{"retention_notarization_verification_receipt_id": r["retention_notarization_verification_receipt_id"], "retention_notarization_record_id": r["retention_notarization_record_id"], "verification_state": r["verification_state"], "checked_notarization_refs": len(r["checked_notarization_refs"])} for r in load_retention_notarization_verification_receipts(p)]


def _retention_notarization_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-notarization-closeout-records.json"
    return [] if not p.exists() else [{"retention_notarization_closeout_record_id": r["retention_notarization_closeout_record_id"], "retention_notarization_verification_receipt_id": r["retention_notarization_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_notarization_closeout_records(p)]


def _retention_archive_anchor_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-anchor-records.json"
    return [] if not p.exists() else [{"retention_archive_anchor_record_id": r["retention_archive_anchor_record_id"], "retention_notarization_closeout_record_id": r["retention_notarization_closeout_record_id"], "archive_anchor_state": r["archive_anchor_state"], "archive_anchor_scope": r["archive_anchor_scope"]} for r in load_retention_archive_anchor_records(p)]


def _retention_archive_anchor_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-anchor-verification-receipts.json"
    return [] if not p.exists() else [{"retention_archive_anchor_verification_receipt_id": r["retention_archive_anchor_verification_receipt_id"], "retention_archive_anchor_record_id": r["retention_archive_anchor_record_id"], "verification_state": r["verification_state"], "checked_archive_anchor_refs": len(r["checked_archive_anchor_refs"])} for r in load_retention_archive_anchor_verification_receipts(p)]


def _retention_archive_anchor_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-anchor-closeout-records.json"
    return [] if not p.exists() else [{"retention_archive_anchor_closeout_record_id": r["retention_archive_anchor_closeout_record_id"], "retention_archive_anchor_verification_receipt_id": r["retention_archive_anchor_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_archive_anchor_closeout_records(p)]


def _retention_endcap_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-endcap-records.json"
    return [] if not p.exists() else [{"retention_endcap_record_id": r["retention_endcap_record_id"], "retention_archive_anchor_closeout_record_id": r["retention_archive_anchor_closeout_record_id"], "endcap_state": r["endcap_state"], "endcap_scope": r["endcap_scope"]} for r in load_retention_endcap_records(p)]


def _retention_endcap_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-endcap-verification-receipts.json"
    return [] if not p.exists() else [{"retention_endcap_verification_receipt_id": r["retention_endcap_verification_receipt_id"], "retention_endcap_record_id": r["retention_endcap_record_id"], "verification_state": r["verification_state"], "checked_endcap_refs": len(r["checked_endcap_refs"])} for r in load_retention_endcap_verification_receipts(p)]


def _retention_endcap_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-endcap-closeout-records.json"
    return [] if not p.exists() else [{"retention_endcap_closeout_record_id": r["retention_endcap_closeout_record_id"], "retention_endcap_verification_receipt_id": r["retention_endcap_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_endcap_closeout_records(p)]


def _retention_final_index_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-index-records.json"
    return [] if not p.exists() else [{"retention_final_index_record_id": r["retention_final_index_record_id"], "retention_endcap_closeout_record_id": r["retention_endcap_closeout_record_id"], "final_index_state": r["final_index_state"], "final_index_scope": r["final_index_scope"]} for r in load_retention_final_index_records(p)]


def _retention_final_index_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-index-verification-receipts.json"
    return [] if not p.exists() else [{"retention_final_index_verification_receipt_id": r["retention_final_index_verification_receipt_id"], "retention_final_index_record_id": r["retention_final_index_record_id"], "verification_state": r["verification_state"], "checked_final_index_refs": len(r["checked_final_index_refs"])} for r in load_retention_final_index_verification_receipts(p)]


def _retention_final_index_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-index-closeout-records.json"
    return [] if not p.exists() else [{"retention_final_index_closeout_record_id": r["retention_final_index_closeout_record_id"], "retention_final_index_verification_receipt_id": r["retention_final_index_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_final_index_closeout_records(p)]


def _retention_master_ledger_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-master-ledger-records.json"
    return [] if not p.exists() else [{"retention_master_ledger_record_id": r["retention_master_ledger_record_id"], "retention_final_index_closeout_record_id": r["retention_final_index_closeout_record_id"], "master_ledger_state": r["master_ledger_state"], "master_ledger_scope": r["master_ledger_scope"]} for r in load_retention_master_ledger_records(p)]


def _retention_master_ledger_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-master-ledger-verification-receipts.json"
    return [] if not p.exists() else [{"retention_master_ledger_verification_receipt_id": r["retention_master_ledger_verification_receipt_id"], "retention_master_ledger_record_id": r["retention_master_ledger_record_id"], "verification_state": r["verification_state"], "checked_master_ledger_refs": len(r["checked_master_ledger_refs"])} for r in load_retention_master_ledger_verification_receipts(p)]


def _retention_master_ledger_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-master-ledger-closeout-records.json"
    return [] if not p.exists() else [{"retention_master_ledger_closeout_record_id": r["retention_master_ledger_closeout_record_id"], "retention_master_ledger_verification_receipt_id": r["retention_master_ledger_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_master_ledger_closeout_records(p)]


def _retention_terminal_manifest_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-manifest-records.json"
    return [] if not p.exists() else [{"retention_terminal_manifest_record_id": r["retention_terminal_manifest_record_id"], "retention_master_ledger_closeout_record_id": r["retention_master_ledger_closeout_record_id"], "terminal_manifest_state": r["terminal_manifest_state"], "terminal_manifest_scope": r["terminal_manifest_scope"]} for r in load_retention_terminal_manifest_records(p)]


def _retention_terminal_manifest_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-manifest-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_manifest_verification_receipt_id": r["retention_terminal_manifest_verification_receipt_id"], "retention_terminal_manifest_record_id": r["retention_terminal_manifest_record_id"], "verification_state": r["verification_state"], "checked_terminal_manifest_refs": len(r["checked_terminal_manifest_refs"])} for r in load_retention_terminal_manifest_verification_receipts(p)]


def _retention_terminal_manifest_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-manifest-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_manifest_closeout_record_id": r["retention_terminal_manifest_closeout_record_id"], "retention_terminal_manifest_verification_receipt_id": r["retention_terminal_manifest_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_manifest_closeout_records(p)]


def _retention_repository_release_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-repository-release-records.json"
    return [] if not p.exists() else [{"retention_repository_release_record_id": r["retention_repository_release_record_id"], "retention_terminal_manifest_closeout_record_id": r["retention_terminal_manifest_closeout_record_id"], "repository_release_state": r["repository_release_state"], "repository_release_scope": r["repository_release_scope"]} for r in load_retention_repository_release_records(p)]


def _retention_repository_release_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-repository-release-verification-receipts.json"
    return [] if not p.exists() else [{"retention_repository_release_verification_receipt_id": r["retention_repository_release_verification_receipt_id"], "retention_repository_release_record_id": r["retention_repository_release_record_id"], "verification_state": r["verification_state"], "checked_repository_release_refs": len(r["checked_repository_release_refs"])} for r in load_retention_repository_release_verification_receipts(p)]


def _retention_repository_release_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-repository-release-closeout-records.json"
    return [] if not p.exists() else [{"retention_repository_release_closeout_record_id": r["retention_repository_release_closeout_record_id"], "retention_repository_release_verification_receipt_id": r["retention_repository_release_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_repository_release_closeout_records(p)]


def _retention_deployment_release_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-deployment-release-records.json"
    return [] if not p.exists() else [{"retention_deployment_release_record_id": r["retention_deployment_release_record_id"], "retention_repository_release_closeout_record_id": r["retention_repository_release_closeout_record_id"], "deployment_release_state": r["deployment_release_state"], "deployment_release_scope": r["deployment_release_scope"]} for r in load_retention_deployment_release_records(p)]


def _retention_deployment_release_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-deployment-release-verification-receipts.json"
    return [] if not p.exists() else [{"retention_deployment_release_verification_receipt_id": r["retention_deployment_release_verification_receipt_id"], "retention_deployment_release_record_id": r["retention_deployment_release_record_id"], "verification_state": r["verification_state"], "checked_deployment_release_refs": len(r["checked_deployment_release_refs"])} for r in load_retention_deployment_release_verification_receipts(p)]


def _retention_deployment_release_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-deployment-release-closeout-records.json"
    return [] if not p.exists() else [{"retention_deployment_release_closeout_record_id": r["retention_deployment_release_closeout_record_id"], "retention_deployment_release_verification_receipt_id": r["retention_deployment_release_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_deployment_release_closeout_records(p)]


def _retention_availability_notice_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-availability-notice-records.json"
    return [] if not p.exists() else [{"retention_availability_notice_record_id": r["retention_availability_notice_record_id"], "retention_deployment_release_closeout_record_id": r["retention_deployment_release_closeout_record_id"], "availability_notice_state": r["availability_notice_state"], "availability_notice_scope": r["availability_notice_scope"]} for r in load_retention_availability_notice_records(p)]


def _retention_availability_notice_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-availability-notice-verification-receipts.json"
    return [] if not p.exists() else [{"retention_availability_notice_verification_receipt_id": r["retention_availability_notice_verification_receipt_id"], "retention_availability_notice_record_id": r["retention_availability_notice_record_id"], "verification_state": r["verification_state"], "checked_availability_notice_refs": len(r["checked_availability_notice_refs"])} for r in load_retention_availability_notice_verification_receipts(p)]


def _retention_availability_notice_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-availability-notice-closeout-records.json"
    return [] if not p.exists() else [{"retention_availability_notice_closeout_record_id": r["retention_availability_notice_closeout_record_id"], "retention_availability_notice_verification_receipt_id": r["retention_availability_notice_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_availability_notice_closeout_records(p)]


def _retention_release_acknowledgement_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acknowledgement-records.json"
    return [] if not p.exists() else [{"retention_release_acknowledgement_record_id": r["retention_release_acknowledgement_record_id"], "retention_availability_notice_closeout_record_id": r["retention_availability_notice_closeout_record_id"], "release_acknowledgement_state": r["release_acknowledgement_state"], "release_acknowledgement_scope": r["release_acknowledgement_scope"]} for r in load_retention_release_acknowledgement_records(p)]


def _retention_release_acknowledgement_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acknowledgement-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_acknowledgement_verification_receipt_id": r["retention_release_acknowledgement_verification_receipt_id"], "retention_release_acknowledgement_record_id": r["retention_release_acknowledgement_record_id"], "verification_state": r["verification_state"], "checked_release_acknowledgement_refs": len(r["checked_release_acknowledgement_refs"])} for r in load_retention_release_acknowledgement_verification_receipts(p)]


def _retention_release_acknowledgement_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acknowledgement-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_acknowledgement_closeout_record_id": r["retention_release_acknowledgement_closeout_record_id"], "retention_release_acknowledgement_verification_receipt_id": r["retention_release_acknowledgement_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_acknowledgement_closeout_records(p)]


def _retention_release_confirmation_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-confirmation-records.json"
    return [] if not p.exists() else [{"retention_release_confirmation_record_id": r["retention_release_confirmation_record_id"], "retention_release_acknowledgement_closeout_record_id": r["retention_release_acknowledgement_closeout_record_id"], "release_confirmation_state": r["release_confirmation_state"], "release_confirmation_scope": r["release_confirmation_scope"]} for r in load_retention_release_confirmation_records(p)]


def _retention_release_confirmation_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-confirmation-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_confirmation_verification_receipt_id": r["retention_release_confirmation_verification_receipt_id"], "retention_release_confirmation_record_id": r["retention_release_confirmation_record_id"], "verification_state": r["verification_state"], "checked_release_confirmation_refs": len(r["checked_release_confirmation_refs"])} for r in load_retention_release_confirmation_verification_receipts(p)]


def _retention_release_confirmation_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-confirmation-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_confirmation_closeout_record_id": r["retention_release_confirmation_closeout_record_id"], "retention_release_confirmation_verification_receipt_id": r["retention_release_confirmation_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_confirmation_closeout_records(p)]


def _retention_distribution_package_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-package-records.json"
    return [] if not p.exists() else [{"retention_distribution_package_record_id": r["retention_distribution_package_record_id"], "retention_release_confirmation_closeout_record_id": r["retention_release_confirmation_closeout_record_id"], "distribution_package_state": r["distribution_package_state"], "distribution_package_scope": r["distribution_package_scope"]} for r in load_retention_distribution_package_records(p)]


def _retention_distribution_package_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-package-verification-receipts.json"
    return [] if not p.exists() else [{"retention_distribution_package_verification_receipt_id": r["retention_distribution_package_verification_receipt_id"], "retention_distribution_package_record_id": r["retention_distribution_package_record_id"], "verification_state": r["verification_state"], "checked_distribution_package_refs": len(r["checked_distribution_package_refs"])} for r in load_retention_distribution_package_verification_receipts(p)]


def _retention_distribution_package_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-package-closeout-records.json"
    return [] if not p.exists() else [{"retention_distribution_package_closeout_record_id": r["retention_distribution_package_closeout_record_id"], "retention_distribution_package_verification_receipt_id": r["retention_distribution_package_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_distribution_package_closeout_records(p)]


def _retention_distribution_manifest_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-manifest-records.json"
    return [] if not p.exists() else [{"retention_distribution_manifest_record_id": r["retention_distribution_manifest_record_id"], "retention_distribution_package_closeout_record_id": r["retention_distribution_package_closeout_record_id"], "distribution_manifest_state": r["distribution_manifest_state"], "distribution_manifest_scope": r["distribution_manifest_scope"]} for r in load_retention_distribution_manifest_records(p)]


def _retention_distribution_manifest_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-manifest-verification-receipts.json"
    return [] if not p.exists() else [{"retention_distribution_manifest_verification_receipt_id": r["retention_distribution_manifest_verification_receipt_id"], "retention_distribution_manifest_record_id": r["retention_distribution_manifest_record_id"], "verification_state": r["verification_state"], "checked_distribution_manifest_refs": len(r["checked_distribution_manifest_refs"])} for r in load_retention_distribution_manifest_verification_receipts(p)]


def _retention_distribution_manifest_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-manifest-closeout-records.json"
    return [] if not p.exists() else [{"retention_distribution_manifest_closeout_record_id": r["retention_distribution_manifest_closeout_record_id"], "retention_distribution_manifest_verification_receipt_id": r["retention_distribution_manifest_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_distribution_manifest_closeout_records(p)]


def _retention_access_publication_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-publication-records.json"
    return [] if not p.exists() else [{"retention_access_publication_record_id": r["retention_access_publication_record_id"], "retention_distribution_manifest_closeout_record_id": r["retention_distribution_manifest_closeout_record_id"], "access_publication_state": r["access_publication_state"], "access_publication_scope": r["access_publication_scope"]} for r in load_retention_access_publication_records(p)]


def _retention_access_publication_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-publication-verification-receipts.json"
    return [] if not p.exists() else [{"retention_access_publication_verification_receipt_id": r["retention_access_publication_verification_receipt_id"], "retention_access_publication_record_id": r["retention_access_publication_record_id"], "verification_state": r["verification_state"], "checked_access_publication_refs": len(r["checked_access_publication_refs"])} for r in load_retention_access_publication_verification_receipts(p)]


def _retention_access_publication_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-publication-closeout-records.json"
    return [] if not p.exists() else [{"retention_access_publication_closeout_record_id": r["retention_access_publication_closeout_record_id"], "retention_access_publication_verification_receipt_id": r["retention_access_publication_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_access_publication_closeout_records(p)]


def _retention_access_grant_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-grant-records.json"
    return [] if not p.exists() else [{"retention_access_grant_record_id": r["retention_access_grant_record_id"], "retention_access_publication_closeout_record_id": r["retention_access_publication_closeout_record_id"], "access_grant_state": r["access_grant_state"], "access_grant_scope": r["access_grant_scope"]} for r in load_retention_access_grant_records(p)]


def _retention_access_grant_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-grant-verification-receipts.json"
    return [] if not p.exists() else [{"retention_access_grant_verification_receipt_id": r["retention_access_grant_verification_receipt_id"], "retention_access_grant_record_id": r["retention_access_grant_record_id"], "verification_state": r["verification_state"], "checked_access_grant_refs": len(r["checked_access_grant_refs"])} for r in load_retention_access_grant_verification_receipts(p)]


def _retention_access_grant_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-grant-closeout-records.json"
    return [] if not p.exists() else [{"retention_access_grant_closeout_record_id": r["retention_access_grant_closeout_record_id"], "retention_access_grant_verification_receipt_id": r["retention_access_grant_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_access_grant_closeout_records(p)]


def _retention_access_ledger_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-ledger-records.json"
    return [] if not p.exists() else [{"retention_access_ledger_record_id": r["retention_access_ledger_record_id"], "retention_access_grant_closeout_record_id": r["retention_access_grant_closeout_record_id"], "access_ledger_state": r["access_ledger_state"], "access_ledger_scope": r["access_ledger_scope"]} for r in load_retention_access_ledger_records(p)]


def _retention_access_ledger_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-ledger-verification-receipts.json"
    return [] if not p.exists() else [{"retention_access_ledger_verification_receipt_id": r["retention_access_ledger_verification_receipt_id"], "retention_access_ledger_record_id": r["retention_access_ledger_record_id"], "verification_state": r["verification_state"], "checked_access_ledger_refs": len(r["checked_access_ledger_refs"])} for r in load_retention_access_ledger_verification_receipts(p)]


def _retention_access_ledger_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-ledger-closeout-records.json"
    return [] if not p.exists() else [{"retention_access_ledger_closeout_record_id": r["retention_access_ledger_closeout_record_id"], "retention_access_ledger_verification_receipt_id": r["retention_access_ledger_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_access_ledger_closeout_records(p)]


def _retention_retrieval_catalog_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-catalog-records.json"
    return [] if not p.exists() else [{"retention_retrieval_catalog_record_id": r["retention_retrieval_catalog_record_id"], "retention_access_ledger_closeout_record_id": r["retention_access_ledger_closeout_record_id"], "retrieval_catalog_state": r["retrieval_catalog_state"], "retrieval_catalog_scope": r["retrieval_catalog_scope"]} for r in load_retention_retrieval_catalog_records(p)]


def _retention_retrieval_catalog_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-catalog-verification-receipts.json"
    return [] if not p.exists() else [{"retention_retrieval_catalog_verification_receipt_id": r["retention_retrieval_catalog_verification_receipt_id"], "retention_retrieval_catalog_record_id": r["retention_retrieval_catalog_record_id"], "verification_state": r["verification_state"], "checked_retrieval_catalog_refs": len(r["checked_retrieval_catalog_refs"])} for r in load_retention_retrieval_catalog_verification_receipts(p)]


def _retention_retrieval_catalog_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-catalog-closeout-records.json"
    return [] if not p.exists() else [{"retention_retrieval_catalog_closeout_record_id": r["retention_retrieval_catalog_closeout_record_id"], "retention_retrieval_catalog_verification_receipt_id": r["retention_retrieval_catalog_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_retrieval_catalog_closeout_records(p)]


def _retention_retrieval_endpoint_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-endpoint-records.json"
    return [] if not p.exists() else [{"retention_retrieval_endpoint_record_id": r["retention_retrieval_endpoint_record_id"], "retention_retrieval_catalog_closeout_record_id": r["retention_retrieval_catalog_closeout_record_id"], "retrieval_endpoint_state": r["retrieval_endpoint_state"], "retrieval_endpoint_scope": r["retrieval_endpoint_scope"]} for r in load_retention_retrieval_endpoint_records(p)]


def _retention_retrieval_endpoint_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-endpoint-verification-receipts.json"
    return [] if not p.exists() else [{"retention_retrieval_endpoint_verification_receipt_id": r["retention_retrieval_endpoint_verification_receipt_id"], "retention_retrieval_endpoint_record_id": r["retention_retrieval_endpoint_record_id"], "verification_state": r["verification_state"], "checked_retrieval_endpoint_refs": len(r["checked_retrieval_endpoint_refs"])} for r in load_retention_retrieval_endpoint_verification_receipts(p)]


def _retention_retrieval_endpoint_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-endpoint-closeout-records.json"
    return [] if not p.exists() else [{"retention_retrieval_endpoint_closeout_record_id": r["retention_retrieval_endpoint_closeout_record_id"], "retention_retrieval_endpoint_verification_receipt_id": r["retention_retrieval_endpoint_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_retrieval_endpoint_closeout_records(p)]


def _retention_retrieval_token_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-token-records.json"
    return [] if not p.exists() else [{"retention_retrieval_token_record_id": r["retention_retrieval_token_record_id"], "retention_retrieval_endpoint_closeout_record_id": r["retention_retrieval_endpoint_closeout_record_id"], "retrieval_token_state": r["retrieval_token_state"], "retrieval_token_scope": r["retrieval_token_scope"]} for r in load_retention_retrieval_token_records(p)]


def _retention_retrieval_token_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-token-verification-receipts.json"
    return [] if not p.exists() else [{"retention_retrieval_token_verification_receipt_id": r["retention_retrieval_token_verification_receipt_id"], "retention_retrieval_token_record_id": r["retention_retrieval_token_record_id"], "verification_state": r["verification_state"], "checked_retrieval_token_refs": len(r["checked_retrieval_token_refs"])} for r in load_retention_retrieval_token_verification_receipts(p)]


def _retention_retrieval_token_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-token-closeout-records.json"
    return [] if not p.exists() else [{"retention_retrieval_token_closeout_record_id": r["retention_retrieval_token_closeout_record_id"], "retention_retrieval_token_verification_receipt_id": r["retention_retrieval_token_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_retrieval_token_closeout_records(p)]


def _retention_consumer_receipt_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-receipt-records.json"
    return [] if not p.exists() else [{"retention_consumer_receipt_record_id": r["retention_consumer_receipt_record_id"], "retention_retrieval_token_closeout_record_id": r["retention_retrieval_token_closeout_record_id"], "consumer_receipt_state": r["consumer_receipt_state"], "consumer_receipt_scope": r["consumer_receipt_scope"]} for r in load_retention_consumer_receipt_records(p)]


def _retention_consumer_receipt_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-receipt-verification-receipts.json"
    return [] if not p.exists() else [{"retention_consumer_receipt_verification_receipt_id": r["retention_consumer_receipt_verification_receipt_id"], "retention_consumer_receipt_record_id": r["retention_consumer_receipt_record_id"], "verification_state": r["verification_state"], "checked_consumer_receipt_refs": len(r["checked_consumer_receipt_refs"])} for r in load_retention_consumer_receipt_verification_receipts(p)]


def _retention_consumer_receipt_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-receipt-closeout-records.json"
    return [] if not p.exists() else [{"retention_consumer_receipt_closeout_record_id": r["retention_consumer_receipt_closeout_record_id"], "retention_consumer_receipt_verification_receipt_id": r["retention_consumer_receipt_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_consumer_receipt_closeout_records(p)]


def _retention_publication_rollup_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-rollup-records.json"
    return [] if not p.exists() else [{"retention_publication_rollup_record_id": r["retention_publication_rollup_record_id"], "retention_consumer_receipt_closeout_record_id": r["retention_consumer_receipt_closeout_record_id"], "publication_rollup_state": r["publication_rollup_state"], "publication_rollup_scope": r["publication_rollup_scope"]} for r in load_retention_publication_rollup_records(p)]


def _retention_publication_rollup_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-rollup-verification-receipts.json"
    return [] if not p.exists() else [{"retention_publication_rollup_verification_receipt_id": r["retention_publication_rollup_verification_receipt_id"], "retention_publication_rollup_record_id": r["retention_publication_rollup_record_id"], "verification_state": r["verification_state"], "checked_publication_rollup_refs": len(r["checked_publication_rollup_refs"])} for r in load_retention_publication_rollup_verification_receipts(p)]


def _retention_publication_rollup_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-rollup-closeout-records.json"
    return [] if not p.exists() else [{"retention_publication_rollup_closeout_record_id": r["retention_publication_rollup_closeout_record_id"], "retention_publication_rollup_verification_receipt_id": r["retention_publication_rollup_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_publication_rollup_closeout_records(p)]


def _retention_distribution_receipt_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-receipt-records.json"
    return [] if not p.exists() else [{"retention_distribution_receipt_record_id": r["retention_distribution_receipt_record_id"], "retention_publication_rollup_closeout_record_id": r["retention_publication_rollup_closeout_record_id"], "distribution_receipt_state": r["distribution_receipt_state"], "distribution_receipt_scope": r["distribution_receipt_scope"]} for r in load_retention_distribution_receipt_records(p)]


def _retention_distribution_receipt_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-receipt-verification-receipts.json"
    return [] if not p.exists() else [{"retention_distribution_receipt_verification_receipt_id": r["retention_distribution_receipt_verification_receipt_id"], "retention_distribution_receipt_record_id": r["retention_distribution_receipt_record_id"], "verification_state": r["verification_state"], "checked_distribution_receipt_refs": len(r["checked_distribution_receipt_refs"])} for r in load_retention_distribution_receipt_verification_receipts(p)]


def _retention_distribution_receipt_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-receipt-closeout-records.json"
    return [] if not p.exists() else [{"retention_distribution_receipt_closeout_record_id": r["retention_distribution_receipt_closeout_record_id"], "retention_distribution_receipt_verification_receipt_id": r["retention_distribution_receipt_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_distribution_receipt_closeout_records(p)]


def _retention_access_audit_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-audit-snapshot-records.json"
    return [] if not p.exists() else [{"retention_access_audit_snapshot_record_id": r["retention_access_audit_snapshot_record_id"], "retention_distribution_receipt_closeout_record_id": r["retention_distribution_receipt_closeout_record_id"], "access_audit_snapshot_state": r["access_audit_snapshot_state"], "access_audit_snapshot_scope": r["access_audit_snapshot_scope"]} for r in load_retention_access_audit_snapshot_records(p)]


def _retention_access_audit_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-audit-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_access_audit_snapshot_verification_receipt_id": r["retention_access_audit_snapshot_verification_receipt_id"], "retention_access_audit_snapshot_record_id": r["retention_access_audit_snapshot_record_id"], "verification_state": r["verification_state"], "checked_access_audit_snapshot_refs": len(r["checked_access_audit_snapshot_refs"])} for r in load_retention_access_audit_snapshot_verification_receipts(p)]


def _retention_access_audit_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-audit-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_access_audit_snapshot_closeout_record_id": r["retention_access_audit_snapshot_closeout_record_id"], "retention_access_audit_snapshot_verification_receipt_id": r["retention_access_audit_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_access_audit_snapshot_closeout_records(p)]


def _retention_release_health_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-health-snapshot-records.json"
    return [] if not p.exists() else [{"retention_release_health_snapshot_record_id": r["retention_release_health_snapshot_record_id"], "retention_access_audit_snapshot_closeout_record_id": r["retention_access_audit_snapshot_closeout_record_id"], "release_health_snapshot_state": r["release_health_snapshot_state"], "release_health_snapshot_scope": r["release_health_snapshot_scope"]} for r in load_retention_release_health_snapshot_records(p)]


def _retention_release_health_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-health-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_health_snapshot_verification_receipt_id": r["retention_release_health_snapshot_verification_receipt_id"], "retention_release_health_snapshot_record_id": r["retention_release_health_snapshot_record_id"], "verification_state": r["verification_state"], "checked_release_health_snapshot_refs": len(r["checked_release_health_snapshot_refs"])} for r in load_retention_release_health_snapshot_verification_receipts(p)]


def _retention_release_health_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-health-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_health_snapshot_closeout_record_id": r["retention_release_health_snapshot_closeout_record_id"], "retention_release_health_snapshot_verification_receipt_id": r["retention_release_health_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_health_snapshot_closeout_records(p)]


def _retention_release_usage_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-usage-summary-records.json"
    return [] if not p.exists() else [{"retention_release_usage_summary_record_id": r["retention_release_usage_summary_record_id"], "retention_release_health_snapshot_closeout_record_id": r["retention_release_health_snapshot_closeout_record_id"], "release_usage_summary_state": r["release_usage_summary_state"], "release_usage_summary_scope": r["release_usage_summary_scope"]} for r in load_retention_release_usage_summary_records(p)]


def _retention_release_usage_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-usage-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_usage_summary_verification_receipt_id": r["retention_release_usage_summary_verification_receipt_id"], "retention_release_usage_summary_record_id": r["retention_release_usage_summary_record_id"], "verification_state": r["verification_state"], "checked_release_usage_summary_refs": len(r["checked_release_usage_summary_refs"])} for r in load_retention_release_usage_summary_verification_receipts(p)]


def _retention_release_usage_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-usage-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_usage_summary_closeout_record_id": r["retention_release_usage_summary_closeout_record_id"], "retention_release_usage_summary_verification_receipt_id": r["retention_release_usage_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_usage_summary_closeout_records(p)]


def _retention_retention_exposure_report_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retention-exposure-report-records.json"
    return [] if not p.exists() else [{"retention_retention_exposure_report_record_id": r["retention_retention_exposure_report_record_id"], "retention_release_usage_summary_closeout_record_id": r["retention_release_usage_summary_closeout_record_id"], "retention_exposure_report_state": r["retention_exposure_report_state"], "retention_exposure_report_scope": r["retention_exposure_report_scope"]} for r in load_retention_retention_exposure_report_records(p)]


def _retention_retention_exposure_report_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retention-exposure-report-verification-receipts.json"
    return [] if not p.exists() else [{"retention_retention_exposure_report_verification_receipt_id": r["retention_retention_exposure_report_verification_receipt_id"], "retention_retention_exposure_report_record_id": r["retention_retention_exposure_report_record_id"], "verification_state": r["verification_state"], "checked_retention_exposure_report_refs": len(r["checked_retention_exposure_report_refs"])} for r in load_retention_retention_exposure_report_verification_receipts(p)]


def _retention_retention_exposure_report_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retention-exposure-report-closeout-records.json"
    return [] if not p.exists() else [{"retention_retention_exposure_report_closeout_record_id": r["retention_retention_exposure_report_closeout_record_id"], "retention_retention_exposure_report_verification_receipt_id": r["retention_retention_exposure_report_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_retention_exposure_report_closeout_records(p)]


def _retention_release_closeout_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-closeout-summary-records.json"
    return [] if not p.exists() else [{"retention_release_closeout_summary_record_id": r["retention_release_closeout_summary_record_id"], "retention_retention_exposure_report_closeout_record_id": r["retention_retention_exposure_report_closeout_record_id"], "release_closeout_summary_state": r["release_closeout_summary_state"], "release_closeout_summary_scope": r["release_closeout_summary_scope"]} for r in load_retention_release_closeout_summary_records(p)]


def _retention_release_closeout_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-closeout-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_closeout_summary_verification_receipt_id": r["retention_release_closeout_summary_verification_receipt_id"], "retention_release_closeout_summary_record_id": r["retention_release_closeout_summary_record_id"], "verification_state": r["verification_state"], "checked_release_closeout_summary_refs": len(r["checked_release_closeout_summary_refs"])} for r in load_retention_release_closeout_summary_verification_receipts(p)]


def _retention_release_closeout_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-closeout-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_closeout_summary_closeout_record_id": r["retention_release_closeout_summary_closeout_record_id"], "retention_release_closeout_summary_verification_receipt_id": r["retention_release_closeout_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_closeout_summary_closeout_records(p)]


def _retention_public_record_index_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-record-index-records.json"
    return [] if not p.exists() else [{"retention_public_record_index_record_id": r["retention_public_record_index_record_id"], "retention_release_closeout_summary_closeout_record_id": r["retention_release_closeout_summary_closeout_record_id"], "public_record_index_state": r["public_record_index_state"], "public_record_index_scope": r["public_record_index_scope"]} for r in load_retention_public_record_index_records(p)]


def _retention_public_record_index_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-record-index-verification-receipts.json"
    return [] if not p.exists() else [{"retention_public_record_index_verification_receipt_id": r["retention_public_record_index_verification_receipt_id"], "retention_public_record_index_record_id": r["retention_public_record_index_record_id"], "verification_state": r["verification_state"], "checked_public_record_index_refs": len(r["checked_public_record_index_refs"])} for r in load_retention_public_record_index_verification_receipts(p)]


def _retention_public_record_index_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-record-index-closeout-records.json"
    return [] if not p.exists() else [{"retention_public_record_index_closeout_record_id": r["retention_public_record_index_closeout_record_id"], "retention_public_record_index_verification_receipt_id": r["retention_public_record_index_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_public_record_index_closeout_records(p)]


def _retention_final_release_bundle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-release-bundle-records.json"
    return [] if not p.exists() else [{"retention_final_release_bundle_record_id": r["retention_final_release_bundle_record_id"], "retention_public_record_index_closeout_record_id": r["retention_public_record_index_closeout_record_id"], "final_release_bundle_state": r["final_release_bundle_state"], "final_release_bundle_scope": r["final_release_bundle_scope"]} for r in load_retention_final_release_bundle_records(p)]


def _retention_final_release_bundle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-release-bundle-verification-receipts.json"
    return [] if not p.exists() else [{"retention_final_release_bundle_verification_receipt_id": r["retention_final_release_bundle_verification_receipt_id"], "retention_final_release_bundle_record_id": r["retention_final_release_bundle_record_id"], "verification_state": r["verification_state"], "checked_final_release_bundle_refs": len(r["checked_final_release_bundle_refs"])} for r in load_retention_final_release_bundle_verification_receipts(p)]


def _retention_final_release_bundle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-release-bundle-closeout-records.json"
    return [] if not p.exists() else [{"retention_final_release_bundle_closeout_record_id": r["retention_final_release_bundle_closeout_record_id"], "retention_final_release_bundle_verification_receipt_id": r["retention_final_release_bundle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_final_release_bundle_closeout_records(p)]


def _retention_terminal_access_notice_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-notice-records.json"
    return [] if not p.exists() else [{"retention_terminal_access_notice_record_id": r["retention_terminal_access_notice_record_id"], "retention_final_release_bundle_closeout_record_id": r["retention_final_release_bundle_closeout_record_id"], "terminal_access_notice_state": r["terminal_access_notice_state"], "terminal_access_notice_scope": r["terminal_access_notice_scope"]} for r in load_retention_terminal_access_notice_records(p)]


def _retention_terminal_access_notice_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-notice-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_access_notice_verification_receipt_id": r["retention_terminal_access_notice_verification_receipt_id"], "retention_terminal_access_notice_record_id": r["retention_terminal_access_notice_record_id"], "verification_state": r["verification_state"], "checked_terminal_access_notice_refs": len(r["checked_terminal_access_notice_refs"])} for r in load_retention_terminal_access_notice_verification_receipts(p)]


def _retention_terminal_access_notice_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-notice-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_access_notice_closeout_record_id": r["retention_terminal_access_notice_closeout_record_id"], "retention_terminal_access_notice_verification_receipt_id": r["retention_terminal_access_notice_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_access_notice_closeout_records(p)]


def _retention_release_acceptance_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acceptance-records.json"
    return [] if not p.exists() else [{"retention_release_acceptance_record_id": r["retention_release_acceptance_record_id"], "retention_terminal_access_notice_closeout_record_id": r["retention_terminal_access_notice_closeout_record_id"], "release_acceptance_state": r["release_acceptance_state"], "release_acceptance_scope": r["release_acceptance_scope"]} for r in load_retention_release_acceptance_records(p)]


def _retention_release_acceptance_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acceptance-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_acceptance_verification_receipt_id": r["retention_release_acceptance_verification_receipt_id"], "retention_release_acceptance_record_id": r["retention_release_acceptance_record_id"], "verification_state": r["verification_state"], "checked_release_acceptance_refs": len(r["checked_release_acceptance_refs"])} for r in load_retention_release_acceptance_verification_receipts(p)]


def _retention_release_acceptance_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-acceptance-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_acceptance_closeout_record_id": r["retention_release_acceptance_closeout_record_id"], "retention_release_acceptance_verification_receipt_id": r["retention_release_acceptance_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_acceptance_closeout_records(p)]


def _retention_access_completion_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-completion-records.json"
    return [] if not p.exists() else [{"retention_access_completion_record_id": r["retention_access_completion_record_id"], "retention_release_acceptance_closeout_record_id": r["retention_release_acceptance_closeout_record_id"], "access_completion_state": r["access_completion_state"], "access_completion_scope": r["access_completion_scope"]} for r in load_retention_access_completion_records(p)]


def _retention_access_completion_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-completion-verification-receipts.json"
    return [] if not p.exists() else [{"retention_access_completion_verification_receipt_id": r["retention_access_completion_verification_receipt_id"], "retention_access_completion_record_id": r["retention_access_completion_record_id"], "verification_state": r["verification_state"], "checked_access_completion_refs": len(r["checked_access_completion_refs"])} for r in load_retention_access_completion_verification_receipts(p)]


def _retention_access_completion_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-access-completion-closeout-records.json"
    return [] if not p.exists() else [{"retention_access_completion_closeout_record_id": r["retention_access_completion_closeout_record_id"], "retention_access_completion_verification_receipt_id": r["retention_access_completion_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_access_completion_closeout_records(p)]


def _retention_publication_certificate_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-certificate-records.json"
    return [] if not p.exists() else [{"retention_publication_certificate_record_id": r["retention_publication_certificate_record_id"], "retention_access_completion_closeout_record_id": r["retention_access_completion_closeout_record_id"], "publication_certificate_state": r["publication_certificate_state"], "publication_certificate_scope": r["publication_certificate_scope"]} for r in load_retention_publication_certificate_records(p)]


def _retention_publication_certificate_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-certificate-verification-receipts.json"
    return [] if not p.exists() else [{"retention_publication_certificate_verification_receipt_id": r["retention_publication_certificate_verification_receipt_id"], "retention_publication_certificate_record_id": r["retention_publication_certificate_record_id"], "verification_state": r["verification_state"], "checked_publication_certificate_refs": len(r["checked_publication_certificate_refs"])} for r in load_retention_publication_certificate_verification_receipts(p)]


def _retention_publication_certificate_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-certificate-closeout-records.json"
    return [] if not p.exists() else [{"retention_publication_certificate_closeout_record_id": r["retention_publication_certificate_closeout_record_id"], "retention_publication_certificate_verification_receipt_id": r["retention_publication_certificate_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_publication_certificate_closeout_records(p)]


def _retention_distribution_closure_notice_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-closure-notice-records.json"
    return [] if not p.exists() else [{"retention_distribution_closure_notice_record_id": r["retention_distribution_closure_notice_record_id"], "retention_publication_certificate_closeout_record_id": r["retention_publication_certificate_closeout_record_id"], "distribution_closure_notice_state": r["distribution_closure_notice_state"], "distribution_closure_notice_scope": r["distribution_closure_notice_scope"]} for r in load_retention_distribution_closure_notice_records(p)]


def _retention_distribution_closure_notice_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-closure-notice-verification-receipts.json"
    return [] if not p.exists() else [{"retention_distribution_closure_notice_verification_receipt_id": r["retention_distribution_closure_notice_verification_receipt_id"], "retention_distribution_closure_notice_record_id": r["retention_distribution_closure_notice_record_id"], "verification_state": r["verification_state"], "checked_distribution_closure_notice_refs": len(r["checked_distribution_closure_notice_refs"])} for r in load_retention_distribution_closure_notice_verification_receipts(p)]


def _retention_distribution_closure_notice_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-distribution-closure-notice-closeout-records.json"
    return [] if not p.exists() else [{"retention_distribution_closure_notice_closeout_record_id": r["retention_distribution_closure_notice_closeout_record_id"], "retention_distribution_closure_notice_verification_receipt_id": r["retention_distribution_closure_notice_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_distribution_closure_notice_closeout_records(p)]


def _retention_public_access_register_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-access-register-records.json"
    return [] if not p.exists() else [{"retention_public_access_register_record_id": r["retention_public_access_register_record_id"], "retention_distribution_closure_notice_closeout_record_id": r["retention_distribution_closure_notice_closeout_record_id"], "public_access_register_state": r["public_access_register_state"], "public_access_register_scope": r["public_access_register_scope"]} for r in load_retention_public_access_register_records(p)]


def _retention_public_access_register_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-access-register-verification-receipts.json"
    return [] if not p.exists() else [{"retention_public_access_register_verification_receipt_id": r["retention_public_access_register_verification_receipt_id"], "retention_public_access_register_record_id": r["retention_public_access_register_record_id"], "verification_state": r["verification_state"], "checked_public_access_register_refs": len(r["checked_public_access_register_refs"])} for r in load_retention_public_access_register_verification_receipts(p)]


def _retention_public_access_register_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-access-register-closeout-records.json"
    return [] if not p.exists() else [{"retention_public_access_register_closeout_record_id": r["retention_public_access_register_closeout_record_id"], "retention_public_access_register_verification_receipt_id": r["retention_public_access_register_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_public_access_register_closeout_records(p)]


def _retention_release_access_index_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-index-records.json"
    return [] if not p.exists() else [{"retention_release_access_index_record_id": r["retention_release_access_index_record_id"], "retention_public_access_register_closeout_record_id": r["retention_public_access_register_closeout_record_id"], "release_access_index_state": r["release_access_index_state"], "release_access_index_scope": r["release_access_index_scope"]} for r in load_retention_release_access_index_records(p)]


def _retention_release_access_index_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-index-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_access_index_verification_receipt_id": r["retention_release_access_index_verification_receipt_id"], "retention_release_access_index_record_id": r["retention_release_access_index_record_id"], "verification_state": r["verification_state"], "checked_release_access_index_refs": len(r["checked_release_access_index_refs"])} for r in load_retention_release_access_index_verification_receipts(p)]


def _retention_release_access_index_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-index-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_access_index_closeout_record_id": r["retention_release_access_index_closeout_record_id"], "retention_release_access_index_verification_receipt_id": r["retention_release_access_index_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_access_index_closeout_records(p)]


def _retention_release_access_verification_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-verification-summary-records.json"
    return [] if not p.exists() else [{"retention_release_access_verification_summary_record_id": r["retention_release_access_verification_summary_record_id"], "retention_release_access_index_closeout_record_id": r["retention_release_access_index_closeout_record_id"], "release_access_verification_summary_state": r["release_access_verification_summary_state"], "release_access_verification_summary_scope": r["release_access_verification_summary_scope"]} for r in load_retention_release_access_verification_summary_records(p)]


def _retention_release_access_verification_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-verification-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_access_verification_summary_verification_receipt_id": r["retention_release_access_verification_summary_verification_receipt_id"], "retention_release_access_verification_summary_record_id": r["retention_release_access_verification_summary_record_id"], "verification_state": r["verification_state"], "checked_release_access_verification_summary_refs": len(r["checked_release_access_verification_summary_refs"])} for r in load_retention_release_access_verification_summary_verification_receipts(p)]


def _retention_release_access_verification_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-verification-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_access_verification_summary_closeout_record_id": r["retention_release_access_verification_summary_closeout_record_id"], "retention_release_access_verification_summary_verification_receipt_id": r["retention_release_access_verification_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_access_verification_summary_closeout_records(p)]


def _retention_release_access_closeout_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-closeout-summary-records.json"
    return [] if not p.exists() else [{"retention_release_access_closeout_summary_record_id": r["retention_release_access_closeout_summary_record_id"], "retention_release_access_verification_summary_closeout_record_id": r["retention_release_access_verification_summary_closeout_record_id"], "release_access_closeout_summary_state": r["release_access_closeout_summary_state"], "release_access_closeout_summary_scope": r["release_access_closeout_summary_scope"]} for r in load_retention_release_access_closeout_summary_records(p)]


def _retention_release_access_closeout_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-closeout-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_access_closeout_summary_verification_receipt_id": r["retention_release_access_closeout_summary_verification_receipt_id"], "retention_release_access_closeout_summary_record_id": r["retention_release_access_closeout_summary_record_id"], "verification_state": r["verification_state"], "checked_release_access_closeout_summary_refs": len(r["checked_release_access_closeout_summary_refs"])} for r in load_retention_release_access_closeout_summary_verification_receipts(p)]


def _retention_release_access_closeout_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-access-closeout-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_access_closeout_summary_closeout_record_id": r["retention_release_access_closeout_summary_closeout_record_id"], "retention_release_access_closeout_summary_verification_receipt_id": r["retention_release_access_closeout_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_access_closeout_summary_closeout_records(p)]


def _retention_archive_availability_rollup_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-availability-rollup-records.json"
    return [] if not p.exists() else [{"retention_archive_availability_rollup_record_id": r["retention_archive_availability_rollup_record_id"], "retention_release_access_closeout_summary_closeout_record_id": r["retention_release_access_closeout_summary_closeout_record_id"], "archive_availability_rollup_state": r["archive_availability_rollup_state"], "archive_availability_rollup_scope": r["archive_availability_rollup_scope"]} for r in load_retention_archive_availability_rollup_records(p)]


def _retention_archive_availability_rollup_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-availability-rollup-verification-receipts.json"
    return [] if not p.exists() else [{"retention_archive_availability_rollup_verification_receipt_id": r["retention_archive_availability_rollup_verification_receipt_id"], "retention_archive_availability_rollup_record_id": r["retention_archive_availability_rollup_record_id"], "verification_state": r["verification_state"], "checked_archive_availability_rollup_refs": len(r["checked_archive_availability_rollup_refs"])} for r in load_retention_archive_availability_rollup_verification_receipts(p)]


def _retention_archive_availability_rollup_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-archive-availability-rollup-closeout-records.json"
    return [] if not p.exists() else [{"retention_archive_availability_rollup_closeout_record_id": r["retention_archive_availability_rollup_closeout_record_id"], "retention_archive_availability_rollup_verification_receipt_id": r["retention_archive_availability_rollup_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_archive_availability_rollup_closeout_records(p)]


def _retention_retrieval_readiness_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-readiness-snapshot-records.json"
    return [] if not p.exists() else [{"retention_retrieval_readiness_snapshot_record_id": r["retention_retrieval_readiness_snapshot_record_id"], "retention_archive_availability_rollup_closeout_record_id": r["retention_archive_availability_rollup_closeout_record_id"], "retrieval_readiness_snapshot_state": r["retrieval_readiness_snapshot_state"], "retrieval_readiness_snapshot_scope": r["retrieval_readiness_snapshot_scope"]} for r in load_retention_retrieval_readiness_snapshot_records(p)]


def _retention_retrieval_readiness_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-readiness-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_retrieval_readiness_snapshot_verification_receipt_id": r["retention_retrieval_readiness_snapshot_verification_receipt_id"], "retention_retrieval_readiness_snapshot_record_id": r["retention_retrieval_readiness_snapshot_record_id"], "verification_state": r["verification_state"], "checked_retrieval_readiness_snapshot_refs": len(r["checked_retrieval_readiness_snapshot_refs"])} for r in load_retention_retrieval_readiness_snapshot_verification_receipts(p)]


def _retention_retrieval_readiness_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-retrieval-readiness-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_retrieval_readiness_snapshot_closeout_record_id": r["retention_retrieval_readiness_snapshot_closeout_record_id"], "retention_retrieval_readiness_snapshot_verification_receipt_id": r["retention_retrieval_readiness_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_retrieval_readiness_snapshot_closeout_records(p)]


def _retention_consumer_availability_notice_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-availability-notice-records.json"
    return [] if not p.exists() else [{"retention_consumer_availability_notice_record_id": r["retention_consumer_availability_notice_record_id"], "retention_retrieval_readiness_snapshot_closeout_record_id": r["retention_retrieval_readiness_snapshot_closeout_record_id"], "consumer_availability_notice_state": r["consumer_availability_notice_state"], "consumer_availability_notice_scope": r["consumer_availability_notice_scope"]} for r in load_retention_consumer_availability_notice_records(p)]


def _retention_consumer_availability_notice_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-availability-notice-verification-receipts.json"
    return [] if not p.exists() else [{"retention_consumer_availability_notice_verification_receipt_id": r["retention_consumer_availability_notice_verification_receipt_id"], "retention_consumer_availability_notice_record_id": r["retention_consumer_availability_notice_record_id"], "verification_state": r["verification_state"], "checked_consumer_availability_notice_refs": len(r["checked_consumer_availability_notice_refs"])} for r in load_retention_consumer_availability_notice_verification_receipts(p)]


def _retention_consumer_availability_notice_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-consumer-availability-notice-closeout-records.json"
    return [] if not p.exists() else [{"retention_consumer_availability_notice_closeout_record_id": r["retention_consumer_availability_notice_closeout_record_id"], "retention_consumer_availability_notice_verification_receipt_id": r["retention_consumer_availability_notice_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_consumer_availability_notice_closeout_records(p)]


def _retention_public_release_receipt_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-release-receipt-records.json"
    return [] if not p.exists() else [{"retention_public_release_receipt_record_id": r["retention_public_release_receipt_record_id"], "retention_consumer_availability_notice_closeout_record_id": r["retention_consumer_availability_notice_closeout_record_id"], "public_release_receipt_state": r["public_release_receipt_state"], "public_release_receipt_scope": r["public_release_receipt_scope"]} for r in load_retention_public_release_receipt_records(p)]


def _retention_public_release_receipt_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-release-receipt-verification-receipts.json"
    return [] if not p.exists() else [{"retention_public_release_receipt_verification_receipt_id": r["retention_public_release_receipt_verification_receipt_id"], "retention_public_release_receipt_record_id": r["retention_public_release_receipt_record_id"], "verification_state": r["verification_state"], "checked_public_release_receipt_refs": len(r["checked_public_release_receipt_refs"])} for r in load_retention_public_release_receipt_verification_receipts(p)]


def _retention_public_release_receipt_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-public-release-receipt-closeout-records.json"
    return [] if not p.exists() else [{"retention_public_release_receipt_closeout_record_id": r["retention_public_release_receipt_closeout_record_id"], "retention_public_release_receipt_verification_receipt_id": r["retention_public_release_receipt_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_public_release_receipt_closeout_records(p)]


def _retention_release_exception_register_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-register-records.json"
    return [] if not p.exists() else [{"retention_release_exception_register_record_id": r["retention_release_exception_register_record_id"], "retention_public_release_receipt_closeout_record_id": r["retention_public_release_receipt_closeout_record_id"], "release_exception_register_state": r["release_exception_register_state"], "release_exception_register_scope": r["release_exception_register_scope"]} for r in load_retention_release_exception_register_records(p)]


def _retention_release_exception_register_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-register-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_exception_register_verification_receipt_id": r["retention_release_exception_register_verification_receipt_id"], "retention_release_exception_register_record_id": r["retention_release_exception_register_record_id"], "verification_state": r["verification_state"], "checked_release_exception_register_refs": len(r["checked_release_exception_register_refs"])} for r in load_retention_release_exception_register_verification_receipts(p)]


def _retention_release_exception_register_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-register-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_exception_register_closeout_record_id": r["retention_release_exception_register_closeout_record_id"], "retention_release_exception_register_verification_receipt_id": r["retention_release_exception_register_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_exception_register_closeout_records(p)]


def _retention_release_exception_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-summary-records.json"
    return [] if not p.exists() else [{"retention_release_exception_summary_record_id": r["retention_release_exception_summary_record_id"], "retention_release_exception_register_closeout_record_id": r["retention_release_exception_register_closeout_record_id"], "release_exception_summary_state": r["release_exception_summary_state"], "release_exception_summary_scope": r["release_exception_summary_scope"]} for r in load_retention_release_exception_summary_records(p)]


def _retention_release_exception_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_exception_summary_verification_receipt_id": r["retention_release_exception_summary_verification_receipt_id"], "retention_release_exception_summary_record_id": r["retention_release_exception_summary_record_id"], "verification_state": r["verification_state"], "checked_release_exception_summary_refs": len(r["checked_release_exception_summary_refs"])} for r in load_retention_release_exception_summary_verification_receipts(p)]


def _retention_release_exception_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-exception-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_exception_summary_closeout_record_id": r["retention_release_exception_summary_closeout_record_id"], "retention_release_exception_summary_verification_receipt_id": r["retention_release_exception_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_exception_summary_closeout_records(p)]


def _retention_release_metrics_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-metrics-snapshot-records.json"
    return [] if not p.exists() else [{"retention_release_metrics_snapshot_record_id": r["retention_release_metrics_snapshot_record_id"], "retention_release_exception_summary_closeout_record_id": r["retention_release_exception_summary_closeout_record_id"], "release_metrics_snapshot_state": r["release_metrics_snapshot_state"], "release_metrics_snapshot_scope": r["release_metrics_snapshot_scope"]} for r in load_retention_release_metrics_snapshot_records(p)]


def _retention_release_metrics_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-metrics-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_metrics_snapshot_verification_receipt_id": r["retention_release_metrics_snapshot_verification_receipt_id"], "retention_release_metrics_snapshot_record_id": r["retention_release_metrics_snapshot_record_id"], "verification_state": r["verification_state"], "checked_release_metrics_snapshot_refs": len(r["checked_release_metrics_snapshot_refs"])} for r in load_retention_release_metrics_snapshot_verification_receipts(p)]


def _retention_release_metrics_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-metrics-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_metrics_snapshot_closeout_record_id": r["retention_release_metrics_snapshot_closeout_record_id"], "retention_release_metrics_snapshot_verification_receipt_id": r["retention_release_metrics_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_metrics_snapshot_closeout_records(p)]


def _retention_release_terminal_report_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-terminal-report-records.json"
    return [] if not p.exists() else [{"retention_release_terminal_report_record_id": r["retention_release_terminal_report_record_id"], "retention_release_metrics_snapshot_closeout_record_id": r["retention_release_metrics_snapshot_closeout_record_id"], "release_terminal_report_state": r["release_terminal_report_state"], "release_terminal_report_scope": r["release_terminal_report_scope"]} for r in load_retention_release_terminal_report_records(p)]


def _retention_release_terminal_report_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-terminal-report-verification-receipts.json"
    return [] if not p.exists() else [{"retention_release_terminal_report_verification_receipt_id": r["retention_release_terminal_report_verification_receipt_id"], "retention_release_terminal_report_record_id": r["retention_release_terminal_report_record_id"], "verification_state": r["verification_state"], "checked_release_terminal_report_refs": len(r["checked_release_terminal_report_refs"])} for r in load_retention_release_terminal_report_verification_receipts(p)]


def _retention_release_terminal_report_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-release-terminal-report-closeout-records.json"
    return [] if not p.exists() else [{"retention_release_terminal_report_closeout_record_id": r["retention_release_terminal_report_closeout_record_id"], "retention_release_terminal_report_verification_receipt_id": r["retention_release_terminal_report_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_release_terminal_report_closeout_records(p)]


def _retention_final_publication_notice_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-publication-notice-records.json"
    return [] if not p.exists() else [{"retention_final_publication_notice_record_id": r["retention_final_publication_notice_record_id"], "retention_release_terminal_report_closeout_record_id": r["retention_release_terminal_report_closeout_record_id"], "final_publication_notice_state": r["final_publication_notice_state"], "final_publication_notice_scope": r["final_publication_notice_scope"]} for r in load_retention_final_publication_notice_records(p)]


def _retention_final_publication_notice_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-publication-notice-verification-receipts.json"
    return [] if not p.exists() else [{"retention_final_publication_notice_verification_receipt_id": r["retention_final_publication_notice_verification_receipt_id"], "retention_final_publication_notice_record_id": r["retention_final_publication_notice_record_id"], "verification_state": r["verification_state"], "checked_final_publication_notice_refs": len(r["checked_final_publication_notice_refs"])} for r in load_retention_final_publication_notice_verification_receipts(p)]


def _retention_final_publication_notice_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-final-publication-notice-closeout-records.json"
    return [] if not p.exists() else [{"retention_final_publication_notice_closeout_record_id": r["retention_final_publication_notice_closeout_record_id"], "retention_final_publication_notice_verification_receipt_id": r["retention_final_publication_notice_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_final_publication_notice_closeout_records(p)]


def _retention_publication_completion_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-completion-records.json"
    return [] if not p.exists() else [{"retention_publication_completion_record_id": r["retention_publication_completion_record_id"], "retention_final_publication_notice_closeout_record_id": r["retention_final_publication_notice_closeout_record_id"], "publication_completion_state": r["publication_completion_state"], "publication_completion_scope": r["publication_completion_scope"]} for r in load_retention_publication_completion_records(p)]


def _retention_publication_completion_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-completion-verification-receipts.json"
    return [] if not p.exists() else [{"retention_publication_completion_verification_receipt_id": r["retention_publication_completion_verification_receipt_id"], "retention_publication_completion_record_id": r["retention_publication_completion_record_id"], "verification_state": r["verification_state"], "checked_publication_completion_refs": len(r["checked_publication_completion_refs"])} for r in load_retention_publication_completion_verification_receipts(p)]


def _retention_publication_completion_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-completion-closeout-records.json"
    return [] if not p.exists() else [{"retention_publication_completion_closeout_record_id": r["retention_publication_completion_closeout_record_id"], "retention_publication_completion_verification_receipt_id": r["retention_publication_completion_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_publication_completion_closeout_records(p)]


def _retention_publication_closure_certificate_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-closure-certificate-records.json"
    return [] if not p.exists() else [{"retention_publication_closure_certificate_record_id": r["retention_publication_closure_certificate_record_id"], "retention_publication_completion_closeout_record_id": r["retention_publication_completion_closeout_record_id"], "publication_closure_certificate_state": r["publication_closure_certificate_state"], "publication_closure_certificate_scope": r["publication_closure_certificate_scope"]} for r in load_retention_publication_closure_certificate_records(p)]


def _retention_publication_closure_certificate_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-closure-certificate-verification-receipts.json"
    return [] if not p.exists() else [{"retention_publication_closure_certificate_verification_receipt_id": r["retention_publication_closure_certificate_verification_receipt_id"], "retention_publication_closure_certificate_record_id": r["retention_publication_closure_certificate_record_id"], "verification_state": r["verification_state"], "checked_publication_closure_certificate_refs": len(r["checked_publication_closure_certificate_refs"])} for r in load_retention_publication_closure_certificate_verification_receipts(p)]


def _retention_publication_closure_certificate_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-closure-certificate-closeout-records.json"
    return [] if not p.exists() else [{"retention_publication_closure_certificate_closeout_record_id": r["retention_publication_closure_certificate_closeout_record_id"], "retention_publication_closure_certificate_verification_receipt_id": r["retention_publication_closure_certificate_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_publication_closure_certificate_closeout_records(p)]


def _retention_terminal_public_index_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-index-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_index_record_id": r["retention_terminal_public_index_record_id"], "retention_publication_closure_certificate_closeout_record_id": r["retention_publication_closure_certificate_closeout_record_id"], "terminal_public_index_state": r["terminal_public_index_state"], "terminal_public_index_scope": r["terminal_public_index_scope"]} for r in load_retention_terminal_public_index_records(p)]


def _retention_terminal_public_index_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-index-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_index_verification_receipt_id": r["retention_terminal_public_index_verification_receipt_id"], "retention_terminal_public_index_record_id": r["retention_terminal_public_index_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_index_refs": len(r["checked_terminal_public_index_refs"])} for r in load_retention_terminal_public_index_verification_receipts(p)]


def _retention_terminal_public_index_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-index-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_index_closeout_record_id": r["retention_terminal_public_index_closeout_record_id"], "retention_terminal_public_index_verification_receipt_id": r["retention_terminal_public_index_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_index_closeout_records(p)]


def _retention_terminal_public_digest_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-digest-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_digest_record_id": r["retention_terminal_public_digest_record_id"], "retention_terminal_public_index_closeout_record_id": r["retention_terminal_public_index_closeout_record_id"], "terminal_public_digest_state": r["terminal_public_digest_state"], "terminal_public_digest_scope": r["terminal_public_digest_scope"]} for r in load_retention_terminal_public_digest_records(p)]


def _retention_terminal_public_digest_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-digest-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_digest_verification_receipt_id": r["retention_terminal_public_digest_verification_receipt_id"], "retention_terminal_public_digest_record_id": r["retention_terminal_public_digest_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_digest_refs": len(r["checked_terminal_public_digest_refs"])} for r in load_retention_terminal_public_digest_verification_receipts(p)]


def _retention_terminal_public_digest_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-digest-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_digest_closeout_record_id": r["retention_terminal_public_digest_closeout_record_id"], "retention_terminal_public_digest_verification_receipt_id": r["retention_terminal_public_digest_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_digest_closeout_records(p)]


def _retention_terminal_public_archive_copy_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-archive-copy-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_archive_copy_record_id": r["retention_terminal_public_archive_copy_record_id"], "retention_terminal_public_digest_closeout_record_id": r["retention_terminal_public_digest_closeout_record_id"], "terminal_public_archive_copy_state": r["terminal_public_archive_copy_state"], "terminal_public_archive_copy_scope": r["terminal_public_archive_copy_scope"]} for r in load_retention_terminal_public_archive_copy_records(p)]


def _retention_terminal_public_archive_copy_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-archive-copy-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_archive_copy_verification_receipt_id": r["retention_terminal_public_archive_copy_verification_receipt_id"], "retention_terminal_public_archive_copy_record_id": r["retention_terminal_public_archive_copy_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_archive_copy_refs": len(r["checked_terminal_public_archive_copy_refs"])} for r in load_retention_terminal_public_archive_copy_verification_receipts(p)]


def _retention_terminal_public_archive_copy_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-archive-copy-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_archive_copy_closeout_record_id": r["retention_terminal_public_archive_copy_closeout_record_id"], "retention_terminal_public_archive_copy_verification_receipt_id": r["retention_terminal_public_archive_copy_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_archive_copy_closeout_records(p)]


def _retention_terminal_public_audit_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-audit-snapshot-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_audit_snapshot_record_id": r["retention_terminal_public_audit_snapshot_record_id"], "retention_terminal_public_archive_copy_closeout_record_id": r["retention_terminal_public_archive_copy_closeout_record_id"], "terminal_public_audit_snapshot_state": r["terminal_public_audit_snapshot_state"], "terminal_public_audit_snapshot_scope": r["terminal_public_audit_snapshot_scope"]} for r in load_retention_terminal_public_audit_snapshot_records(p)]


def _retention_terminal_public_audit_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-audit-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_audit_snapshot_verification_receipt_id": r["retention_terminal_public_audit_snapshot_verification_receipt_id"], "retention_terminal_public_audit_snapshot_record_id": r["retention_terminal_public_audit_snapshot_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_audit_snapshot_refs": len(r["checked_terminal_public_audit_snapshot_refs"])} for r in load_retention_terminal_public_audit_snapshot_verification_receipts(p)]


def _retention_terminal_public_audit_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-audit-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_audit_snapshot_closeout_record_id": r["retention_terminal_public_audit_snapshot_closeout_record_id"], "retention_terminal_public_audit_snapshot_verification_receipt_id": r["retention_terminal_public_audit_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_audit_snapshot_closeout_records(p)]


def _retention_terminal_public_metrics_snapshot_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-metrics-snapshot-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_metrics_snapshot_record_id": r["retention_terminal_public_metrics_snapshot_record_id"], "retention_terminal_public_audit_snapshot_closeout_record_id": r["retention_terminal_public_audit_snapshot_closeout_record_id"], "terminal_public_metrics_snapshot_state": r["terminal_public_metrics_snapshot_state"], "terminal_public_metrics_snapshot_scope": r["terminal_public_metrics_snapshot_scope"]} for r in load_retention_terminal_public_metrics_snapshot_records(p)]


def _retention_terminal_public_metrics_snapshot_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-metrics-snapshot-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_metrics_snapshot_verification_receipt_id": r["retention_terminal_public_metrics_snapshot_verification_receipt_id"], "retention_terminal_public_metrics_snapshot_record_id": r["retention_terminal_public_metrics_snapshot_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_metrics_snapshot_refs": len(r["checked_terminal_public_metrics_snapshot_refs"])} for r in load_retention_terminal_public_metrics_snapshot_verification_receipts(p)]


def _retention_terminal_public_metrics_snapshot_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-metrics-snapshot-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_metrics_snapshot_closeout_record_id": r["retention_terminal_public_metrics_snapshot_closeout_record_id"], "retention_terminal_public_metrics_snapshot_verification_receipt_id": r["retention_terminal_public_metrics_snapshot_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_metrics_snapshot_closeout_records(p)]


def _retention_terminal_public_release_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-release-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_release_summary_record_id": r["retention_terminal_public_release_summary_record_id"], "retention_terminal_public_metrics_snapshot_closeout_record_id": r["retention_terminal_public_metrics_snapshot_closeout_record_id"], "terminal_public_release_summary_state": r["terminal_public_release_summary_state"], "terminal_public_release_summary_scope": r["terminal_public_release_summary_scope"]} for r in load_retention_terminal_public_release_summary_records(p)]


def _retention_terminal_public_release_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-release-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_public_release_summary_verification_receipt_id": r["retention_terminal_public_release_summary_verification_receipt_id"], "retention_terminal_public_release_summary_record_id": r["retention_terminal_public_release_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_public_release_summary_refs": len(r["checked_terminal_public_release_summary_refs"])} for r in load_retention_terminal_public_release_summary_verification_receipts(p)]


def _retention_terminal_public_release_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-public-release-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_public_release_summary_closeout_record_id": r["retention_terminal_public_release_summary_closeout_record_id"], "retention_terminal_public_release_summary_verification_receipt_id": r["retention_terminal_public_release_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_public_release_summary_closeout_records(p)]


def _retention_terminal_distribution_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-distribution-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_distribution_summary_record_id": r["retention_terminal_distribution_summary_record_id"], "retention_terminal_public_release_summary_closeout_record_id": r["retention_terminal_public_release_summary_closeout_record_id"], "terminal_distribution_summary_state": r["terminal_distribution_summary_state"], "terminal_distribution_summary_scope": r["terminal_distribution_summary_scope"]} for r in load_retention_terminal_distribution_summary_records(p)]


def _retention_terminal_distribution_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-distribution-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_distribution_summary_verification_receipt_id": r["retention_terminal_distribution_summary_verification_receipt_id"], "retention_terminal_distribution_summary_record_id": r["retention_terminal_distribution_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_distribution_summary_refs": len(r["checked_terminal_distribution_summary_refs"])} for r in load_retention_terminal_distribution_summary_verification_receipts(p)]


def _retention_terminal_distribution_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-distribution-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_distribution_summary_closeout_record_id": r["retention_terminal_distribution_summary_closeout_record_id"], "retention_terminal_distribution_summary_verification_receipt_id": r["retention_terminal_distribution_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_distribution_summary_closeout_records(p)]


def _retention_terminal_access_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_access_summary_record_id": r["retention_terminal_access_summary_record_id"], "retention_terminal_distribution_summary_closeout_record_id": r["retention_terminal_distribution_summary_closeout_record_id"], "terminal_access_summary_state": r["terminal_access_summary_state"], "terminal_access_summary_scope": r["terminal_access_summary_scope"]} for r in load_retention_terminal_access_summary_records(p)]


def _retention_terminal_access_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_access_summary_verification_receipt_id": r["retention_terminal_access_summary_verification_receipt_id"], "retention_terminal_access_summary_record_id": r["retention_terminal_access_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_access_summary_refs": len(r["checked_terminal_access_summary_refs"])} for r in load_retention_terminal_access_summary_verification_receipts(p)]


def _retention_terminal_access_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-access-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_access_summary_closeout_record_id": r["retention_terminal_access_summary_closeout_record_id"], "retention_terminal_access_summary_verification_receipt_id": r["retention_terminal_access_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_access_summary_closeout_records(p)]


def _retention_terminal_retrieval_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-retrieval-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_retrieval_summary_record_id": r["retention_terminal_retrieval_summary_record_id"], "retention_terminal_access_summary_closeout_record_id": r["retention_terminal_access_summary_closeout_record_id"], "terminal_retrieval_summary_state": r["terminal_retrieval_summary_state"], "terminal_retrieval_summary_scope": r["terminal_retrieval_summary_scope"]} for r in load_retention_terminal_retrieval_summary_records(p)]


def _retention_terminal_retrieval_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-retrieval-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_retrieval_summary_verification_receipt_id": r["retention_terminal_retrieval_summary_verification_receipt_id"], "retention_terminal_retrieval_summary_record_id": r["retention_terminal_retrieval_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_retrieval_summary_refs": len(r["checked_terminal_retrieval_summary_refs"])} for r in load_retention_terminal_retrieval_summary_verification_receipts(p)]


def _retention_terminal_retrieval_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-retrieval-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_retrieval_summary_closeout_record_id": r["retention_terminal_retrieval_summary_closeout_record_id"], "retention_terminal_retrieval_summary_verification_receipt_id": r["retention_terminal_retrieval_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_retrieval_summary_closeout_records(p)]


def _retention_terminal_consumer_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-consumer-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_consumer_summary_record_id": r["retention_terminal_consumer_summary_record_id"], "retention_terminal_retrieval_summary_closeout_record_id": r["retention_terminal_retrieval_summary_closeout_record_id"], "terminal_consumer_summary_state": r["terminal_consumer_summary_state"], "terminal_consumer_summary_scope": r["terminal_consumer_summary_scope"]} for r in load_retention_terminal_consumer_summary_records(p)]


def _retention_terminal_consumer_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-consumer-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_consumer_summary_verification_receipt_id": r["retention_terminal_consumer_summary_verification_receipt_id"], "retention_terminal_consumer_summary_record_id": r["retention_terminal_consumer_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_consumer_summary_refs": len(r["checked_terminal_consumer_summary_refs"])} for r in load_retention_terminal_consumer_summary_verification_receipts(p)]


def _retention_terminal_consumer_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-consumer-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_consumer_summary_closeout_record_id": r["retention_terminal_consumer_summary_closeout_record_id"], "retention_terminal_consumer_summary_verification_receipt_id": r["retention_terminal_consumer_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_consumer_summary_closeout_records(p)]


def _retention_terminal_exception_summary_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-exception-summary-records.json"
    return [] if not p.exists() else [{"retention_terminal_exception_summary_record_id": r["retention_terminal_exception_summary_record_id"], "retention_terminal_consumer_summary_closeout_record_id": r["retention_terminal_consumer_summary_closeout_record_id"], "terminal_exception_summary_state": r["terminal_exception_summary_state"], "terminal_exception_summary_scope": r["terminal_exception_summary_scope"]} for r in load_retention_terminal_exception_summary_records(p)]


def _retention_terminal_exception_summary_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-exception-summary-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_exception_summary_verification_receipt_id": r["retention_terminal_exception_summary_verification_receipt_id"], "retention_terminal_exception_summary_record_id": r["retention_terminal_exception_summary_record_id"], "verification_state": r["verification_state"], "checked_terminal_exception_summary_refs": len(r["checked_terminal_exception_summary_refs"])} for r in load_retention_terminal_exception_summary_verification_receipts(p)]


def _retention_terminal_exception_summary_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-exception-summary-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_exception_summary_closeout_record_id": r["retention_terminal_exception_summary_closeout_record_id"], "retention_terminal_exception_summary_verification_receipt_id": r["retention_terminal_exception_summary_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_exception_summary_closeout_records(p)]


def _retention_terminal_archive_report_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-archive-report-records.json"
    return [] if not p.exists() else [{"retention_terminal_archive_report_record_id": r["retention_terminal_archive_report_record_id"], "retention_terminal_exception_summary_closeout_record_id": r["retention_terminal_exception_summary_closeout_record_id"], "terminal_archive_report_state": r["terminal_archive_report_state"], "terminal_archive_report_scope": r["terminal_archive_report_scope"]} for r in load_retention_terminal_archive_report_records(p)]


def _retention_terminal_archive_report_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-archive-report-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_archive_report_verification_receipt_id": r["retention_terminal_archive_report_verification_receipt_id"], "retention_terminal_archive_report_record_id": r["retention_terminal_archive_report_record_id"], "verification_state": r["verification_state"], "checked_terminal_archive_report_refs": len(r["checked_terminal_archive_report_refs"])} for r in load_retention_terminal_archive_report_verification_receipts(p)]


def _retention_terminal_archive_report_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-archive-report-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_archive_report_closeout_record_id": r["retention_terminal_archive_report_closeout_record_id"], "retention_terminal_archive_report_verification_receipt_id": r["retention_terminal_archive_report_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_archive_report_closeout_records(p)]


def _retention_terminal_release_annex_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-release-annex-records.json"
    return [] if not p.exists() else [{"retention_terminal_release_annex_record_id": r["retention_terminal_release_annex_record_id"], "retention_terminal_archive_report_closeout_record_id": r["retention_terminal_archive_report_closeout_record_id"], "terminal_release_annex_state": r["terminal_release_annex_state"], "terminal_release_annex_scope": r["terminal_release_annex_scope"]} for r in load_retention_terminal_release_annex_records(p)]


def _retention_terminal_release_annex_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-release-annex-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_release_annex_verification_receipt_id": r["retention_terminal_release_annex_verification_receipt_id"], "retention_terminal_release_annex_record_id": r["retention_terminal_release_annex_record_id"], "verification_state": r["verification_state"], "checked_terminal_release_annex_refs": len(r["checked_terminal_release_annex_refs"])} for r in load_retention_terminal_release_annex_verification_receipts(p)]


def _retention_terminal_release_annex_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-release-annex-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_release_annex_closeout_record_id": r["retention_terminal_release_annex_closeout_record_id"], "retention_terminal_release_annex_verification_receipt_id": r["retention_terminal_release_annex_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_release_annex_closeout_records(p)]


def _retention_terminal_publication_bundle_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-publication-bundle-records.json"
    return [] if not p.exists() else [{"retention_terminal_publication_bundle_record_id": r["retention_terminal_publication_bundle_record_id"], "retention_terminal_release_annex_closeout_record_id": r["retention_terminal_release_annex_closeout_record_id"], "terminal_publication_bundle_state": r["terminal_publication_bundle_state"], "terminal_publication_bundle_scope": r["terminal_publication_bundle_scope"]} for r in load_retention_terminal_publication_bundle_records(p)]


def _retention_terminal_publication_bundle_verification_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-publication-bundle-verification-receipts.json"
    return [] if not p.exists() else [{"retention_terminal_publication_bundle_verification_receipt_id": r["retention_terminal_publication_bundle_verification_receipt_id"], "retention_terminal_publication_bundle_record_id": r["retention_terminal_publication_bundle_record_id"], "verification_state": r["verification_state"], "checked_terminal_publication_bundle_refs": len(r["checked_terminal_publication_bundle_refs"])} for r in load_retention_terminal_publication_bundle_verification_receipts(p)]


def _retention_terminal_publication_bundle_closeout_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-terminal-publication-bundle-closeout-records.json"
    return [] if not p.exists() else [{"retention_terminal_publication_bundle_closeout_record_id": r["retention_terminal_publication_bundle_closeout_record_id"], "retention_terminal_publication_bundle_verification_receipt_id": r["retention_terminal_publication_bundle_verification_receipt_id"], "closeout_state": r["closeout_state"], "outcome": r["outcome"]} for r in load_retention_terminal_publication_bundle_closeout_records(p)]


def _retention_publication_terminal_status_record_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention/retention-publication-terminal-status-records.json"
    return [] if not p.exists() else [{"retention_publication_terminal_status_record_id": r["retention_publication_terminal_status_record_id"], "retention_terminal_publication_bundle_closeout_record_id": r["retention_terminal_publication_bundle_closeout_record_id"], "publication_terminal_status_state": r["publication_terminal_status_state"], "publication_terminal_status_scope": r["publication_terminal_status_scope"]} for r in load_retention_publication_terminal_status_records(p)]


def _merge_decision_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "merge" / "merge-decisions.json"
    return [] if not p.exists() else [{"merge_decision_id": d.merge_decision_id, "import_record_id": d.import_record_id, "decision": d.decision, "reason_code": d.reason_code} for d in load_merge_decisions(p)]


def _intake_decision_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "intake" / "intake-decisions.json"
    return [] if not p.exists() else [{"intake_decision_id": d.intake_decision_id, "inbox_item_id": d.inbox_item_id, "decision": d.decision, "reason_code": d.reason_code} for d in load_intake_decisions(p)]


def _routing_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "routing" / "routing-policy.json"
    return [] if not p.exists() else [{"route_id": route.route_id, "route_kind": route.route_kind, "enabled": route.enabled, "channels": len(route.allowed_delivery_channel_ids)} for route in load_routing_policy(p).routes]


def _delivery_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "delivery" / "delivery-channel-registry.json"
    return [] if not p.exists() else [{"channel_id": c.channel_id, "channel_kind": c.channel_kind, "status": c.status, "route_kinds": ",".join(c.supports_route_kinds)} for c in load_delivery_channel_registry(p).channels]


def _delivery_job_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "delivery" / "delivery-jobs.json"
    return [] if not p.exists() else [{"delivery_job_id": j.delivery_job_id, "dispatch_rule_id": j.dispatch_rule_id or "", "job_state": j.job_state, "priority": j.priority} for j in load_delivery_jobs(p)]


def _transport_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "transport" / "transport-adapter-registry.json"
    return [] if not p.exists() else [{"transport_adapter_id": t.transport_adapter_id, "transport_kind": t.transport_kind, "status": t.status, "channels": ",".join(t.delivery_channel_ids)} for t in load_transport_adapter_registry(p).adapters]


def _transport_receipt_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "transport" / "transport-receipts.json"
    return [] if not p.exists() else [{"transport_receipt_id": r.transport_receipt_id, "outbox_item_id": r.outbox_item_id, "transport_adapter_id": r.transport_adapter_id, "transport_state": r.transport_state} for r in load_transport_receipts(p)]


def _handling_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "handling" / "handling-policy.json"
    return [] if not p.exists() else [{"label_id": label.label_id, "allowed_sharing_scopes": ",".join(label.allowed_sharing_scopes), "requires_redaction": label.requires_redaction_before_share} for label in load_handling_policy(p).handling_labels]


def _retention_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "retention" / "retention-policy.json"
    return [] if not p.exists() else [{"retention_class": item.retention_class, "default_duration": item.default_duration, "allowed_states": ",".join(item.allowed_disposition_states)} for item in load_retention_policy(p).retention_classes]


def _integrity_rows(root: Path) -> list[dict[str, Any]]:
    p = root / "integrity" / "receipt-manifest.json"
    return [] if not p.exists() else [{"path": r.path, "digest_algorithm": r.digest_algorithm, "purpose": r.purpose} for r in load_integrity_manifest(p).receipts]


def _policy_rows(root: Path) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    p = root / "policy" / "sharing-policy.json"
    if not p.exists():
        return [], []
    policy = load_sharing_policy(p)
    return ([{"scope_id": s.scope_id, "display_name": s.display_name, "path": str(p.relative_to(root))} for s in policy.sharing_scopes],
            [{"gate_id": g.gate_id, "display_name": g.display_name, "path": str(p.relative_to(root))} for g in policy.review_gates])


def build_catalog(start: str | Path | None = None) -> dict[str, Any]:
    root = find_repo_root(start)
    sharing_scopes, review_gates = _policy_rows(root)
    catalog = {
        "root": str(root),
        "capabilities": _capability_rows(root),
        "adapters": _adapter_rows(root),
        "profiles": _profile_rows(root),
        "nodes": _node_rows(root),
        "sources": _source_rows(root),
        "examples": _example_rows(root),
        "reviews": _review_rows(root),
        "audit_events": _audit_rows(root),
        "bundles": _bundle_rows(root),
        "exchange_receipts": _exchange_rows(root),
        "reconciliation_records": _reconciliation_rows(root),
        "quality_levels": _quality_policy_rows(root),
        "quality_assessments": _quality_assessment_rows(root),
        "action_kinds": _action_policy_rows(root),
        "action_records": _action_rows(root),
        "playbooks": _playbook_rows(root),
        "dispatch_rules": _dispatch_rows(root),
        "dispatch_decisions": _dispatch_decision_rows(root),
        "outbox_items": _outbox_rows(root),
        "inbox_items": _inbox_rows(root),
        "import_records": _import_record_rows(root),
        "conflict_records": _conflict_record_rows(root),
        "apply_receipts": _apply_receipt_rows(root),
        "state_checkpoints": _state_checkpoint_rows(root),
        "state_transitions": _state_transition_rows(root),
        "snapshot_manifests": _snapshot_manifest_rows(root),
        "snapshot_verification_receipts": _snapshot_verification_receipt_rows(root),
        "recovery_points": _recovery_point_rows(root),
        "restore_plans": _restore_plan_rows(root),
        "restore_approvals": _restore_approval_rows(root),
        "restore_receipts": _restore_receipt_rows(root),
        "restore_verification_receipts": _restore_verification_receipt_rows(root),
        "restore_closeout_records": _restore_closeout_record_rows(root),
        "disposition_records": _disposition_record_rows(root),
        "disposition_receipts": _disposition_receipt_rows(root),
        "custody_records": _custody_record_rows(root),
        "custody_verification_receipts": _custody_verification_receipt_rows(root),
        "custody_transfer_records": _custody_transfer_record_rows(root),
        "custody_transfer_verification_receipts": _custody_transfer_verification_receipt_rows(root),
        "custody_closeout_records": _custody_closeout_record_rows(root),
        "custody_chain_records": _custody_chain_record_rows(root),
        "custody_chain_verification_receipts": _custody_chain_verification_receipt_rows(root),
        "custody_ledger_records": _custody_ledger_record_rows(root),
        "custody_ledger_verification_receipts": _custody_ledger_verification_receipt_rows(root),
        "custody_release_requests": _custody_release_request_rows(root),
        "custody_release_approvals": _custody_release_approval_rows(root),
        "custody_release_receipts": _custody_release_receipt_rows(root),
        "custody_release_verification_receipts": _custody_release_verification_receipt_rows(root),
        "custody_release_closeout_records": _custody_release_closeout_record_rows(root),
        "custody_release_chain_records": _custody_release_chain_record_rows(root),
        "custody_release_chain_verification_receipts": _custody_release_chain_verification_receipt_rows(root),
        "custody_lifecycle_records": _custody_lifecycle_record_rows(root),
        "custody_lifecycle_verification_receipts": _custody_lifecycle_verification_receipt_rows(root),
        "custody_lifecycle_closeout_records": _custody_lifecycle_closeout_record_rows(root),
        "archive_manifest_records": _archive_manifest_record_rows(root),
        "archive_receipts": _archive_receipt_rows(root),
        "archive_verification_receipts": _archive_verification_receipt_rows(root),
        "archive_closeout_records": _archive_closeout_record_rows(root),
        "archive_chain_records": _archive_chain_record_rows(root),
        "archive_chain_verification_receipts": _archive_chain_verification_receipt_rows(root),
        "archive_index_records": _archive_index_record_rows(root),
        "archive_index_verification_receipts": _archive_index_verification_receipt_rows(root),
        "archive_index_closeout_records": _archive_index_closeout_record_rows(root),
        "archive_lifecycle_records": _archive_lifecycle_record_rows(root),
        "archive_lifecycle_verification_receipts": _archive_lifecycle_verification_receipt_rows(root),
        "archive_lifecycle_closeout_records": _archive_lifecycle_closeout_record_rows(root),
        "preservation_records": _preservation_record_rows(root),
        "preservation_verification_receipts": _preservation_verification_receipt_rows(root),
        "preservation_closeout_records": _preservation_closeout_record_rows(root),
        "preservation_chain_records": _preservation_chain_record_rows(root),
        "preservation_chain_verification_receipts": _preservation_chain_verification_receipt_rows(root),
        "retention_review_records": _retention_review_record_rows(root),
        "retention_review_verification_receipts": _retention_review_verification_receipt_rows(root),
        "retention_decision_records": _retention_decision_record_rows(root),
        "retention_decision_approvals": _retention_decision_approval_rows(root),
        "retention_action_receipts": _retention_action_receipt_rows(root),
        "retention_action_verification_receipts": _retention_action_verification_receipt_rows(root),
        "retention_action_closeout_records": _retention_action_closeout_record_rows(root),
        "retention_chain_records": _retention_chain_record_rows(root),
        "retention_chain_verification_receipts": _retention_chain_verification_receipt_rows(root),
        "retention_lifecycle_records": _retention_lifecycle_record_rows(root),
        "retention_lifecycle_verification_receipts": _retention_lifecycle_verification_receipt_rows(root),
        "retention_lifecycle_closeout_records": _retention_lifecycle_closeout_record_rows(root),
        "retention_ledger_records": _retention_ledger_record_rows(root),
        "retention_ledger_verification_receipts": _retention_ledger_verification_receipt_rows(root),
        "retention_ledger_closeout_records": _retention_ledger_closeout_record_rows(root),
        "retention_policy_compliance_records": _retention_policy_compliance_record_rows(root),
        "retention_policy_compliance_verification_receipts": _retention_policy_compliance_verification_receipt_rows(root),
        "retention_obligation_records": _retention_obligation_record_rows(root),
        "retention_obligation_verification_receipts": _retention_obligation_verification_receipt_rows(root),
        "retention_schedule_records": _retention_schedule_record_rows(root),
        "retention_schedule_verification_receipts": _retention_schedule_verification_receipt_rows(root),
        "retention_schedule_closeout_records": _retention_schedule_closeout_record_rows(root),
        "retention_cycle_records": _retention_cycle_record_rows(root),
        "retention_cycle_verification_receipts": _retention_cycle_verification_receipt_rows(root),
        "retention_cycle_closeout_records": _retention_cycle_closeout_record_rows(root),
        "retention_hold_records": _retention_hold_record_rows(root),
        "retention_hold_verification_receipts": _retention_hold_verification_receipt_rows(root),
        "retention_hold_closeout_records": _retention_hold_closeout_record_rows(root),
        "retention_status_snapshot_records": _retention_status_snapshot_record_rows(root),
        "retention_status_snapshot_verification_receipts": _retention_status_snapshot_verification_receipt_rows(root),
        "retention_rollup_records": _retention_rollup_record_rows(root),
        "retention_rollup_verification_receipts": _retention_rollup_verification_receipt_rows(root),
        "retention_rollup_closeout_records": _retention_rollup_closeout_record_rows(root),
        "retention_report_records": _retention_report_record_rows(root),
        "retention_report_verification_receipts": _retention_report_verification_receipt_rows(root),
        "retention_report_closeout_records": _retention_report_closeout_record_rows(root),
        "retention_publication_records": _retention_publication_record_rows(root),
        "retention_publication_verification_receipts": _retention_publication_verification_receipt_rows(root),
        "retention_publication_closeout_records": _retention_publication_closeout_record_rows(root),
        "retention_dashboard_snapshot_records": _retention_dashboard_snapshot_record_rows(root),
        "retention_dashboard_snapshot_verification_receipts": _retention_dashboard_snapshot_verification_receipt_rows(root),
        "retention_dashboard_snapshot_closeout_records": _retention_dashboard_snapshot_closeout_record_rows(root),
        "retention_summary_records": _retention_summary_record_rows(root),
        "retention_summary_verification_receipts": _retention_summary_verification_receipt_rows(root),
        "retention_summary_closeout_records": _retention_summary_closeout_record_rows(root),
        "retention_export_records": _retention_export_record_rows(root),
        "retention_export_verification_receipts": _retention_export_verification_receipt_rows(root),
        "retention_export_closeout_records": _retention_export_closeout_record_rows(root),
        "retention_handoff_records": _retention_handoff_record_rows(root),
        "retention_handoff_verification_receipts": _retention_handoff_verification_receipt_rows(root),
        "retention_handoff_closeout_records": _retention_handoff_closeout_record_rows(root),
        "retention_acceptance_records": _retention_acceptance_record_rows(root),
        "retention_acceptance_verification_receipts": _retention_acceptance_verification_receipt_rows(root),
        "retention_acceptance_closeout_records": _retention_acceptance_closeout_record_rows(root),
        "retention_package_records": _retention_package_record_rows(root),
        "retention_package_verification_receipts": _retention_package_verification_receipt_rows(root),
        "retention_package_closeout_records": _retention_package_closeout_record_rows(root),
        "retention_finalization_records": _retention_finalization_record_rows(root),
        "retention_finalization_verification_receipts": _retention_finalization_verification_receipt_rows(root),
        "retention_finalization_closeout_records": _retention_finalization_closeout_record_rows(root),
        "retention_terminal_status_records": _retention_terminal_status_record_rows(root),
        "retention_terminal_status_verification_receipts": _retention_terminal_status_verification_receipt_rows(root),
        "retention_terminal_status_closeout_records": _retention_terminal_status_closeout_record_rows(root),
        "retention_certificate_records": _retention_certificate_record_rows(root),
        "retention_certificate_verification_receipts": _retention_certificate_verification_receipt_rows(root),
        "retention_certificate_closeout_records": _retention_certificate_closeout_record_rows(root),
        "retention_registry_records": _retention_registry_record_rows(root),
        "retention_registry_verification_receipts": _retention_registry_verification_receipt_rows(root),
        "retention_registry_closeout_records": _retention_registry_closeout_record_rows(root),
        "retention_closure_records": _retention_closure_record_rows(root),
        "retention_closure_verification_receipts": _retention_closure_verification_receipt_rows(root),
        "retention_closure_closeout_records": _retention_closure_closeout_record_rows(root),
        "retention_completion_records": _retention_completion_record_rows(root),
        "retention_completion_verification_receipts": _retention_completion_verification_receipt_rows(root),
        "retention_completion_closeout_records": _retention_completion_closeout_record_rows(root),
        "retention_attestation_records": _retention_attestation_record_rows(root),
        "retention_attestation_verification_receipts": _retention_attestation_verification_receipt_rows(root),
        "retention_attestation_closeout_records": _retention_attestation_closeout_record_rows(root),
        "retention_seal_records": _retention_seal_record_rows(root),
        "retention_seal_verification_receipts": _retention_seal_verification_receipt_rows(root),
        "retention_seal_closeout_records": _retention_seal_closeout_record_rows(root),
        "retention_notarization_records": _retention_notarization_record_rows(root),
        "retention_notarization_verification_receipts": _retention_notarization_verification_receipt_rows(root),
        "retention_notarization_closeout_records": _retention_notarization_closeout_record_rows(root),
        "retention_archive_anchor_records": _retention_archive_anchor_record_rows(root),
        "retention_archive_anchor_verification_receipts": _retention_archive_anchor_verification_receipt_rows(root),
        "retention_archive_anchor_closeout_records": _retention_archive_anchor_closeout_record_rows(root),
        "retention_endcap_records": _retention_endcap_record_rows(root),
        "retention_endcap_verification_receipts": _retention_endcap_verification_receipt_rows(root),
        "retention_endcap_closeout_records": _retention_endcap_closeout_record_rows(root),
        "retention_final_index_records": _retention_final_index_record_rows(root),
        "retention_final_index_verification_receipts": _retention_final_index_verification_receipt_rows(root),
        "retention_final_index_closeout_records": _retention_final_index_closeout_record_rows(root),
        "retention_master_ledger_records": _retention_master_ledger_record_rows(root),
        "retention_master_ledger_verification_receipts": _retention_master_ledger_verification_receipt_rows(root),
        "retention_master_ledger_closeout_records": _retention_master_ledger_closeout_record_rows(root),
        "retention_terminal_manifest_records": _retention_terminal_manifest_record_rows(root),
        "retention_terminal_manifest_verification_receipts": _retention_terminal_manifest_verification_receipt_rows(root),
        "retention_terminal_manifest_closeout_records": _retention_terminal_manifest_closeout_record_rows(root),
        "retention_repository_release_records": _retention_repository_release_record_rows(root),
        "retention_repository_release_verification_receipts": _retention_repository_release_verification_receipt_rows(root),
        "retention_repository_release_closeout_records": _retention_repository_release_closeout_record_rows(root),
        "retention_deployment_release_records": _retention_deployment_release_record_rows(root),
        "retention_deployment_release_verification_receipts": _retention_deployment_release_verification_receipt_rows(root),
        "retention_deployment_release_closeout_records": _retention_deployment_release_closeout_record_rows(root),
        "retention_availability_notice_records": _retention_availability_notice_record_rows(root),
        "retention_availability_notice_verification_receipts": _retention_availability_notice_verification_receipt_rows(root),
        "retention_availability_notice_closeout_records": _retention_availability_notice_closeout_record_rows(root),
        "retention_release_acknowledgement_records": _retention_release_acknowledgement_record_rows(root),
        "retention_release_acknowledgement_verification_receipts": _retention_release_acknowledgement_verification_receipt_rows(root),
        "retention_release_acknowledgement_closeout_records": _retention_release_acknowledgement_closeout_record_rows(root),
        "retention_release_confirmation_records": _retention_release_confirmation_record_rows(root),
        "retention_release_confirmation_verification_receipts": _retention_release_confirmation_verification_receipt_rows(root),
        "retention_release_confirmation_closeout_records": _retention_release_confirmation_closeout_record_rows(root),
        "retention_distribution_package_records": _retention_distribution_package_record_rows(root),
        "retention_distribution_package_verification_receipts": _retention_distribution_package_verification_receipt_rows(root),
        "retention_distribution_package_closeout_records": _retention_distribution_package_closeout_record_rows(root),
        "retention_distribution_manifest_records": _retention_distribution_manifest_record_rows(root),
        "retention_distribution_manifest_verification_receipts": _retention_distribution_manifest_verification_receipt_rows(root),
        "retention_distribution_manifest_closeout_records": _retention_distribution_manifest_closeout_record_rows(root),
        "retention_access_publication_records": _retention_access_publication_record_rows(root),
        "retention_access_publication_verification_receipts": _retention_access_publication_verification_receipt_rows(root),
        "retention_access_publication_closeout_records": _retention_access_publication_closeout_record_rows(root),
        "retention_access_grant_records": _retention_access_grant_record_rows(root),
        "retention_access_grant_verification_receipts": _retention_access_grant_verification_receipt_rows(root),
        "retention_access_grant_closeout_records": _retention_access_grant_closeout_record_rows(root),
        "retention_access_ledger_records": _retention_access_ledger_record_rows(root),
        "retention_access_ledger_verification_receipts": _retention_access_ledger_verification_receipt_rows(root),
        "retention_access_ledger_closeout_records": _retention_access_ledger_closeout_record_rows(root),
        "retention_retrieval_catalog_records": _retention_retrieval_catalog_record_rows(root),
        "retention_retrieval_catalog_verification_receipts": _retention_retrieval_catalog_verification_receipt_rows(root),
        "retention_retrieval_catalog_closeout_records": _retention_retrieval_catalog_closeout_record_rows(root),
        "retention_retrieval_endpoint_records": _retention_retrieval_endpoint_record_rows(root),
        "retention_retrieval_endpoint_verification_receipts": _retention_retrieval_endpoint_verification_receipt_rows(root),
        "retention_retrieval_endpoint_closeout_records": _retention_retrieval_endpoint_closeout_record_rows(root),
        "retention_retrieval_token_records": _retention_retrieval_token_record_rows(root),
        "retention_retrieval_token_verification_receipts": _retention_retrieval_token_verification_receipt_rows(root),
        "retention_retrieval_token_closeout_records": _retention_retrieval_token_closeout_record_rows(root),
        "retention_consumer_receipt_records": _retention_consumer_receipt_record_rows(root),
        "retention_consumer_receipt_verification_receipts": _retention_consumer_receipt_verification_receipt_rows(root),
        "retention_consumer_receipt_closeout_records": _retention_consumer_receipt_closeout_record_rows(root),
        "retention_publication_rollup_records": _retention_publication_rollup_record_rows(root),
        "retention_publication_rollup_verification_receipts": _retention_publication_rollup_verification_receipt_rows(root),
        "retention_publication_rollup_closeout_records": _retention_publication_rollup_closeout_record_rows(root),
        "retention_distribution_receipt_records": _retention_distribution_receipt_record_rows(root),
        "retention_distribution_receipt_verification_receipts": _retention_distribution_receipt_verification_receipt_rows(root),
        "retention_distribution_receipt_closeout_records": _retention_distribution_receipt_closeout_record_rows(root),
        "retention_access_audit_snapshot_records": _retention_access_audit_snapshot_record_rows(root),
        "retention_access_audit_snapshot_verification_receipts": _retention_access_audit_snapshot_verification_receipt_rows(root),
        "retention_access_audit_snapshot_closeout_records": _retention_access_audit_snapshot_closeout_record_rows(root),
        "retention_release_health_snapshot_records": _retention_release_health_snapshot_record_rows(root),
        "retention_release_health_snapshot_verification_receipts": _retention_release_health_snapshot_verification_receipt_rows(root),
        "retention_release_health_snapshot_closeout_records": _retention_release_health_snapshot_closeout_record_rows(root),
        "retention_release_usage_summary_records": _retention_release_usage_summary_record_rows(root),
        "retention_release_usage_summary_verification_receipts": _retention_release_usage_summary_verification_receipt_rows(root),
        "retention_release_usage_summary_closeout_records": _retention_release_usage_summary_closeout_record_rows(root),
        "retention_retention_exposure_report_records": _retention_retention_exposure_report_record_rows(root),
        "retention_retention_exposure_report_verification_receipts": _retention_retention_exposure_report_verification_receipt_rows(root),
        "retention_retention_exposure_report_closeout_records": _retention_retention_exposure_report_closeout_record_rows(root),
        "retention_release_closeout_summary_records": _retention_release_closeout_summary_record_rows(root),
        "retention_release_closeout_summary_verification_receipts": _retention_release_closeout_summary_verification_receipt_rows(root),
        "retention_release_closeout_summary_closeout_records": _retention_release_closeout_summary_closeout_record_rows(root),
        "retention_public_record_index_records": _retention_public_record_index_record_rows(root),
        "retention_public_record_index_verification_receipts": _retention_public_record_index_verification_receipt_rows(root),
        "retention_public_record_index_closeout_records": _retention_public_record_index_closeout_record_rows(root),
        "retention_final_release_bundle_records": _retention_final_release_bundle_record_rows(root),
        "retention_final_release_bundle_verification_receipts": _retention_final_release_bundle_verification_receipt_rows(root),
        "retention_final_release_bundle_closeout_records": _retention_final_release_bundle_closeout_record_rows(root),
        "retention_terminal_access_notice_records": _retention_terminal_access_notice_record_rows(root),
        "retention_terminal_access_notice_verification_receipts": _retention_terminal_access_notice_verification_receipt_rows(root),
        "retention_terminal_access_notice_closeout_records": _retention_terminal_access_notice_closeout_record_rows(root),
        "retention_release_acceptance_records": _retention_release_acceptance_record_rows(root),
        "retention_release_acceptance_verification_receipts": _retention_release_acceptance_verification_receipt_rows(root),
        "retention_release_acceptance_closeout_records": _retention_release_acceptance_closeout_record_rows(root),
        "retention_access_completion_records": _retention_access_completion_record_rows(root),
        "retention_access_completion_verification_receipts": _retention_access_completion_verification_receipt_rows(root),
        "retention_access_completion_closeout_records": _retention_access_completion_closeout_record_rows(root),
        "retention_publication_certificate_records": _retention_publication_certificate_record_rows(root),
        "retention_publication_certificate_verification_receipts": _retention_publication_certificate_verification_receipt_rows(root),
        "retention_publication_certificate_closeout_records": _retention_publication_certificate_closeout_record_rows(root),
        "retention_distribution_closure_notice_records": _retention_distribution_closure_notice_record_rows(root),
        "retention_distribution_closure_notice_verification_receipts": _retention_distribution_closure_notice_verification_receipt_rows(root),
        "retention_distribution_closure_notice_closeout_records": _retention_distribution_closure_notice_closeout_record_rows(root),
        "retention_public_access_register_records": _retention_public_access_register_record_rows(root),
        "retention_public_access_register_verification_receipts": _retention_public_access_register_verification_receipt_rows(root),
        "retention_public_access_register_closeout_records": _retention_public_access_register_closeout_record_rows(root),
        "retention_release_access_index_records": _retention_release_access_index_record_rows(root),
        "retention_release_access_index_verification_receipts": _retention_release_access_index_verification_receipt_rows(root),
        "retention_release_access_index_closeout_records": _retention_release_access_index_closeout_record_rows(root),
        "retention_release_access_verification_summary_records": _retention_release_access_verification_summary_record_rows(root),
        "retention_release_access_verification_summary_verification_receipts": _retention_release_access_verification_summary_verification_receipt_rows(root),
        "retention_release_access_verification_summary_closeout_records": _retention_release_access_verification_summary_closeout_record_rows(root),
        "retention_release_access_closeout_summary_records": _retention_release_access_closeout_summary_record_rows(root),
        "retention_release_access_closeout_summary_verification_receipts": _retention_release_access_closeout_summary_verification_receipt_rows(root),
        "retention_release_access_closeout_summary_closeout_records": _retention_release_access_closeout_summary_closeout_record_rows(root),
        "retention_archive_availability_rollup_records": _retention_archive_availability_rollup_record_rows(root),
        "retention_archive_availability_rollup_verification_receipts": _retention_archive_availability_rollup_verification_receipt_rows(root),
        "retention_archive_availability_rollup_closeout_records": _retention_archive_availability_rollup_closeout_record_rows(root),
        "retention_retrieval_readiness_snapshot_records": _retention_retrieval_readiness_snapshot_record_rows(root),
        "retention_retrieval_readiness_snapshot_verification_receipts": _retention_retrieval_readiness_snapshot_verification_receipt_rows(root),
        "retention_retrieval_readiness_snapshot_closeout_records": _retention_retrieval_readiness_snapshot_closeout_record_rows(root),
        "retention_consumer_availability_notice_records": _retention_consumer_availability_notice_record_rows(root),
        "retention_consumer_availability_notice_verification_receipts": _retention_consumer_availability_notice_verification_receipt_rows(root),
        "retention_consumer_availability_notice_closeout_records": _retention_consumer_availability_notice_closeout_record_rows(root),
        "retention_public_release_receipt_records": _retention_public_release_receipt_record_rows(root),
        "retention_public_release_receipt_verification_receipts": _retention_public_release_receipt_verification_receipt_rows(root),
        "retention_public_release_receipt_closeout_records": _retention_public_release_receipt_closeout_record_rows(root),
        "retention_release_exception_register_records": _retention_release_exception_register_record_rows(root),
        "retention_release_exception_register_verification_receipts": _retention_release_exception_register_verification_receipt_rows(root),
        "retention_release_exception_register_closeout_records": _retention_release_exception_register_closeout_record_rows(root),
        "retention_release_exception_summary_records": _retention_release_exception_summary_record_rows(root),
        "retention_release_exception_summary_verification_receipts": _retention_release_exception_summary_verification_receipt_rows(root),
        "retention_release_exception_summary_closeout_records": _retention_release_exception_summary_closeout_record_rows(root),
        "retention_release_metrics_snapshot_records": _retention_release_metrics_snapshot_record_rows(root),
        "retention_release_metrics_snapshot_verification_receipts": _retention_release_metrics_snapshot_verification_receipt_rows(root),
        "retention_release_metrics_snapshot_closeout_records": _retention_release_metrics_snapshot_closeout_record_rows(root),
        "retention_release_terminal_report_records": _retention_release_terminal_report_record_rows(root),
        "retention_release_terminal_report_verification_receipts": _retention_release_terminal_report_verification_receipt_rows(root),
        "retention_release_terminal_report_closeout_records": _retention_release_terminal_report_closeout_record_rows(root),
        "retention_final_publication_notice_records": _retention_final_publication_notice_record_rows(root),
        "retention_final_publication_notice_verification_receipts": _retention_final_publication_notice_verification_receipt_rows(root),
        "retention_final_publication_notice_closeout_records": _retention_final_publication_notice_closeout_record_rows(root),
        "retention_publication_completion_records": _retention_publication_completion_record_rows(root),
        "retention_publication_completion_verification_receipts": _retention_publication_completion_verification_receipt_rows(root),
        "retention_publication_completion_closeout_records": _retention_publication_completion_closeout_record_rows(root),
        "retention_publication_closure_certificate_records": _retention_publication_closure_certificate_record_rows(root),
        "retention_publication_closure_certificate_verification_receipts": _retention_publication_closure_certificate_verification_receipt_rows(root),
        "retention_publication_closure_certificate_closeout_records": _retention_publication_closure_certificate_closeout_record_rows(root),
        "retention_terminal_public_index_records": _retention_terminal_public_index_record_rows(root),
        "retention_terminal_public_index_verification_receipts": _retention_terminal_public_index_verification_receipt_rows(root),
        "retention_terminal_public_index_closeout_records": _retention_terminal_public_index_closeout_record_rows(root),
        "retention_terminal_public_digest_records": _retention_terminal_public_digest_record_rows(root),
        "retention_terminal_public_digest_verification_receipts": _retention_terminal_public_digest_verification_receipt_rows(root),
        "retention_terminal_public_digest_closeout_records": _retention_terminal_public_digest_closeout_record_rows(root),
        "retention_terminal_public_archive_copy_records": _retention_terminal_public_archive_copy_record_rows(root),
        "retention_terminal_public_archive_copy_verification_receipts": _retention_terminal_public_archive_copy_verification_receipt_rows(root),
        "retention_terminal_public_archive_copy_closeout_records": _retention_terminal_public_archive_copy_closeout_record_rows(root),
        "retention_terminal_public_audit_snapshot_records": _retention_terminal_public_audit_snapshot_record_rows(root),
        "retention_terminal_public_audit_snapshot_verification_receipts": _retention_terminal_public_audit_snapshot_verification_receipt_rows(root),
        "retention_terminal_public_audit_snapshot_closeout_records": _retention_terminal_public_audit_snapshot_closeout_record_rows(root),
        "retention_terminal_public_metrics_snapshot_records": _retention_terminal_public_metrics_snapshot_record_rows(root),
        "retention_terminal_public_metrics_snapshot_verification_receipts": _retention_terminal_public_metrics_snapshot_verification_receipt_rows(root),
        "retention_terminal_public_metrics_snapshot_closeout_records": _retention_terminal_public_metrics_snapshot_closeout_record_rows(root),
        "retention_terminal_public_release_summary_records": _retention_terminal_public_release_summary_record_rows(root),
        "retention_terminal_public_release_summary_verification_receipts": _retention_terminal_public_release_summary_verification_receipt_rows(root),
        "retention_terminal_public_release_summary_closeout_records": _retention_terminal_public_release_summary_closeout_record_rows(root),
        "retention_terminal_distribution_summary_records": _retention_terminal_distribution_summary_record_rows(root),
        "retention_terminal_distribution_summary_verification_receipts": _retention_terminal_distribution_summary_verification_receipt_rows(root),
        "retention_terminal_distribution_summary_closeout_records": _retention_terminal_distribution_summary_closeout_record_rows(root),
        "retention_terminal_access_summary_records": _retention_terminal_access_summary_record_rows(root),
        "retention_terminal_access_summary_verification_receipts": _retention_terminal_access_summary_verification_receipt_rows(root),
        "retention_terminal_access_summary_closeout_records": _retention_terminal_access_summary_closeout_record_rows(root),
        "retention_terminal_retrieval_summary_records": _retention_terminal_retrieval_summary_record_rows(root),
        "retention_terminal_retrieval_summary_verification_receipts": _retention_terminal_retrieval_summary_verification_receipt_rows(root),
        "retention_terminal_retrieval_summary_closeout_records": _retention_terminal_retrieval_summary_closeout_record_rows(root),
        "retention_terminal_consumer_summary_records": _retention_terminal_consumer_summary_record_rows(root),
        "retention_terminal_consumer_summary_verification_receipts": _retention_terminal_consumer_summary_verification_receipt_rows(root),
        "retention_terminal_consumer_summary_closeout_records": _retention_terminal_consumer_summary_closeout_record_rows(root),
        "retention_terminal_exception_summary_records": _retention_terminal_exception_summary_record_rows(root),
        "retention_terminal_exception_summary_verification_receipts": _retention_terminal_exception_summary_verification_receipt_rows(root),
        "retention_terminal_exception_summary_closeout_records": _retention_terminal_exception_summary_closeout_record_rows(root),
        "retention_terminal_archive_report_records": _retention_terminal_archive_report_record_rows(root),
        "retention_terminal_archive_report_verification_receipts": _retention_terminal_archive_report_verification_receipt_rows(root),
        "retention_terminal_archive_report_closeout_records": _retention_terminal_archive_report_closeout_record_rows(root),
        "retention_terminal_release_annex_records": _retention_terminal_release_annex_record_rows(root),
        "retention_terminal_release_annex_verification_receipts": _retention_terminal_release_annex_verification_receipt_rows(root),
        "retention_terminal_release_annex_closeout_records": _retention_terminal_release_annex_closeout_record_rows(root),
        "retention_terminal_publication_bundle_records": _retention_terminal_publication_bundle_record_rows(root),
        "retention_terminal_publication_bundle_verification_receipts": _retention_terminal_publication_bundle_verification_receipt_rows(root),
        "retention_terminal_publication_bundle_closeout_records": _retention_terminal_publication_bundle_closeout_record_rows(root),
        "retention_publication_terminal_status_records": _retention_publication_terminal_status_record_rows(root),
        "merge_decisions": _merge_decision_rows(root),
        "intake_decisions": _intake_decision_rows(root),
        "routes": _routing_rows(root),
        "delivery_channels": _delivery_rows(root),
        "delivery_jobs": _delivery_job_rows(root),
        "transport_adapters": _transport_rows(root),
        "transport_receipts": _transport_receipt_rows(root),
        "handling_labels": _handling_rows(root),
        "retention_classes": _retention_rows(root),
        "integrity_receipts": _integrity_rows(root),
        "sharing_scopes": sharing_scopes,
        "review_gates": review_gates,
    }
    catalog["counts"] = {key: len(value) for key, value in catalog.items() if isinstance(value, list)}
    return catalog


def _format_table(title: str, rows: list[dict[str, Any]], columns: list[str]) -> list[str]:
    lines = ["", title, "-" * len(title)]
    if not rows:
        lines.append("(none)")
        return lines
    widths = {c: max(len(c), *(len(str(r.get(c, ""))) for r in rows)) for c in columns}
    lines.append("  ".join(c.ljust(widths[c]) for c in columns))
    lines.append("  ".join("-" * widths[c] for c in columns))
    for row in rows:
        lines.append("  ".join(str(row.get(c, "")).ljust(widths[c]) for c in columns))
    return lines


def format_catalog(catalog: dict[str, Any]) -> str:
    counts = catalog["counts"]
    lines = [
        f"PFEM catalog root: {catalog['root']}",
        "Counts: "
        f"{counts.get('capabilities', 0)} capabilities, {counts.get('adapters', 0)} adapters, "
        f"{counts.get('profiles', 0)} profiles, {counts.get('nodes', 0)} nodes, "
        f"{counts.get('sources', 0)} sources, {counts.get('dispatch_rules', 0)} dispatch rules, "
        f"{counts.get('dispatch_decisions', 0)} dispatch decisions, "
        f"{counts.get('outbox_items', 0)} outbox items, "
        f"{counts.get('inbox_items', 0)} inbox items, "
        f"{counts.get('import_records', 0)} import records, "
        f"{counts.get('conflict_records', 0)} conflict records, "
        f"{counts.get('apply_receipts', 0)} apply receipts, "
        f"{counts.get('state_checkpoints', 0)} state checkpoints, "
        f"{counts.get('state_transitions', 0)} state transitions, "
        f"{counts.get('snapshot_manifests', 0)} snapshot manifests, "
        f"{counts.get('snapshot_verification_receipts', 0)} snapshot verification receipts, "
        f"{counts.get('recovery_points', 0)} recovery points, "
        f"{counts.get('restore_plans', 0)} restore plans, "
        f"{counts.get('restore_approvals', 0)} restore approvals, "
        f"{counts.get('restore_receipts', 0)} restore receipts, "
        f"{counts.get('restore_verification_receipts', 0)} restore verification receipts, "
        f"{counts.get('restore_closeout_records', 0)} restore closeout records, "
        f"{counts.get('disposition_records', 0)} disposition records, "
        f"{counts.get('disposition_receipts', 0)} disposition receipts, "
        f"{counts.get('custody_records', 0)} custody records, "
        f"{counts.get('custody_verification_receipts', 0)} custody verification receipts, "
        f"{counts.get('custody_transfer_records', 0)} custody transfer records, "
        f"{counts.get('custody_transfer_verification_receipts', 0)} custody transfer verification receipts, "
        f"{counts.get('custody_closeout_records', 0)} custody closeout records, "
        f"{counts.get('custody_chain_records', 0)} custody chain records, "
        f"{counts.get('custody_chain_verification_receipts', 0)} custody chain verification receipts, "
        f"{counts.get('custody_ledger_records', 0)} custody ledger records, "
        f"{counts.get('custody_ledger_verification_receipts', 0)} custody ledger verification receipts, "
        f"{counts.get('custody_release_requests', 0)} custody release requests, "
        f"{counts.get('custody_release_approvals', 0)} custody release approvals, "
        f"{counts.get('custody_release_receipts', 0)} custody release receipts, "
        f"{counts.get('custody_release_verification_receipts', 0)} custody release verification receipts, "
        f"{counts.get('custody_release_closeout_records', 0)} custody release closeout records, "
        f"{counts.get('custody_release_chain_records', 0)} custody release chain records, "
        f"{counts.get('custody_release_chain_verification_receipts', 0)} custody release chain verification receipts, "
        f"{counts.get('custody_lifecycle_records', 0)} custody lifecycle records, "
        f"{counts.get('custody_lifecycle_verification_receipts', 0)} custody lifecycle verification receipts, "
        f"{counts.get('custody_lifecycle_closeout_records', 0)} custody lifecycle closeout records, "
        f"{counts.get('archive_manifest_records', 0)} archive manifest records, "
        f"{counts.get('archive_receipts', 0)} archive receipts, "
        f"{counts.get('archive_verification_receipts', 0)} archive verification receipts, "
        f"{counts.get('archive_closeout_records', 0)} archive closeout records, "
        f"{counts.get('archive_chain_records', 0)} archive chain records, "
        f"{counts.get('archive_chain_verification_receipts', 0)} archive chain verification receipts, "
        f"{counts.get('archive_index_records', 0)} archive index records, "
        f"{counts.get('archive_index_verification_receipts', 0)} archive index verification receipts, "
        f"{counts.get('archive_index_closeout_records', 0)} archive index closeout records, "
        f"{counts.get('archive_lifecycle_records', 0)} archive lifecycle records, "
        f"{counts.get('archive_lifecycle_verification_receipts', 0)} archive lifecycle verification receipts, "
        f"{counts.get('archive_lifecycle_closeout_records', 0)} archive lifecycle closeout records, "
        f"{counts.get('preservation_records', 0)} preservation records, "
        f"{counts.get('preservation_verification_receipts', 0)} preservation verification receipts, "
        f"{counts.get('preservation_closeout_records', 0)} preservation closeout records, "
        f"{counts.get('preservation_chain_records', 0)} preservation chain records, "
        f"{counts.get('preservation_chain_verification_receipts', 0)} preservation chain verification receipts, "
        f"{counts.get('retention_review_records', 0)} retention review records, "
        f"{counts.get('retention_review_verification_receipts', 0)} retention review verification receipts, "
        f"{counts.get('retention_decision_records', 0)} retention decision records, "
        f"{counts.get('retention_decision_approvals', 0)} retention decision approvals, "
        f"{counts.get('retention_action_receipts', 0)} retention action receipts, "
        f"{counts.get('retention_action_verification_receipts', 0)} retention action verification receipts, "
        f"{counts.get('retention_action_closeout_records', 0)} retention action closeout records, "
        f"{counts.get('retention_chain_records', 0)} retention chain records, "
        f"{counts.get('retention_chain_verification_receipts', 0)} retention chain verification receipts, "
        f"{counts.get('retention_lifecycle_records', 0)} retention lifecycle records, "
        f"{counts.get('retention_lifecycle_verification_receipts', 0)} retention lifecycle verification receipts, "
        f"{counts.get('retention_lifecycle_closeout_records', 0)} retention lifecycle closeout records, "
        f"{counts.get('retention_ledger_records', 0)} retention ledger records, "
        f"{counts.get('retention_ledger_verification_receipts', 0)} retention ledger verification receipts, "
        f"{counts.get('retention_ledger_closeout_records', 0)} retention ledger closeout records, "
        f"{counts.get('retention_policy_compliance_records', 0)} retention policy compliance records, "
        f"{counts.get('retention_policy_compliance_verification_receipts', 0)} retention policy compliance verification receipts, "
        f"{counts.get('retention_obligation_records', 0)} retention obligation records, "
        f"{counts.get('retention_obligation_verification_receipts', 0)} retention obligation verification receipts, "
        f"{counts.get('retention_schedule_records', 0)} retention schedule records, "
        f"{counts.get('retention_schedule_verification_receipts', 0)} retention schedule verification receipts, "
        f"{counts.get('retention_schedule_closeout_records', 0)} retention schedule closeout records, "
        f"{counts.get('retention_cycle_records', 0)} retention cycle records, "
        f"{counts.get('retention_cycle_verification_receipts', 0)} retention cycle verification receipts, "
        f"{counts.get('retention_cycle_closeout_records', 0)} retention cycle closeout records, "
        f"{counts.get('retention_hold_records', 0)} retention hold records, "
        f"{counts.get('retention_hold_verification_receipts', 0)} retention hold verification receipts, "
        f"{counts.get('retention_hold_closeout_records', 0)} retention hold closeout records, "
        f"{counts.get('retention_status_snapshot_records', 0)} retention status snapshot records, "
        f"{counts.get('retention_status_snapshot_verification_receipts', 0)} retention status snapshot verification receipts, "
        f"{counts.get('retention_rollup_records', 0)} retention rollup records, "
        f"{counts.get('retention_rollup_verification_receipts', 0)} retention rollup verification receipts, "
        f"{counts.get('retention_rollup_closeout_records', 0)} retention rollup closeout records, "
        f"{counts.get('retention_report_records', 0)} retention report records, "
        f"{counts.get('retention_report_verification_receipts', 0)} retention report verification receipts, "
        f"{counts.get('retention_report_closeout_records', 0)} retention report closeout records, "
        f"{counts.get('retention_publication_records', 0)} retention publication records, "
        f"{counts.get('retention_publication_verification_receipts', 0)} retention publication verification receipts, "
        f"{counts.get('retention_publication_closeout_records', 0)} retention publication closeout records, "
        f"{counts.get('retention_dashboard_snapshot_records', 0)} retention dashboard snapshot records, "
        f"{counts.get('retention_dashboard_snapshot_verification_receipts', 0)} retention dashboard snapshot verification receipts, "
        f"{counts.get('retention_dashboard_snapshot_closeout_records', 0)} retention dashboard snapshot closeout records, "
        f"{counts.get('retention_summary_records', 0)} retention summary records, "
        f"{counts.get('retention_summary_verification_receipts', 0)} retention summary verification receipts, "
        f"{counts.get('retention_summary_closeout_records', 0)} retention summary closeout records, "
        f"{counts.get('retention_export_records', 0)} retention export records, "
        f"{counts.get('retention_export_verification_receipts', 0)} retention export verification receipts, "
        f"{counts.get('retention_export_closeout_records', 0)} retention export closeout records, "
        f"{counts.get('retention_handoff_records', 0)} retention handoff records, "
        f"{counts.get('retention_handoff_verification_receipts', 0)} retention handoff verification receipts, "
        f"{counts.get('retention_handoff_closeout_records', 0)} retention handoff closeout records, "
        f"{counts.get('retention_acceptance_records', 0)} retention acceptance records, "
        f"{counts.get('retention_acceptance_verification_receipts', 0)} retention acceptance verification receipts, "
        f"{counts.get('retention_acceptance_closeout_records', 0)} retention acceptance closeout records, "
        f"{counts.get('retention_package_records', 0)} retention package records, "
        f"{counts.get('retention_package_verification_receipts', 0)} retention package verification receipts, "
        f"{counts.get('retention_package_closeout_records', 0)} retention package closeout records, "
        f"{counts.get('retention_finalization_records', 0)} retention finalization records, "
        f"{counts.get('retention_finalization_verification_receipts', 0)} retention finalization verification receipts, "
        f"{counts.get('retention_finalization_closeout_records', 0)} retention finalization closeout records, "
        f"{counts.get('retention_terminal_status_records', 0)} retention terminal status records, "
        f"{counts.get('retention_terminal_status_verification_receipts', 0)} retention terminal status verification receipts, "
        f"{counts.get('retention_terminal_status_closeout_records', 0)} retention terminal status closeout records, "
        f"{counts.get('retention_certificate_records', 0)} retention certificate records, "
        f"{counts.get('retention_certificate_verification_receipts', 0)} retention certificate verification receipts, "
        f"{counts.get('retention_certificate_closeout_records', 0)} retention certificate closeout records, "
        f"{counts.get('retention_registry_records', 0)} retention registry records, "
        f"{counts.get('retention_registry_verification_receipts', 0)} retention registry verification receipts, "
        f"{counts.get('retention_registry_closeout_records', 0)} retention registry closeout records, "
        f"{counts.get('retention_closure_records', 0)} retention closure records, "
        f"{counts.get('retention_closure_verification_receipts', 0)} retention closure verification receipts, "
        f"{counts.get('retention_closure_closeout_records', 0)} retention closure closeout records, "
        f"{counts.get('retention_completion_records', 0)} retention completion records, "
        f"{counts.get('retention_completion_verification_receipts', 0)} retention completion verification receipts, "
        f"{counts.get('retention_completion_closeout_records', 0)} retention completion closeout records, "
        f"{counts.get('retention_attestation_records', 0)} retention attestation records, "
        f"{counts.get('retention_attestation_verification_receipts', 0)} retention attestation verification receipts, "
        f"{counts.get('retention_attestation_closeout_records', 0)} retention attestation closeout records, "
        f"{counts.get('retention_seal_records', 0)} retention seal records, "
        f"{counts.get('retention_seal_verification_receipts', 0)} retention seal verification receipts, "
        f"{counts.get('retention_seal_closeout_records', 0)} retention seal closeout records, "
        f"{counts.get('retention_notarization_records', 0)} retention notarization records, "
        f"{counts.get('retention_notarization_verification_receipts', 0)} retention notarization verification receipts, "
        f"{counts.get('retention_notarization_closeout_records', 0)} retention notarization closeout records, "
        f"{counts.get('retention_archive_anchor_records', 0)} retention archive anchor records, "
        f"{counts.get('retention_archive_anchor_verification_receipts', 0)} retention archive anchor verification receipts, "
        f"{counts.get('retention_archive_anchor_closeout_records', 0)} retention archive anchor closeout records, "
        f"{counts.get('retention_endcap_records', 0)} retention endcap records, "
        f"{counts.get('retention_endcap_verification_receipts', 0)} retention endcap verification receipts, "
        f"{counts.get('retention_endcap_closeout_records', 0)} retention endcap closeout records, "
        f"{counts.get('retention_final_index_records', 0)} retention final index records, "
        f"{counts.get('retention_final_index_verification_receipts', 0)} retention final index verification receipts, "
        f"{counts.get('retention_final_index_closeout_records', 0)} retention final index closeout records, "
        f"{counts.get('retention_master_ledger_records', 0)} retention master ledger records, "
        f"{counts.get('retention_master_ledger_verification_receipts', 0)} retention master ledger verification receipts, "
        f"{counts.get('retention_master_ledger_closeout_records', 0)} retention master ledger closeout records, "
        f"{counts.get('retention_terminal_manifest_records', 0)} retention terminal manifest records, "
        f"{counts.get('retention_terminal_manifest_verification_receipts', 0)} retention terminal manifest verification receipts, "
        f"{counts.get('retention_terminal_manifest_closeout_records', 0)} retention terminal manifest closeout records, "
        f"{counts.get('retention_repository_release_records', 0)} retention repository release records, "
        f"{counts.get('retention_repository_release_verification_receipts', 0)} retention repository release verification receipts, "
        f"{counts.get('retention_repository_release_closeout_records', 0)} retention repository release closeout records, "
        f"{counts.get('retention_deployment_release_records', 0)} retention deployment release records, "
        f"{counts.get('retention_deployment_release_verification_receipts', 0)} retention deployment release verification receipts, "
        f"{counts.get('retention_deployment_release_closeout_records', 0)} retention deployment release closeout records, "
        f"{counts.get('retention_availability_notice_records', 0)} retention availability notice records, "
        f"{counts.get('retention_availability_notice_verification_receipts', 0)} retention availability notice verification receipts, "
        f"{counts.get('retention_availability_notice_closeout_records', 0)} retention availability notice closeout records, "
        f"{counts.get('retention_release_acknowledgement_records', 0)} retention release acknowledgement records, "
        f"{counts.get('retention_release_acknowledgement_verification_receipts', 0)} retention release acknowledgement verification receipts, "
        f"{counts.get('retention_release_acknowledgement_closeout_records', 0)} retention release acknowledgement closeout records, "
        f"{counts.get('retention_release_confirmation_records', 0)} retention release confirmation records, "
        f"{counts.get('retention_release_confirmation_verification_receipts', 0)} retention release confirmation verification receipts, "
        f"{counts.get('retention_release_confirmation_closeout_records', 0)} retention release confirmation closeout records, "
        f"{counts.get('retention_distribution_package_records', 0)} retention distribution package records, "
        f"{counts.get('retention_distribution_package_verification_receipts', 0)} retention distribution package verification receipts, "
        f"{counts.get('retention_distribution_package_closeout_records', 0)} retention distribution package closeout records, "
        f"{counts.get('retention_distribution_manifest_records', 0)} retention distribution manifest records, "
        f"{counts.get('retention_distribution_manifest_verification_receipts', 0)} retention distribution manifest verification receipts, "
        f"{counts.get('retention_distribution_manifest_closeout_records', 0)} retention distribution manifest closeout records, "
        f"{counts.get('retention_access_publication_records', 0)} retention access publication records, "
        f"{counts.get('retention_access_publication_verification_receipts', 0)} retention access publication verification receipts, "
        f"{counts.get('retention_access_publication_closeout_records', 0)} retention access publication closeout records, "
        f"{counts.get('retention_access_grant_records', 0)} retention access grant records, "
        f"{counts.get('retention_access_grant_verification_receipts', 0)} retention access grant verification receipts, "
        f"{counts.get('retention_access_grant_closeout_records', 0)} retention access grant closeout records, "
        f"{counts.get('retention_access_ledger_records', 0)} retention access ledger records, "
        f"{counts.get('retention_access_ledger_verification_receipts', 0)} retention access ledger verification receipts, "
        f"{counts.get('retention_access_ledger_closeout_records', 0)} retention access ledger closeout records, "
        f"{counts.get('retention_retrieval_catalog_records', 0)} retention retrieval catalog records, "
        f"{counts.get('retention_retrieval_catalog_verification_receipts', 0)} retention retrieval catalog verification receipts, "
        f"{counts.get('retention_retrieval_catalog_closeout_records', 0)} retention retrieval catalog closeout records, "
        f"{counts.get('retention_retrieval_endpoint_records', 0)} retention retrieval endpoint records, "
        f"{counts.get('retention_retrieval_endpoint_verification_receipts', 0)} retention retrieval endpoint verification receipts, "
        f"{counts.get('retention_retrieval_endpoint_closeout_records', 0)} retention retrieval endpoint closeout records, "
        f"{counts.get('retention_retrieval_token_records', 0)} retention retrieval token records, "
        f"{counts.get('retention_retrieval_token_verification_receipts', 0)} retention retrieval token verification receipts, "
        f"{counts.get('retention_retrieval_token_closeout_records', 0)} retention retrieval token closeout records, "
        f"{counts.get('retention_consumer_receipt_records', 0)} retention consumer receipt records, "
        f"{counts.get('retention_consumer_receipt_verification_receipts', 0)} retention consumer receipt verification receipts, "
        f"{counts.get('retention_consumer_receipt_closeout_records', 0)} retention consumer receipt closeout records, "
        f"{counts.get('retention_publication_rollup_records', 0)} retention publication rollup records, "
        f"{counts.get('retention_publication_rollup_verification_receipts', 0)} retention publication rollup verification receipts, "
        f"{counts.get('retention_publication_rollup_closeout_records', 0)} retention publication rollup closeout records, "
        f"{counts.get('retention_distribution_receipt_records', 0)} retention distribution receipt records, "
        f"{counts.get('retention_distribution_receipt_verification_receipts', 0)} retention distribution receipt verification receipts, "
        f"{counts.get('retention_distribution_receipt_closeout_records', 0)} retention distribution receipt closeout records, "
        f"{counts.get('retention_access_audit_snapshot_records', 0)} retention access audit snapshot records, "
        f"{counts.get('retention_access_audit_snapshot_verification_receipts', 0)} retention access audit snapshot verification receipts, "
        f"{counts.get('retention_access_audit_snapshot_closeout_records', 0)} retention access audit snapshot closeout records, "
        f"{counts.get('retention_release_health_snapshot_records', 0)} retention release health snapshot records, "
        f"{counts.get('retention_release_health_snapshot_verification_receipts', 0)} retention release health snapshot verification receipts, "
        f"{counts.get('retention_release_health_snapshot_closeout_records', 0)} retention release health snapshot closeout records, "
        f"{counts.get('retention_release_usage_summary_records', 0)} retention release usage summary records, "
        f"{counts.get('retention_release_usage_summary_verification_receipts', 0)} retention release usage summary verification receipts, "
        f"{counts.get('retention_release_usage_summary_closeout_records', 0)} retention release usage summary closeout records, "
        f"{counts.get('retention_retention_exposure_report_records', 0)} retention retention exposure report records, "
        f"{counts.get('retention_retention_exposure_report_verification_receipts', 0)} retention retention exposure report verification receipts, "
        f"{counts.get('retention_retention_exposure_report_closeout_records', 0)} retention retention exposure report closeout records, "
        f"{counts.get('retention_release_closeout_summary_records', 0)} retention release closeout summary records, "
        f"{counts.get('retention_release_closeout_summary_verification_receipts', 0)} retention release closeout summary verification receipts, "
        f"{counts.get('retention_release_closeout_summary_closeout_records', 0)} retention release closeout summary closeout records, "
        f"{counts.get('retention_public_record_index_records', 0)} retention public record index records, "
        f"{counts.get('retention_public_record_index_verification_receipts', 0)} retention public record index verification receipts, "
        f"{counts.get('retention_public_record_index_closeout_records', 0)} retention public record index closeout records, "
        f"{counts.get('retention_final_release_bundle_records', 0)} retention final release bundle records, "
        f"{counts.get('retention_final_release_bundle_verification_receipts', 0)} retention final release bundle verification receipts, "
        f"{counts.get('retention_final_release_bundle_closeout_records', 0)} retention final release bundle closeout records, "
        f"{counts.get('retention_terminal_access_notice_records', 0)} retention terminal access notice records, "
        f"{counts.get('retention_terminal_access_notice_verification_receipts', 0)} retention terminal access notice verification receipts, "
        f"{counts.get('retention_terminal_access_notice_closeout_records', 0)} retention terminal access notice closeout records, "
        f"{counts.get('retention_release_acceptance_records', 0)} retention release acceptance records, "
        f"{counts.get('retention_release_acceptance_verification_receipts', 0)} retention release acceptance verification receipts, "
        f"{counts.get('retention_release_acceptance_closeout_records', 0)} retention release acceptance closeout records, "
        f"{counts.get('retention_access_completion_records', 0)} retention access completion records, "
        f"{counts.get('retention_access_completion_verification_receipts', 0)} retention access completion verification receipts, "
        f"{counts.get('retention_access_completion_closeout_records', 0)} retention access completion closeout records, "
        f"{counts.get('retention_publication_certificate_records', 0)} retention publication certificate records, "
        f"{counts.get('retention_publication_certificate_verification_receipts', 0)} retention publication certificate verification receipts, "
        f"{counts.get('retention_publication_certificate_closeout_records', 0)} retention publication certificate closeout records, "
        f"{counts.get('retention_distribution_closure_notice_records', 0)} retention distribution closure notice records, "
        f"{counts.get('retention_distribution_closure_notice_verification_receipts', 0)} retention distribution closure notice verification receipts, "
        f"{counts.get('retention_distribution_closure_notice_closeout_records', 0)} retention distribution closure notice closeout records, "
        f"{counts.get('retention_public_access_register_records', 0)} retention public access register records, "
        f"{counts.get('retention_public_access_register_verification_receipts', 0)} retention public access register verification receipts, "
        f"{counts.get('retention_public_access_register_closeout_records', 0)} retention public access register closeout records, "
        f"{counts.get('retention_release_access_index_records', 0)} retention release access index records, "
        f"{counts.get('retention_release_access_index_verification_receipts', 0)} retention release access index verification receipts, "
        f"{counts.get('retention_release_access_index_closeout_records', 0)} retention release access index closeout records, "
        f"{counts.get('retention_release_access_verification_summary_records', 0)} retention release access verification summary records, "
        f"{counts.get('retention_release_access_verification_summary_verification_receipts', 0)} retention release access verification summary verification receipts, "
        f"{counts.get('retention_release_access_verification_summary_closeout_records', 0)} retention release access verification summary closeout records, "
        f"{counts.get('retention_release_access_closeout_summary_records', 0)} retention release access closeout summary records, "
        f"{counts.get('retention_release_access_closeout_summary_verification_receipts', 0)} retention release access closeout summary verification receipts, "
        f"{counts.get('retention_release_access_closeout_summary_closeout_records', 0)} retention release access closeout summary closeout records, "
        f"{counts.get('retention_archive_availability_rollup_records', 0)} retention archive availability rollup records, "
        f"{counts.get('retention_archive_availability_rollup_verification_receipts', 0)} retention archive availability rollup verification receipts, "
        f"{counts.get('retention_archive_availability_rollup_closeout_records', 0)} retention archive availability rollup closeout records, "
        f"{counts.get('retention_retrieval_readiness_snapshot_records', 0)} retention retrieval readiness snapshot records, "
        f"{counts.get('retention_retrieval_readiness_snapshot_verification_receipts', 0)} retention retrieval readiness snapshot verification receipts, "
        f"{counts.get('retention_retrieval_readiness_snapshot_closeout_records', 0)} retention retrieval readiness snapshot closeout records, "
        f"{counts.get('retention_consumer_availability_notice_records', 0)} retention consumer availability notice records, "
        f"{counts.get('retention_consumer_availability_notice_verification_receipts', 0)} retention consumer availability notice verification receipts, "
        f"{counts.get('retention_consumer_availability_notice_closeout_records', 0)} retention consumer availability notice closeout records, "
        f"{counts.get('retention_public_release_receipt_records', 0)} retention public release receipt records, "
        f"{counts.get('retention_public_release_receipt_verification_receipts', 0)} retention public release receipt verification receipts, "
        f"{counts.get('retention_public_release_receipt_closeout_records', 0)} retention public release receipt closeout records, "
        f"{counts.get('retention_release_exception_register_records', 0)} retention release exception register records, "
        f"{counts.get('retention_release_exception_register_verification_receipts', 0)} retention release exception register verification receipts, "
        f"{counts.get('retention_release_exception_register_closeout_records', 0)} retention release exception register closeout records, "
        f"{counts.get('retention_release_exception_summary_records', 0)} retention release exception summary records, "
        f"{counts.get('retention_release_exception_summary_verification_receipts', 0)} retention release exception summary verification receipts, "
        f"{counts.get('retention_release_exception_summary_closeout_records', 0)} retention release exception summary closeout records, "
        f"{counts.get('retention_release_metrics_snapshot_records', 0)} retention release metrics snapshot records, "
        f"{counts.get('retention_release_metrics_snapshot_verification_receipts', 0)} retention release metrics snapshot verification receipts, "
        f"{counts.get('retention_release_metrics_snapshot_closeout_records', 0)} retention release metrics snapshot closeout records, "
        f"{counts.get('retention_release_terminal_report_records', 0)} retention release terminal report records, "
        f"{counts.get('retention_release_terminal_report_verification_receipts', 0)} retention release terminal report verification receipts, "
        f"{counts.get('retention_release_terminal_report_closeout_records', 0)} retention release terminal report closeout records, "
        f"{counts.get('retention_final_publication_notice_records', 0)} retention final publication notice records, "
        f"{counts.get('retention_final_publication_notice_verification_receipts', 0)} retention final publication notice verification receipts, "
        f"{counts.get('retention_final_publication_notice_closeout_records', 0)} retention final publication notice closeout records, "
        f"{counts.get('retention_publication_completion_records', 0)} retention publication completion records, "
        f"{counts.get('retention_publication_completion_verification_receipts', 0)} retention publication completion verification receipts, "
        f"{counts.get('retention_publication_completion_closeout_records', 0)} retention publication completion closeout records, "
        f"{counts.get('retention_publication_closure_certificate_records', 0)} retention publication closure certificate records, "
        f"{counts.get('retention_publication_closure_certificate_verification_receipts', 0)} retention publication closure certificate verification receipts, "
        f"{counts.get('retention_publication_closure_certificate_closeout_records', 0)} retention publication closure certificate closeout records, "
        f"{counts.get('retention_terminal_public_index_records', 0)} retention terminal public index records, "
        f"{counts.get('retention_terminal_public_index_verification_receipts', 0)} retention terminal public index verification receipts, "
        f"{counts.get('retention_terminal_public_index_closeout_records', 0)} retention terminal public index closeout records, "
        f"{counts.get('retention_terminal_public_digest_records', 0)} retention terminal public digest records, "
        f"{counts.get('retention_terminal_public_digest_verification_receipts', 0)} retention terminal public digest verification receipts, "
        f"{counts.get('retention_terminal_public_digest_closeout_records', 0)} retention terminal public digest closeout records, "
        f"{counts.get('retention_terminal_public_archive_copy_records', 0)} retention terminal public archive copy records, "
        f"{counts.get('retention_terminal_public_archive_copy_verification_receipts', 0)} retention terminal public archive copy verification receipts, "
        f"{counts.get('retention_terminal_public_archive_copy_closeout_records', 0)} retention terminal public archive copy closeout records, "
        f"{counts.get('retention_terminal_public_audit_snapshot_records', 0)} retention terminal public audit snapshot records, "
        f"{counts.get('retention_terminal_public_audit_snapshot_verification_receipts', 0)} retention terminal public audit snapshot verification receipts, "
        f"{counts.get('retention_terminal_public_audit_snapshot_closeout_records', 0)} retention terminal public audit snapshot closeout records, "
        f"{counts.get('retention_terminal_public_metrics_snapshot_records', 0)} retention terminal public metrics snapshot records, "
        f"{counts.get('retention_terminal_public_metrics_snapshot_verification_receipts', 0)} retention terminal public metrics snapshot verification receipts, "
        f"{counts.get('retention_terminal_public_metrics_snapshot_closeout_records', 0)} retention terminal public metrics snapshot closeout records, "
        f"{counts.get('retention_terminal_public_release_summary_records', 0)} retention terminal public release summary records, "
        f"{counts.get('retention_terminal_public_release_summary_verification_receipts', 0)} retention terminal public release summary verification receipts, "
        f"{counts.get('retention_terminal_public_release_summary_closeout_records', 0)} retention terminal public release summary closeout records, "
        f"{counts.get('retention_terminal_distribution_summary_records', 0)} retention terminal distribution summary records, "
        f"{counts.get('retention_terminal_distribution_summary_verification_receipts', 0)} retention terminal distribution summary verification receipts, "
        f"{counts.get('retention_terminal_distribution_summary_closeout_records', 0)} retention terminal distribution summary closeout records, "
        f"{counts.get('retention_terminal_access_summary_records', 0)} retention terminal access summary records, "
        f"{counts.get('retention_terminal_access_summary_verification_receipts', 0)} retention terminal access summary verification receipts, "
        f"{counts.get('retention_terminal_access_summary_closeout_records', 0)} retention terminal access summary closeout records, "
        f"{counts.get('retention_terminal_retrieval_summary_records', 0)} retention terminal retrieval summary records, "
        f"{counts.get('retention_terminal_retrieval_summary_verification_receipts', 0)} retention terminal retrieval summary verification receipts, "
        f"{counts.get('retention_terminal_retrieval_summary_closeout_records', 0)} retention terminal retrieval summary closeout records, "
        f"{counts.get('retention_terminal_consumer_summary_records', 0)} retention terminal consumer summary records, "
        f"{counts.get('retention_terminal_consumer_summary_verification_receipts', 0)} retention terminal consumer summary verification receipts, "
        f"{counts.get('retention_terminal_consumer_summary_closeout_records', 0)} retention terminal consumer summary closeout records, "
        f"{counts.get('retention_terminal_exception_summary_records', 0)} retention terminal exception summary records, "
        f"{counts.get('retention_terminal_exception_summary_verification_receipts', 0)} retention terminal exception summary verification receipts, "
        f"{counts.get('retention_terminal_exception_summary_closeout_records', 0)} retention terminal exception summary closeout records, "
        f"{counts.get('retention_terminal_archive_report_records', 0)} retention terminal archive report records, "
        f"{counts.get('retention_terminal_archive_report_verification_receipts', 0)} retention terminal archive report verification receipts, "
        f"{counts.get('retention_terminal_archive_report_closeout_records', 0)} retention terminal archive report closeout records, "
        f"{counts.get('retention_terminal_release_annex_records', 0)} retention terminal release annex records, "
        f"{counts.get('retention_terminal_release_annex_verification_receipts', 0)} retention terminal release annex verification receipts, "
        f"{counts.get('retention_terminal_release_annex_closeout_records', 0)} retention terminal release annex closeout records, "
        f"{counts.get('retention_terminal_publication_bundle_records', 0)} retention terminal publication bundle records, "
        f"{counts.get('retention_terminal_publication_bundle_verification_receipts', 0)} retention terminal publication bundle verification receipts, "
        f"{counts.get('retention_terminal_publication_bundle_closeout_records', 0)} retention terminal publication bundle closeout records, "
        f"{counts.get('retention_publication_terminal_status_records', 0)} retention publication terminal status records, "
        f"{counts.get('merge_decisions', 0)} merge decisions, "
        f"{counts.get('intake_decisions', 0)} intake decisions, "
        f"{counts.get('routes', 0)} routes, {counts.get('delivery_channels', 0)} delivery channels, "
        f"{counts.get('delivery_jobs', 0)} delivery jobs, "
        f"{counts.get('transport_adapters', 0)} transport adapters, "
        f"{counts.get('transport_receipts', 0)} transport receipts",
    ]
    lines.extend(_format_table("Capabilities", catalog["capabilities"], ["capability_id", "capability_kind", "path"]))
    lines.extend(_format_table("Adapters", catalog["adapters"], ["adapter_id", "adapter_kind", "status", "path"]))
    lines.extend(_format_table("Profiles", catalog["profiles"], ["profile_id", "profile_kind", "status", "path"]))
    lines.extend(_format_table("Nodes", catalog["nodes"], ["node_id", "profile_id", "status", "path"]))
    lines.extend(_format_table("Sources", catalog["sources"], ["source_id", "source_kind", "adapter_id", "node_id", "status"]))
    lines.extend(_format_table("Examples", catalog["examples"], ["example_id", "profile_id", "runnable", "status", "path"]))
    lines.extend(_format_table("Reviews", catalog["reviews"], ["review_id", "review_gate", "decision", "sharing_scope"]))
    lines.extend(_format_table("Audit Events", catalog["audit_events"], ["audit_id", "event_kind", "actor_ref"]))
    lines.extend(_format_table("Bundles", catalog["bundles"], ["bundle_id", "bundle_kind", "sharing_scope", "path"]))
    lines.extend(_format_table("Exchange Receipts", catalog["exchange_receipts"], ["exchange_receipt_id", "receipt_kind", "bundle_id", "decision", "intake_decision_id"]))
    lines.extend(_format_table("Reconciliation Records", catalog["reconciliation_records"], ["reconciliation_id", "reconciliation_kind", "decision", "result_state"]))
    lines.extend(_format_table("Quality Levels", catalog["quality_levels"], ["confidence_level", "rank", "display_name"]))
    lines.extend(_format_table("Quality Assessments", catalog["quality_assessments"], ["quality_assessment_id", "confidence_level", "flags"]))
    lines.extend(_format_table("Action Kinds", catalog["action_kinds"], ["action_kind", "display_name"]))
    lines.extend(_format_table("Action Records", catalog["action_records"], ["action_id", "action_kind", "priority", "action_state"]))
    lines.extend(_format_table("Playbooks", catalog["playbooks"], ["playbook_id", "playbook_kind", "status", "steps"]))
    lines.extend(_format_table("Dispatch Rules", catalog["dispatch_rules"], ["dispatch_rule_id", "enabled", "max_attempts", "retry_delay_seconds"]))
    lines.extend(_format_table("Dispatch Decisions", catalog["dispatch_decisions"], ["dispatch_decision_id", "delivery_job_id", "decision", "reason_code"]))
    lines.extend(_format_table("Outbox Items", catalog["outbox_items"], ["outbox_item_id", "delivery_job_id", "outbox_state", "item_kind"]))
    lines.extend(_format_table("Inbox Items", catalog["inbox_items"], ["inbox_item_id", "transport_receipt_id", "inbox_state", "item_kind"]))
    lines.extend(_format_table("Import Records", catalog["import_records"], ["import_record_id", "exchange_receipt_id", "import_state", "import_kind"]))
    lines.extend(_format_table("Conflict Records", catalog["conflict_records"], ["conflict_record_id", "import_record_id", "conflict_state", "severity"]))
    lines.extend(_format_table("Apply Receipts", catalog["apply_receipts"], ["apply_receipt_id", "merge_decision_id", "apply_state", "receipt_kind"]))
    lines.extend(_format_table("State Checkpoints", catalog["state_checkpoints"], ["state_checkpoint_id", "node_id", "checkpoint_state", "included_refs"]))
    lines.extend(_format_table("State Transitions", catalog["state_transitions"], ["state_transition_id", "to_state_checkpoint_id", "transition_state", "changed_refs"]))
    lines.extend(_format_table("Snapshot Manifests", catalog["snapshot_manifests"], ["snapshot_manifest_id", "state_checkpoint_id", "snapshot_state", "items"]))
    lines.extend(_format_table("Snapshot Verification Receipts", catalog["snapshot_verification_receipts"], ["snapshot_verification_receipt_id", "snapshot_manifest_id", "verification_state", "checked_items"]))
    lines.extend(_format_table("Recovery Points", catalog["recovery_points"], ["recovery_point_id", "snapshot_verification_receipt_id", "recovery_state", "restore_scope"]))
    lines.extend(_format_table("Restore Plans", catalog["restore_plans"], ["restore_plan_id", "recovery_point_id", "plan_state", "restore_scope"]))
    lines.extend(_format_table("Restore Approvals", catalog["restore_approvals"], ["restore_approval_id", "restore_plan_id", "approval_state", "approved_scope"]))
    lines.extend(_format_table("Restore Receipts", catalog["restore_receipts"], ["restore_receipt_id", "restore_plan_id", "restore_state", "restore_scope"]))
    lines.extend(_format_table("Restore Verification Receipts", catalog["restore_verification_receipts"], ["restore_verification_receipt_id", "restore_receipt_id", "verification_state", "checked_refs"]))
    lines.extend(_format_table("Restore Closeout Records", catalog["restore_closeout_records"], ["restore_closeout_record_id", "restore_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Disposition Records", catalog["disposition_records"], ["disposition_record_id", "source_closeout_ref", "disposition_state", "actions"]))
    lines.extend(_format_table("Disposition Receipts", catalog["disposition_receipts"], ["disposition_receipt_id", "disposition_record_id", "receipt_state", "executed_actions"]))
    lines.extend(_format_table("Custody Records", catalog["custody_records"], ["custody_record_id", "disposition_receipt_id", "custody_state", "custody_location_kind"]))
    lines.extend(_format_table("Custody Verification Receipts", catalog["custody_verification_receipts"], ["custody_verification_receipt_id", "custody_record_id", "verification_state", "checked_refs"]))
    lines.extend(_format_table("Custody Transfer Records", catalog["custody_transfer_records"], ["custody_transfer_record_id", "custody_verification_receipt_id", "transfer_state", "transferred_refs"]))
    lines.extend(_format_table("Custody Transfer Verification Receipts", catalog["custody_transfer_verification_receipts"], ["custody_transfer_verification_receipt_id", "custody_transfer_record_id", "verification_state", "checked_refs"]))
    lines.extend(_format_table("Custody Closeout Records", catalog["custody_closeout_records"], ["custody_closeout_record_id", "custody_transfer_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Custody Chain Records", catalog["custody_chain_records"], ["custody_chain_record_id", "terminal_ref", "chain_state", "chain_refs"]))
    lines.extend(_format_table("Custody Chain Verification Receipts", catalog["custody_chain_verification_receipts"], ["custody_chain_verification_receipt_id", "custody_chain_record_id", "verification_state", "checked_chain_refs"]))
    lines.extend(_format_table("Custody Ledger Records", catalog["custody_ledger_records"], ["custody_ledger_record_id", "custody_chain_record_id", "ledger_state", "entry_refs"]))
    lines.extend(_format_table("Custody Ledger Verification Receipts", catalog["custody_ledger_verification_receipts"], ["custody_ledger_verification_receipt_id", "custody_ledger_record_id", "verification_state", "checked_entry_refs"]))
    lines.extend(_format_table("Custody Release Requests", catalog["custody_release_requests"], ["custody_release_request_id", "custody_ledger_record_id", "request_state", "requested_refs"]))
    lines.extend(_format_table("Custody Release Approvals", catalog["custody_release_approvals"], ["custody_release_approval_id", "custody_release_request_id", "approval_state", "approved_refs"]))
    lines.extend(_format_table("Custody Release Receipts", catalog["custody_release_receipts"], ["custody_release_receipt_id", "custody_release_approval_id", "release_state", "released_refs"]))
    lines.extend(_format_table("Custody Release Verification Receipts", catalog["custody_release_verification_receipts"], ["custody_release_verification_receipt_id", "custody_release_receipt_id", "verification_state", "checked_released_refs"]))
    lines.extend(_format_table("Custody Release Closeout Records", catalog["custody_release_closeout_records"], ["custody_release_closeout_record_id", "custody_release_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Custody Release Chain Records", catalog["custody_release_chain_records"], ["custody_release_chain_record_id", "terminal_ref", "chain_state", "chain_refs"]))
    lines.extend(_format_table("Custody Release Chain Verification Receipts", catalog["custody_release_chain_verification_receipts"], ["custody_release_chain_verification_receipt_id", "custody_release_chain_record_id", "verification_state", "checked_chain_refs"]))
    lines.extend(_format_table("Custody Lifecycle Records", catalog["custody_lifecycle_records"], ["custody_lifecycle_record_id", "lifecycle_state", "final_outcome", "lifecycle_refs"]))
    lines.extend(_format_table("Custody Lifecycle Verification Receipts", catalog["custody_lifecycle_verification_receipts"], ["custody_lifecycle_verification_receipt_id", "custody_lifecycle_record_id", "verification_state", "checked_lifecycle_refs"]))
    lines.extend(_format_table("Custody Lifecycle Closeout Records", catalog["custody_lifecycle_closeout_records"], ["custody_lifecycle_closeout_record_id", "custody_lifecycle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Archive Manifest Records", catalog["archive_manifest_records"], ["archive_manifest_record_id", "custody_lifecycle_closeout_record_id", "manifest_state", "manifested_refs"]))
    lines.extend(_format_table("Archive Receipts", catalog["archive_receipts"], ["archive_receipt_id", "archive_manifest_record_id", "archive_state", "archived_refs"]))
    lines.extend(_format_table("Archive Verification Receipts", catalog["archive_verification_receipts"], ["archive_verification_receipt_id", "archive_receipt_id", "verification_state", "checked_archived_refs"]))
    lines.extend(_format_table("Archive Closeout Records", catalog["archive_closeout_records"], ["archive_closeout_record_id", "archive_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Archive Chain Records", catalog["archive_chain_records"], ["archive_chain_record_id", "terminal_ref", "chain_state", "chain_refs"]))
    lines.extend(_format_table("Archive Chain Verification Receipts", catalog["archive_chain_verification_receipts"], ["archive_chain_verification_receipt_id", "archive_chain_record_id", "verification_state", "checked_chain_refs"]))
    lines.extend(_format_table("Archive Index Records", catalog["archive_index_records"], ["archive_index_record_id", "archive_chain_record_id", "index_state", "indexed_refs"]))
    lines.extend(_format_table("Archive Index Verification Receipts", catalog["archive_index_verification_receipts"], ["archive_index_verification_receipt_id", "archive_index_record_id", "verification_state", "checked_indexed_refs"]))
    lines.extend(_format_table("Archive Index Closeout Records", catalog["archive_index_closeout_records"], ["archive_index_closeout_record_id", "archive_index_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Archive Lifecycle Records", catalog["archive_lifecycle_records"], ["archive_lifecycle_record_id", "archive_index_closeout_record_id", "lifecycle_state", "lifecycle_refs"]))
    lines.extend(_format_table("Archive Lifecycle Verification Receipts", catalog["archive_lifecycle_verification_receipts"], ["archive_lifecycle_verification_receipt_id", "archive_lifecycle_record_id", "verification_state", "checked_lifecycle_refs"]))
    lines.extend(_format_table("Archive Lifecycle Closeout Records", catalog["archive_lifecycle_closeout_records"], ["archive_lifecycle_closeout_record_id", "archive_lifecycle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Preservation Records", catalog["preservation_records"], ["preservation_record_id", "archive_lifecycle_closeout_record_id", "preservation_state", "preserved_refs"]))
    lines.extend(_format_table("Preservation Verification Receipts", catalog["preservation_verification_receipts"], ["preservation_verification_receipt_id", "preservation_record_id", "verification_state", "checked_preserved_refs"]))
    lines.extend(_format_table("Preservation Closeout Records", catalog["preservation_closeout_records"], ["preservation_closeout_record_id", "preservation_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Preservation Chain Records", catalog["preservation_chain_records"], ["preservation_chain_record_id", "terminal_ref", "chain_state", "chain_refs"]))
    lines.extend(_format_table("Preservation Chain Verification Receipts", catalog["preservation_chain_verification_receipts"], ["preservation_chain_verification_receipt_id", "preservation_chain_record_id", "verification_state", "checked_chain_refs"]))
    lines.extend(_format_table("Retention Review Records", catalog["retention_review_records"], ["retention_review_record_id", "preservation_chain_verification_receipt_id", "review_state", "recommendation"]))
    lines.extend(_format_table("Retention Review Verification Receipts", catalog["retention_review_verification_receipts"], ["retention_review_verification_receipt_id", "retention_review_record_id", "verification_state", "checked_reviewed_refs"]))
    lines.extend(_format_table("Retention Decision Records", catalog["retention_decision_records"], ["retention_decision_record_id", "retention_review_verification_receipt_id", "decision_state", "decision"]))
    lines.extend(_format_table("Retention Decision Approvals", catalog["retention_decision_approvals"], ["retention_decision_approval_id", "retention_decision_record_id", "approval_state", "approved_decision"]))
    lines.extend(_format_table("Retention Action Receipts", catalog["retention_action_receipts"], ["retention_action_receipt_id", "retention_decision_approval_id", "action_state", "action"]))
    lines.extend(_format_table("Retention Action Verification Receipts", catalog["retention_action_verification_receipts"], ["retention_action_verification_receipt_id", "retention_action_receipt_id", "verification_state", "checked_action_refs"]))
    lines.extend(_format_table("Retention Action Closeout Records", catalog["retention_action_closeout_records"], ["retention_action_closeout_record_id", "retention_action_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Chain Records", catalog["retention_chain_records"], ["retention_chain_record_id", "terminal_ref", "chain_state", "chain_refs"]))
    lines.extend(_format_table("Retention Chain Verification Receipts", catalog["retention_chain_verification_receipts"], ["retention_chain_verification_receipt_id", "retention_chain_record_id", "verification_state", "checked_chain_refs"]))
    lines.extend(_format_table("Retention Lifecycle Records", catalog["retention_lifecycle_records"], ["retention_lifecycle_record_id", "retention_chain_record_id", "lifecycle_state", "lifecycle_refs"]))
    lines.extend(_format_table("Retention Lifecycle Verification Receipts", catalog["retention_lifecycle_verification_receipts"], ["retention_lifecycle_verification_receipt_id", "retention_lifecycle_record_id", "verification_state", "checked_lifecycle_refs"]))
    lines.extend(_format_table("Retention Lifecycle Closeout Records", catalog["retention_lifecycle_closeout_records"], ["retention_lifecycle_closeout_record_id", "retention_lifecycle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Ledger Records", catalog["retention_ledger_records"], ["retention_ledger_record_id", "retention_lifecycle_closeout_record_id", "ledger_state", "entry_refs"]))
    lines.extend(_format_table("Retention Ledger Verification Receipts", catalog["retention_ledger_verification_receipts"], ["retention_ledger_verification_receipt_id", "retention_ledger_record_id", "verification_state", "checked_entry_refs"]))
    lines.extend(_format_table("Retention Ledger Closeout Records", catalog["retention_ledger_closeout_records"], ["retention_ledger_closeout_record_id", "retention_ledger_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Policy Compliance Records", catalog["retention_policy_compliance_records"], ["retention_policy_compliance_record_id", "retention_ledger_verification_receipt_id", "compliance_state", "compliance_scope"]))
    lines.extend(_format_table("Retention Policy Compliance Verification Receipts", catalog["retention_policy_compliance_verification_receipts"], ["retention_policy_compliance_verification_receipt_id", "retention_policy_compliance_record_id", "verification_state", "checked_compliance_refs"]))
    lines.extend(_format_table("Retention Obligation Records", catalog["retention_obligation_records"], ["retention_obligation_record_id", "retention_policy_compliance_verification_receipt_id", "obligation_state", "obligation_scope"]))
    lines.extend(_format_table("Retention Obligation Verification Receipts", catalog["retention_obligation_verification_receipts"], ["retention_obligation_verification_receipt_id", "retention_obligation_record_id", "verification_state", "checked_obligation_refs"]))
    lines.extend(_format_table("Retention Schedule Records", catalog["retention_schedule_records"], ["retention_schedule_record_id", "retention_obligation_record_id", "schedule_state", "scheduled_action"]))
    lines.extend(_format_table("Retention Schedule Verification Receipts", catalog["retention_schedule_verification_receipts"], ["retention_schedule_verification_receipt_id", "retention_schedule_record_id", "verification_state", "checked_schedule_refs"]))
    lines.extend(_format_table("Retention Schedule Closeout Records", catalog["retention_schedule_closeout_records"], ["retention_schedule_closeout_record_id", "retention_schedule_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Cycle Records", catalog["retention_cycle_records"], ["retention_cycle_record_id", "retention_schedule_closeout_record_id", "cycle_state", "cycle_refs"]))
    lines.extend(_format_table("Retention Cycle Verification Receipts", catalog["retention_cycle_verification_receipts"], ["retention_cycle_verification_receipt_id", "retention_cycle_record_id", "verification_state", "checked_cycle_refs"]))
    lines.extend(_format_table("Retention Cycle Closeout Records", catalog["retention_cycle_closeout_records"], ["retention_cycle_closeout_record_id", "retention_cycle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Hold Records", catalog["retention_hold_records"], ["retention_hold_record_id", "retention_cycle_closeout_record_id", "hold_state", "hold_scope"]))
    lines.extend(_format_table("Retention Hold Verification Receipts", catalog["retention_hold_verification_receipts"], ["retention_hold_verification_receipt_id", "retention_hold_record_id", "verification_state", "checked_hold_refs"]))
    lines.extend(_format_table("Retention Hold Closeout Records", catalog["retention_hold_closeout_records"], ["retention_hold_closeout_record_id", "retention_hold_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Status Snapshot Records", catalog["retention_status_snapshot_records"], ["retention_status_snapshot_record_id", "retention_hold_closeout_record_id", "snapshot_state", "retention_status"]))
    lines.extend(_format_table("Retention Status Snapshot Verification Receipts", catalog["retention_status_snapshot_verification_receipts"], ["retention_status_snapshot_verification_receipt_id", "retention_status_snapshot_record_id", "verification_state", "checked_snapshot_refs"]))
    lines.extend(_format_table("Retention Rollup Records", catalog["retention_rollup_records"], ["retention_rollup_record_id", "retention_status_snapshot_record_id", "rollup_state", "rollup_scope"]))
    lines.extend(_format_table("Retention Rollup Verification Receipts", catalog["retention_rollup_verification_receipts"], ["retention_rollup_verification_receipt_id", "retention_rollup_record_id", "verification_state", "checked_rollup_refs"]))
    lines.extend(_format_table("Retention Rollup Closeout Records", catalog["retention_rollup_closeout_records"], ["retention_rollup_closeout_record_id", "retention_rollup_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Report Records", catalog["retention_report_records"], ["retention_report_record_id", "retention_rollup_closeout_record_id", "report_state", "report_scope"]))
    lines.extend(_format_table("Retention Report Verification Receipts", catalog["retention_report_verification_receipts"], ["retention_report_verification_receipt_id", "retention_report_record_id", "verification_state", "checked_report_refs"]))
    lines.extend(_format_table("Retention Report Closeout Records", catalog["retention_report_closeout_records"], ["retention_report_closeout_record_id", "retention_report_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Records", catalog["retention_publication_records"], ["retention_publication_record_id", "retention_report_closeout_record_id", "publication_state", "publication_scope"]))
    lines.extend(_format_table("Retention Publication Verification Receipts", catalog["retention_publication_verification_receipts"], ["retention_publication_verification_receipt_id", "retention_publication_record_id", "verification_state", "checked_publication_refs"]))
    lines.extend(_format_table("Retention Publication Closeout Records", catalog["retention_publication_closeout_records"], ["retention_publication_closeout_record_id", "retention_publication_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Dashboard Snapshot Records", catalog["retention_dashboard_snapshot_records"], ["retention_dashboard_snapshot_record_id", "retention_publication_closeout_record_id", "snapshot_state", "dashboard_status"]))
    lines.extend(_format_table("Retention Dashboard Snapshot Verification Receipts", catalog["retention_dashboard_snapshot_verification_receipts"], ["retention_dashboard_snapshot_verification_receipt_id", "retention_dashboard_snapshot_record_id", "verification_state", "checked_dashboard_refs"]))
    lines.extend(_format_table("Retention Dashboard Snapshot Closeout Records", catalog["retention_dashboard_snapshot_closeout_records"], ["retention_dashboard_snapshot_closeout_record_id", "retention_dashboard_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Summary Records", catalog["retention_summary_records"], ["retention_summary_record_id", "retention_dashboard_snapshot_closeout_record_id", "summary_state", "summary_scope"]))
    lines.extend(_format_table("Retention Summary Verification Receipts", catalog["retention_summary_verification_receipts"], ["retention_summary_verification_receipt_id", "retention_summary_record_id", "verification_state", "checked_summary_refs"]))
    lines.extend(_format_table("Retention Summary Closeout Records", catalog["retention_summary_closeout_records"], ["retention_summary_closeout_record_id", "retention_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Export Records", catalog["retention_export_records"], ["retention_export_record_id", "retention_summary_closeout_record_id", "export_state", "export_scope"]))
    lines.extend(_format_table("Retention Export Verification Receipts", catalog["retention_export_verification_receipts"], ["retention_export_verification_receipt_id", "retention_export_record_id", "verification_state", "checked_export_refs"]))
    lines.extend(_format_table("Retention Export Closeout Records", catalog["retention_export_closeout_records"], ["retention_export_closeout_record_id", "retention_export_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Handoff Records", catalog["retention_handoff_records"], ["retention_handoff_record_id", "retention_export_closeout_record_id", "handoff_state", "handoff_scope"]))
    lines.extend(_format_table("Retention Handoff Verification Receipts", catalog["retention_handoff_verification_receipts"], ["retention_handoff_verification_receipt_id", "retention_handoff_record_id", "verification_state", "checked_handoff_refs"]))
    lines.extend(_format_table("Retention Handoff Closeout Records", catalog["retention_handoff_closeout_records"], ["retention_handoff_closeout_record_id", "retention_handoff_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Acceptance Records", catalog["retention_acceptance_records"], ["retention_acceptance_record_id", "retention_handoff_closeout_record_id", "acceptance_state", "acceptance_scope"]))
    lines.extend(_format_table("Retention Acceptance Verification Receipts", catalog["retention_acceptance_verification_receipts"], ["retention_acceptance_verification_receipt_id", "retention_acceptance_record_id", "verification_state", "checked_acceptance_refs"]))
    lines.extend(_format_table("Retention Acceptance Closeout Records", catalog["retention_acceptance_closeout_records"], ["retention_acceptance_closeout_record_id", "retention_acceptance_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Package Records", catalog["retention_package_records"], ["retention_package_record_id", "retention_acceptance_closeout_record_id", "package_state", "package_scope"]))
    lines.extend(_format_table("Retention Package Verification Receipts", catalog["retention_package_verification_receipts"], ["retention_package_verification_receipt_id", "retention_package_record_id", "verification_state", "checked_package_refs"]))
    lines.extend(_format_table("Retention Package Closeout Records", catalog["retention_package_closeout_records"], ["retention_package_closeout_record_id", "retention_package_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Finalization Records", catalog["retention_finalization_records"], ["retention_finalization_record_id", "retention_package_closeout_record_id", "finalization_state", "finalization_scope"]))
    lines.extend(_format_table("Retention Finalization Verification Receipts", catalog["retention_finalization_verification_receipts"], ["retention_finalization_verification_receipt_id", "retention_finalization_record_id", "verification_state", "checked_finalization_refs"]))
    lines.extend(_format_table("Retention Finalization Closeout Records", catalog["retention_finalization_closeout_records"], ["retention_finalization_closeout_record_id", "retention_finalization_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Status Records", catalog["retention_terminal_status_records"], ["retention_terminal_status_record_id", "retention_finalization_closeout_record_id", "terminal_state", "retention_status"]))
    lines.extend(_format_table("Retention Terminal Status Verification Receipts", catalog["retention_terminal_status_verification_receipts"], ["retention_terminal_status_verification_receipt_id", "retention_terminal_status_record_id", "verification_state", "checked_terminal_refs"]))
    lines.extend(_format_table("Retention Terminal Status Closeout Records", catalog["retention_terminal_status_closeout_records"], ["retention_terminal_status_closeout_record_id", "retention_terminal_status_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Certificate Records", catalog["retention_certificate_records"], ["retention_certificate_record_id", "retention_terminal_status_closeout_record_id", "certificate_state", "certificate_scope"]))
    lines.extend(_format_table("Retention Certificate Verification Receipts", catalog["retention_certificate_verification_receipts"], ["retention_certificate_verification_receipt_id", "retention_certificate_record_id", "verification_state", "checked_certificate_refs"]))
    lines.extend(_format_table("Retention Certificate Closeout Records", catalog["retention_certificate_closeout_records"], ["retention_certificate_closeout_record_id", "retention_certificate_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Registry Records", catalog["retention_registry_records"], ["retention_registry_record_id", "retention_certificate_closeout_record_id", "registry_state", "registry_scope"]))
    lines.extend(_format_table("Retention Registry Verification Receipts", catalog["retention_registry_verification_receipts"], ["retention_registry_verification_receipt_id", "retention_registry_record_id", "verification_state", "checked_registry_refs"]))
    lines.extend(_format_table("Retention Registry Closeout Records", catalog["retention_registry_closeout_records"], ["retention_registry_closeout_record_id", "retention_registry_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Closure Records", catalog["retention_closure_records"], ["retention_closure_record_id", "retention_registry_closeout_record_id", "closure_state", "closure_scope"]))
    lines.extend(_format_table("Retention Closure Verification Receipts", catalog["retention_closure_verification_receipts"], ["retention_closure_verification_receipt_id", "retention_closure_record_id", "verification_state", "checked_closure_refs"]))
    lines.extend(_format_table("Retention Closure Closeout Records", catalog["retention_closure_closeout_records"], ["retention_closure_closeout_record_id", "retention_closure_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Completion Records", catalog["retention_completion_records"], ["retention_completion_record_id", "retention_closure_closeout_record_id", "completion_state", "completion_scope"]))
    lines.extend(_format_table("Retention Completion Verification Receipts", catalog["retention_completion_verification_receipts"], ["retention_completion_verification_receipt_id", "retention_completion_record_id", "verification_state", "checked_completion_refs"]))
    lines.extend(_format_table("Retention Completion Closeout Records", catalog["retention_completion_closeout_records"], ["retention_completion_closeout_record_id", "retention_completion_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Attestation Records", catalog["retention_attestation_records"], ["retention_attestation_record_id", "retention_completion_closeout_record_id", "attestation_state", "attestation_scope"]))
    lines.extend(_format_table("Retention Attestation Verification Receipts", catalog["retention_attestation_verification_receipts"], ["retention_attestation_verification_receipt_id", "retention_attestation_record_id", "verification_state", "checked_attestation_refs"]))
    lines.extend(_format_table("Retention Attestation Closeout Records", catalog["retention_attestation_closeout_records"], ["retention_attestation_closeout_record_id", "retention_attestation_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Seal Records", catalog["retention_seal_records"], ["retention_seal_record_id", "retention_attestation_closeout_record_id", "seal_state", "seal_scope"]))
    lines.extend(_format_table("Retention Seal Verification Receipts", catalog["retention_seal_verification_receipts"], ["retention_seal_verification_receipt_id", "retention_seal_record_id", "verification_state", "checked_seal_refs"]))
    lines.extend(_format_table("Retention Seal Closeout Records", catalog["retention_seal_closeout_records"], ["retention_seal_closeout_record_id", "retention_seal_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Notarization Records", catalog["retention_notarization_records"], ["retention_notarization_record_id", "retention_seal_closeout_record_id", "notarization_state", "notarization_scope"]))
    lines.extend(_format_table("Retention Notarization Verification Receipts", catalog["retention_notarization_verification_receipts"], ["retention_notarization_verification_receipt_id", "retention_notarization_record_id", "verification_state", "checked_notarization_refs"]))
    lines.extend(_format_table("Retention Notarization Closeout Records", catalog["retention_notarization_closeout_records"], ["retention_notarization_closeout_record_id", "retention_notarization_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Archive Anchor Records", catalog["retention_archive_anchor_records"], ["retention_archive_anchor_record_id", "retention_notarization_closeout_record_id", "archive_anchor_state", "archive_anchor_scope"]))
    lines.extend(_format_table("Retention Archive Anchor Verification Receipts", catalog["retention_archive_anchor_verification_receipts"], ["retention_archive_anchor_verification_receipt_id", "retention_archive_anchor_record_id", "verification_state", "checked_archive_anchor_refs"]))
    lines.extend(_format_table("Retention Archive Anchor Closeout Records", catalog["retention_archive_anchor_closeout_records"], ["retention_archive_anchor_closeout_record_id", "retention_archive_anchor_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Endcap Records", catalog["retention_endcap_records"], ["retention_endcap_record_id", "retention_archive_anchor_closeout_record_id", "endcap_state", "endcap_scope"]))
    lines.extend(_format_table("Retention Endcap Verification Receipts", catalog["retention_endcap_verification_receipts"], ["retention_endcap_verification_receipt_id", "retention_endcap_record_id", "verification_state", "checked_endcap_refs"]))
    lines.extend(_format_table("Retention Endcap Closeout Records", catalog["retention_endcap_closeout_records"], ["retention_endcap_closeout_record_id", "retention_endcap_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Final Index Records", catalog["retention_final_index_records"], ["retention_final_index_record_id", "retention_endcap_closeout_record_id", "final_index_state", "final_index_scope"]))
    lines.extend(_format_table("Retention Final Index Verification Receipts", catalog["retention_final_index_verification_receipts"], ["retention_final_index_verification_receipt_id", "retention_final_index_record_id", "verification_state", "checked_final_index_refs"]))
    lines.extend(_format_table("Retention Final Index Closeout Records", catalog["retention_final_index_closeout_records"], ["retention_final_index_closeout_record_id", "retention_final_index_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Master Ledger Records", catalog["retention_master_ledger_records"], ["retention_master_ledger_record_id", "retention_final_index_closeout_record_id", "master_ledger_state", "master_ledger_scope"]))
    lines.extend(_format_table("Retention Master Ledger Verification Receipts", catalog["retention_master_ledger_verification_receipts"], ["retention_master_ledger_verification_receipt_id", "retention_master_ledger_record_id", "verification_state", "checked_master_ledger_refs"]))
    lines.extend(_format_table("Retention Master Ledger Closeout Records", catalog["retention_master_ledger_closeout_records"], ["retention_master_ledger_closeout_record_id", "retention_master_ledger_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Manifest Records", catalog["retention_terminal_manifest_records"], ["retention_terminal_manifest_record_id", "retention_master_ledger_closeout_record_id", "terminal_manifest_state", "terminal_manifest_scope"]))
    lines.extend(_format_table("Retention Terminal Manifest Verification Receipts", catalog["retention_terminal_manifest_verification_receipts"], ["retention_terminal_manifest_verification_receipt_id", "retention_terminal_manifest_record_id", "verification_state", "checked_terminal_manifest_refs"]))
    lines.extend(_format_table("Retention Terminal Manifest Closeout Records", catalog["retention_terminal_manifest_closeout_records"], ["retention_terminal_manifest_closeout_record_id", "retention_terminal_manifest_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Repository Release Records", catalog["retention_repository_release_records"], ["retention_repository_release_record_id", "retention_terminal_manifest_closeout_record_id", "repository_release_state", "repository_release_scope"]))
    lines.extend(_format_table("Retention Repository Release Verification Receipts", catalog["retention_repository_release_verification_receipts"], ["retention_repository_release_verification_receipt_id", "retention_repository_release_record_id", "verification_state", "checked_repository_release_refs"]))
    lines.extend(_format_table("Retention Repository Release Closeout Records", catalog["retention_repository_release_closeout_records"], ["retention_repository_release_closeout_record_id", "retention_repository_release_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Deployment Release Records", catalog["retention_deployment_release_records"], ["retention_deployment_release_record_id", "retention_repository_release_closeout_record_id", "deployment_release_state", "deployment_release_scope"]))
    lines.extend(_format_table("Retention Deployment Release Verification Receipts", catalog["retention_deployment_release_verification_receipts"], ["retention_deployment_release_verification_receipt_id", "retention_deployment_release_record_id", "verification_state", "checked_deployment_release_refs"]))
    lines.extend(_format_table("Retention Deployment Release Closeout Records", catalog["retention_deployment_release_closeout_records"], ["retention_deployment_release_closeout_record_id", "retention_deployment_release_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Availability Notice Records", catalog["retention_availability_notice_records"], ["retention_availability_notice_record_id", "retention_deployment_release_closeout_record_id", "availability_notice_state", "availability_notice_scope"]))
    lines.extend(_format_table("Retention Availability Notice Verification Receipts", catalog["retention_availability_notice_verification_receipts"], ["retention_availability_notice_verification_receipt_id", "retention_availability_notice_record_id", "verification_state", "checked_availability_notice_refs"]))
    lines.extend(_format_table("Retention Availability Notice Closeout Records", catalog["retention_availability_notice_closeout_records"], ["retention_availability_notice_closeout_record_id", "retention_availability_notice_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Acknowledgement Records", catalog["retention_release_acknowledgement_records"], ["retention_release_acknowledgement_record_id", "retention_availability_notice_closeout_record_id", "release_acknowledgement_state", "release_acknowledgement_scope"]))
    lines.extend(_format_table("Retention Release Acknowledgement Verification Receipts", catalog["retention_release_acknowledgement_verification_receipts"], ["retention_release_acknowledgement_verification_receipt_id", "retention_release_acknowledgement_record_id", "verification_state", "checked_release_acknowledgement_refs"]))
    lines.extend(_format_table("Retention Release Acknowledgement Closeout Records", catalog["retention_release_acknowledgement_closeout_records"], ["retention_release_acknowledgement_closeout_record_id", "retention_release_acknowledgement_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Confirmation Records", catalog["retention_release_confirmation_records"], ["retention_release_confirmation_record_id", "retention_release_acknowledgement_closeout_record_id", "release_confirmation_state", "release_confirmation_scope"]))
    lines.extend(_format_table("Retention Release Confirmation Verification Receipts", catalog["retention_release_confirmation_verification_receipts"], ["retention_release_confirmation_verification_receipt_id", "retention_release_confirmation_record_id", "verification_state", "checked_release_confirmation_refs"]))
    lines.extend(_format_table("Retention Release Confirmation Closeout Records", catalog["retention_release_confirmation_closeout_records"], ["retention_release_confirmation_closeout_record_id", "retention_release_confirmation_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Distribution Package Records", catalog["retention_distribution_package_records"], ["retention_distribution_package_record_id", "retention_release_confirmation_closeout_record_id", "distribution_package_state", "distribution_package_scope"]))
    lines.extend(_format_table("Retention Distribution Package Verification Receipts", catalog["retention_distribution_package_verification_receipts"], ["retention_distribution_package_verification_receipt_id", "retention_distribution_package_record_id", "verification_state", "checked_distribution_package_refs"]))
    lines.extend(_format_table("Retention Distribution Package Closeout Records", catalog["retention_distribution_package_closeout_records"], ["retention_distribution_package_closeout_record_id", "retention_distribution_package_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Distribution Manifest Records", catalog["retention_distribution_manifest_records"], ["retention_distribution_manifest_record_id", "retention_distribution_package_closeout_record_id", "distribution_manifest_state", "distribution_manifest_scope"]))
    lines.extend(_format_table("Retention Distribution Manifest Verification Receipts", catalog["retention_distribution_manifest_verification_receipts"], ["retention_distribution_manifest_verification_receipt_id", "retention_distribution_manifest_record_id", "verification_state", "checked_distribution_manifest_refs"]))
    lines.extend(_format_table("Retention Distribution Manifest Closeout Records", catalog["retention_distribution_manifest_closeout_records"], ["retention_distribution_manifest_closeout_record_id", "retention_distribution_manifest_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Access Publication Records", catalog["retention_access_publication_records"], ["retention_access_publication_record_id", "retention_distribution_manifest_closeout_record_id", "access_publication_state", "access_publication_scope"]))
    lines.extend(_format_table("Retention Access Publication Verification Receipts", catalog["retention_access_publication_verification_receipts"], ["retention_access_publication_verification_receipt_id", "retention_access_publication_record_id", "verification_state", "checked_access_publication_refs"]))
    lines.extend(_format_table("Retention Access Publication Closeout Records", catalog["retention_access_publication_closeout_records"], ["retention_access_publication_closeout_record_id", "retention_access_publication_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Access Grant Records", catalog["retention_access_grant_records"], ["retention_access_grant_record_id", "retention_access_publication_closeout_record_id", "access_grant_state", "access_grant_scope"]))
    lines.extend(_format_table("Retention Access Grant Verification Receipts", catalog["retention_access_grant_verification_receipts"], ["retention_access_grant_verification_receipt_id", "retention_access_grant_record_id", "verification_state", "checked_access_grant_refs"]))
    lines.extend(_format_table("Retention Access Grant Closeout Records", catalog["retention_access_grant_closeout_records"], ["retention_access_grant_closeout_record_id", "retention_access_grant_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Access Ledger Records", catalog["retention_access_ledger_records"], ["retention_access_ledger_record_id", "retention_access_grant_closeout_record_id", "access_ledger_state", "access_ledger_scope"]))
    lines.extend(_format_table("Retention Access Ledger Verification Receipts", catalog["retention_access_ledger_verification_receipts"], ["retention_access_ledger_verification_receipt_id", "retention_access_ledger_record_id", "verification_state", "checked_access_ledger_refs"]))
    lines.extend(_format_table("Retention Access Ledger Closeout Records", catalog["retention_access_ledger_closeout_records"], ["retention_access_ledger_closeout_record_id", "retention_access_ledger_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Retrieval Catalog Records", catalog["retention_retrieval_catalog_records"], ["retention_retrieval_catalog_record_id", "retention_access_ledger_closeout_record_id", "retrieval_catalog_state", "retrieval_catalog_scope"]))
    lines.extend(_format_table("Retention Retrieval Catalog Verification Receipts", catalog["retention_retrieval_catalog_verification_receipts"], ["retention_retrieval_catalog_verification_receipt_id", "retention_retrieval_catalog_record_id", "verification_state", "checked_retrieval_catalog_refs"]))
    lines.extend(_format_table("Retention Retrieval Catalog Closeout Records", catalog["retention_retrieval_catalog_closeout_records"], ["retention_retrieval_catalog_closeout_record_id", "retention_retrieval_catalog_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Retrieval Endpoint Records", catalog["retention_retrieval_endpoint_records"], ["retention_retrieval_endpoint_record_id", "retention_retrieval_catalog_closeout_record_id", "retrieval_endpoint_state", "retrieval_endpoint_scope"]))
    lines.extend(_format_table("Retention Retrieval Endpoint Verification Receipts", catalog["retention_retrieval_endpoint_verification_receipts"], ["retention_retrieval_endpoint_verification_receipt_id", "retention_retrieval_endpoint_record_id", "verification_state", "checked_retrieval_endpoint_refs"]))
    lines.extend(_format_table("Retention Retrieval Endpoint Closeout Records", catalog["retention_retrieval_endpoint_closeout_records"], ["retention_retrieval_endpoint_closeout_record_id", "retention_retrieval_endpoint_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Retrieval Token Records", catalog["retention_retrieval_token_records"], ["retention_retrieval_token_record_id", "retention_retrieval_endpoint_closeout_record_id", "retrieval_token_state", "retrieval_token_scope"]))
    lines.extend(_format_table("Retention Retrieval Token Verification Receipts", catalog["retention_retrieval_token_verification_receipts"], ["retention_retrieval_token_verification_receipt_id", "retention_retrieval_token_record_id", "verification_state", "checked_retrieval_token_refs"]))
    lines.extend(_format_table("Retention Retrieval Token Closeout Records", catalog["retention_retrieval_token_closeout_records"], ["retention_retrieval_token_closeout_record_id", "retention_retrieval_token_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Consumer Receipt Records", catalog["retention_consumer_receipt_records"], ["retention_consumer_receipt_record_id", "retention_retrieval_token_closeout_record_id", "consumer_receipt_state", "consumer_receipt_scope"]))
    lines.extend(_format_table("Retention Consumer Receipt Verification Receipts", catalog["retention_consumer_receipt_verification_receipts"], ["retention_consumer_receipt_verification_receipt_id", "retention_consumer_receipt_record_id", "verification_state", "checked_consumer_receipt_refs"]))
    lines.extend(_format_table("Retention Consumer Receipt Closeout Records", catalog["retention_consumer_receipt_closeout_records"], ["retention_consumer_receipt_closeout_record_id", "retention_consumer_receipt_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Rollup Records", catalog["retention_publication_rollup_records"], ["retention_publication_rollup_record_id", "retention_consumer_receipt_closeout_record_id", "publication_rollup_state", "publication_rollup_scope"]))
    lines.extend(_format_table("Retention Publication Rollup Verification Receipts", catalog["retention_publication_rollup_verification_receipts"], ["retention_publication_rollup_verification_receipt_id", "retention_publication_rollup_record_id", "verification_state", "checked_publication_rollup_refs"]))
    lines.extend(_format_table("Retention Publication Rollup Closeout Records", catalog["retention_publication_rollup_closeout_records"], ["retention_publication_rollup_closeout_record_id", "retention_publication_rollup_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Distribution Receipt Records", catalog["retention_distribution_receipt_records"], ["retention_distribution_receipt_record_id", "retention_publication_rollup_closeout_record_id", "distribution_receipt_state", "distribution_receipt_scope"]))
    lines.extend(_format_table("Retention Distribution Receipt Verification Receipts", catalog["retention_distribution_receipt_verification_receipts"], ["retention_distribution_receipt_verification_receipt_id", "retention_distribution_receipt_record_id", "verification_state", "checked_distribution_receipt_refs"]))
    lines.extend(_format_table("Retention Distribution Receipt Closeout Records", catalog["retention_distribution_receipt_closeout_records"], ["retention_distribution_receipt_closeout_record_id", "retention_distribution_receipt_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Access Audit Snapshot Records", catalog["retention_access_audit_snapshot_records"], ["retention_access_audit_snapshot_record_id", "retention_distribution_receipt_closeout_record_id", "access_audit_snapshot_state", "access_audit_snapshot_scope"]))
    lines.extend(_format_table("Retention Access Audit Snapshot Verification Receipts", catalog["retention_access_audit_snapshot_verification_receipts"], ["retention_access_audit_snapshot_verification_receipt_id", "retention_access_audit_snapshot_record_id", "verification_state", "checked_access_audit_snapshot_refs"]))
    lines.extend(_format_table("Retention Access Audit Snapshot Closeout Records", catalog["retention_access_audit_snapshot_closeout_records"], ["retention_access_audit_snapshot_closeout_record_id", "retention_access_audit_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Health Snapshot Records", catalog["retention_release_health_snapshot_records"], ["retention_release_health_snapshot_record_id", "retention_access_audit_snapshot_closeout_record_id", "release_health_snapshot_state", "release_health_snapshot_scope"]))
    lines.extend(_format_table("Retention Release Health Snapshot Verification Receipts", catalog["retention_release_health_snapshot_verification_receipts"], ["retention_release_health_snapshot_verification_receipt_id", "retention_release_health_snapshot_record_id", "verification_state", "checked_release_health_snapshot_refs"]))
    lines.extend(_format_table("Retention Release Health Snapshot Closeout Records", catalog["retention_release_health_snapshot_closeout_records"], ["retention_release_health_snapshot_closeout_record_id", "retention_release_health_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Usage Summary Records", catalog["retention_release_usage_summary_records"], ["retention_release_usage_summary_record_id", "retention_release_health_snapshot_closeout_record_id", "release_usage_summary_state", "release_usage_summary_scope"]))
    lines.extend(_format_table("Retention Release Usage Summary Verification Receipts", catalog["retention_release_usage_summary_verification_receipts"], ["retention_release_usage_summary_verification_receipt_id", "retention_release_usage_summary_record_id", "verification_state", "checked_release_usage_summary_refs"]))
    lines.extend(_format_table("Retention Release Usage Summary Closeout Records", catalog["retention_release_usage_summary_closeout_records"], ["retention_release_usage_summary_closeout_record_id", "retention_release_usage_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Retention Exposure Report Records", catalog["retention_retention_exposure_report_records"], ["retention_retention_exposure_report_record_id", "retention_release_usage_summary_closeout_record_id", "retention_exposure_report_state", "retention_exposure_report_scope"]))
    lines.extend(_format_table("Retention Retention Exposure Report Verification Receipts", catalog["retention_retention_exposure_report_verification_receipts"], ["retention_retention_exposure_report_verification_receipt_id", "retention_retention_exposure_report_record_id", "verification_state", "checked_retention_exposure_report_refs"]))
    lines.extend(_format_table("Retention Retention Exposure Report Closeout Records", catalog["retention_retention_exposure_report_closeout_records"], ["retention_retention_exposure_report_closeout_record_id", "retention_retention_exposure_report_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Closeout Summary Records", catalog["retention_release_closeout_summary_records"], ["retention_release_closeout_summary_record_id", "retention_retention_exposure_report_closeout_record_id", "release_closeout_summary_state", "release_closeout_summary_scope"]))
    lines.extend(_format_table("Retention Release Closeout Summary Verification Receipts", catalog["retention_release_closeout_summary_verification_receipts"], ["retention_release_closeout_summary_verification_receipt_id", "retention_release_closeout_summary_record_id", "verification_state", "checked_release_closeout_summary_refs"]))
    lines.extend(_format_table("Retention Release Closeout Summary Closeout Records", catalog["retention_release_closeout_summary_closeout_records"], ["retention_release_closeout_summary_closeout_record_id", "retention_release_closeout_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Public Record Index Records", catalog["retention_public_record_index_records"], ["retention_public_record_index_record_id", "retention_release_closeout_summary_closeout_record_id", "public_record_index_state", "public_record_index_scope"]))
    lines.extend(_format_table("Retention Public Record Index Verification Receipts", catalog["retention_public_record_index_verification_receipts"], ["retention_public_record_index_verification_receipt_id", "retention_public_record_index_record_id", "verification_state", "checked_public_record_index_refs"]))
    lines.extend(_format_table("Retention Public Record Index Closeout Records", catalog["retention_public_record_index_closeout_records"], ["retention_public_record_index_closeout_record_id", "retention_public_record_index_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Final Release Bundle Records", catalog["retention_final_release_bundle_records"], ["retention_final_release_bundle_record_id", "retention_public_record_index_closeout_record_id", "final_release_bundle_state", "final_release_bundle_scope"]))
    lines.extend(_format_table("Retention Final Release Bundle Verification Receipts", catalog["retention_final_release_bundle_verification_receipts"], ["retention_final_release_bundle_verification_receipt_id", "retention_final_release_bundle_record_id", "verification_state", "checked_final_release_bundle_refs"]))
    lines.extend(_format_table("Retention Final Release Bundle Closeout Records", catalog["retention_final_release_bundle_closeout_records"], ["retention_final_release_bundle_closeout_record_id", "retention_final_release_bundle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Access Notice Records", catalog["retention_terminal_access_notice_records"], ["retention_terminal_access_notice_record_id", "retention_final_release_bundle_closeout_record_id", "terminal_access_notice_state", "terminal_access_notice_scope"]))
    lines.extend(_format_table("Retention Terminal Access Notice Verification Receipts", catalog["retention_terminal_access_notice_verification_receipts"], ["retention_terminal_access_notice_verification_receipt_id", "retention_terminal_access_notice_record_id", "verification_state", "checked_terminal_access_notice_refs"]))
    lines.extend(_format_table("Retention Terminal Access Notice Closeout Records", catalog["retention_terminal_access_notice_closeout_records"], ["retention_terminal_access_notice_closeout_record_id", "retention_terminal_access_notice_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Acceptance Records", catalog["retention_release_acceptance_records"], ["retention_release_acceptance_record_id", "retention_terminal_access_notice_closeout_record_id", "release_acceptance_state", "release_acceptance_scope"]))
    lines.extend(_format_table("Retention Release Acceptance Verification Receipts", catalog["retention_release_acceptance_verification_receipts"], ["retention_release_acceptance_verification_receipt_id", "retention_release_acceptance_record_id", "verification_state", "checked_release_acceptance_refs"]))
    lines.extend(_format_table("Retention Release Acceptance Closeout Records", catalog["retention_release_acceptance_closeout_records"], ["retention_release_acceptance_closeout_record_id", "retention_release_acceptance_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Access Completion Records", catalog["retention_access_completion_records"], ["retention_access_completion_record_id", "retention_release_acceptance_closeout_record_id", "access_completion_state", "access_completion_scope"]))
    lines.extend(_format_table("Retention Access Completion Verification Receipts", catalog["retention_access_completion_verification_receipts"], ["retention_access_completion_verification_receipt_id", "retention_access_completion_record_id", "verification_state", "checked_access_completion_refs"]))
    lines.extend(_format_table("Retention Access Completion Closeout Records", catalog["retention_access_completion_closeout_records"], ["retention_access_completion_closeout_record_id", "retention_access_completion_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Certificate Records", catalog["retention_publication_certificate_records"], ["retention_publication_certificate_record_id", "retention_access_completion_closeout_record_id", "publication_certificate_state", "publication_certificate_scope"]))
    lines.extend(_format_table("Retention Publication Certificate Verification Receipts", catalog["retention_publication_certificate_verification_receipts"], ["retention_publication_certificate_verification_receipt_id", "retention_publication_certificate_record_id", "verification_state", "checked_publication_certificate_refs"]))
    lines.extend(_format_table("Retention Publication Certificate Closeout Records", catalog["retention_publication_certificate_closeout_records"], ["retention_publication_certificate_closeout_record_id", "retention_publication_certificate_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Distribution Closure Notice Records", catalog["retention_distribution_closure_notice_records"], ["retention_distribution_closure_notice_record_id", "retention_publication_certificate_closeout_record_id", "distribution_closure_notice_state", "distribution_closure_notice_scope"]))
    lines.extend(_format_table("Retention Distribution Closure Notice Verification Receipts", catalog["retention_distribution_closure_notice_verification_receipts"], ["retention_distribution_closure_notice_verification_receipt_id", "retention_distribution_closure_notice_record_id", "verification_state", "checked_distribution_closure_notice_refs"]))
    lines.extend(_format_table("Retention Distribution Closure Notice Closeout Records", catalog["retention_distribution_closure_notice_closeout_records"], ["retention_distribution_closure_notice_closeout_record_id", "retention_distribution_closure_notice_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Public Access Register Records", catalog["retention_public_access_register_records"], ["retention_public_access_register_record_id", "retention_distribution_closure_notice_closeout_record_id", "public_access_register_state", "public_access_register_scope"]))
    lines.extend(_format_table("Retention Public Access Register Verification Receipts", catalog["retention_public_access_register_verification_receipts"], ["retention_public_access_register_verification_receipt_id", "retention_public_access_register_record_id", "verification_state", "checked_public_access_register_refs"]))
    lines.extend(_format_table("Retention Public Access Register Closeout Records", catalog["retention_public_access_register_closeout_records"], ["retention_public_access_register_closeout_record_id", "retention_public_access_register_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Access Index Records", catalog["retention_release_access_index_records"], ["retention_release_access_index_record_id", "retention_public_access_register_closeout_record_id", "release_access_index_state", "release_access_index_scope"]))
    lines.extend(_format_table("Retention Release Access Index Verification Receipts", catalog["retention_release_access_index_verification_receipts"], ["retention_release_access_index_verification_receipt_id", "retention_release_access_index_record_id", "verification_state", "checked_release_access_index_refs"]))
    lines.extend(_format_table("Retention Release Access Index Closeout Records", catalog["retention_release_access_index_closeout_records"], ["retention_release_access_index_closeout_record_id", "retention_release_access_index_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Access Verification Summary Records", catalog["retention_release_access_verification_summary_records"], ["retention_release_access_verification_summary_record_id", "retention_release_access_index_closeout_record_id", "release_access_verification_summary_state", "release_access_verification_summary_scope"]))
    lines.extend(_format_table("Retention Release Access Verification Summary Verification Receipts", catalog["retention_release_access_verification_summary_verification_receipts"], ["retention_release_access_verification_summary_verification_receipt_id", "retention_release_access_verification_summary_record_id", "verification_state", "checked_release_access_verification_summary_refs"]))
    lines.extend(_format_table("Retention Release Access Verification Summary Closeout Records", catalog["retention_release_access_verification_summary_closeout_records"], ["retention_release_access_verification_summary_closeout_record_id", "retention_release_access_verification_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Access Closeout Summary Records", catalog["retention_release_access_closeout_summary_records"], ["retention_release_access_closeout_summary_record_id", "retention_release_access_verification_summary_closeout_record_id", "release_access_closeout_summary_state", "release_access_closeout_summary_scope"]))
    lines.extend(_format_table("Retention Release Access Closeout Summary Verification Receipts", catalog["retention_release_access_closeout_summary_verification_receipts"], ["retention_release_access_closeout_summary_verification_receipt_id", "retention_release_access_closeout_summary_record_id", "verification_state", "checked_release_access_closeout_summary_refs"]))
    lines.extend(_format_table("Retention Release Access Closeout Summary Closeout Records", catalog["retention_release_access_closeout_summary_closeout_records"], ["retention_release_access_closeout_summary_closeout_record_id", "retention_release_access_closeout_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Archive Availability Rollup Records", catalog["retention_archive_availability_rollup_records"], ["retention_archive_availability_rollup_record_id", "retention_release_access_closeout_summary_closeout_record_id", "archive_availability_rollup_state", "archive_availability_rollup_scope"]))
    lines.extend(_format_table("Retention Archive Availability Rollup Verification Receipts", catalog["retention_archive_availability_rollup_verification_receipts"], ["retention_archive_availability_rollup_verification_receipt_id", "retention_archive_availability_rollup_record_id", "verification_state", "checked_archive_availability_rollup_refs"]))
    lines.extend(_format_table("Retention Archive Availability Rollup Closeout Records", catalog["retention_archive_availability_rollup_closeout_records"], ["retention_archive_availability_rollup_closeout_record_id", "retention_archive_availability_rollup_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Retrieval Readiness Snapshot Records", catalog["retention_retrieval_readiness_snapshot_records"], ["retention_retrieval_readiness_snapshot_record_id", "retention_archive_availability_rollup_closeout_record_id", "retrieval_readiness_snapshot_state", "retrieval_readiness_snapshot_scope"]))
    lines.extend(_format_table("Retention Retrieval Readiness Snapshot Verification Receipts", catalog["retention_retrieval_readiness_snapshot_verification_receipts"], ["retention_retrieval_readiness_snapshot_verification_receipt_id", "retention_retrieval_readiness_snapshot_record_id", "verification_state", "checked_retrieval_readiness_snapshot_refs"]))
    lines.extend(_format_table("Retention Retrieval Readiness Snapshot Closeout Records", catalog["retention_retrieval_readiness_snapshot_closeout_records"], ["retention_retrieval_readiness_snapshot_closeout_record_id", "retention_retrieval_readiness_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Consumer Availability Notice Records", catalog["retention_consumer_availability_notice_records"], ["retention_consumer_availability_notice_record_id", "retention_retrieval_readiness_snapshot_closeout_record_id", "consumer_availability_notice_state", "consumer_availability_notice_scope"]))
    lines.extend(_format_table("Retention Consumer Availability Notice Verification Receipts", catalog["retention_consumer_availability_notice_verification_receipts"], ["retention_consumer_availability_notice_verification_receipt_id", "retention_consumer_availability_notice_record_id", "verification_state", "checked_consumer_availability_notice_refs"]))
    lines.extend(_format_table("Retention Consumer Availability Notice Closeout Records", catalog["retention_consumer_availability_notice_closeout_records"], ["retention_consumer_availability_notice_closeout_record_id", "retention_consumer_availability_notice_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Public Release Receipt Records", catalog["retention_public_release_receipt_records"], ["retention_public_release_receipt_record_id", "retention_consumer_availability_notice_closeout_record_id", "public_release_receipt_state", "public_release_receipt_scope"]))
    lines.extend(_format_table("Retention Public Release Receipt Verification Receipts", catalog["retention_public_release_receipt_verification_receipts"], ["retention_public_release_receipt_verification_receipt_id", "retention_public_release_receipt_record_id", "verification_state", "checked_public_release_receipt_refs"]))
    lines.extend(_format_table("Retention Public Release Receipt Closeout Records", catalog["retention_public_release_receipt_closeout_records"], ["retention_public_release_receipt_closeout_record_id", "retention_public_release_receipt_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Exception Register Records", catalog["retention_release_exception_register_records"], ["retention_release_exception_register_record_id", "retention_public_release_receipt_closeout_record_id", "release_exception_register_state", "release_exception_register_scope"]))
    lines.extend(_format_table("Retention Release Exception Register Verification Receipts", catalog["retention_release_exception_register_verification_receipts"], ["retention_release_exception_register_verification_receipt_id", "retention_release_exception_register_record_id", "verification_state", "checked_release_exception_register_refs"]))
    lines.extend(_format_table("Retention Release Exception Register Closeout Records", catalog["retention_release_exception_register_closeout_records"], ["retention_release_exception_register_closeout_record_id", "retention_release_exception_register_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Exception Summary Records", catalog["retention_release_exception_summary_records"], ["retention_release_exception_summary_record_id", "retention_release_exception_register_closeout_record_id", "release_exception_summary_state", "release_exception_summary_scope"]))
    lines.extend(_format_table("Retention Release Exception Summary Verification Receipts", catalog["retention_release_exception_summary_verification_receipts"], ["retention_release_exception_summary_verification_receipt_id", "retention_release_exception_summary_record_id", "verification_state", "checked_release_exception_summary_refs"]))
    lines.extend(_format_table("Retention Release Exception Summary Closeout Records", catalog["retention_release_exception_summary_closeout_records"], ["retention_release_exception_summary_closeout_record_id", "retention_release_exception_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Metrics Snapshot Records", catalog["retention_release_metrics_snapshot_records"], ["retention_release_metrics_snapshot_record_id", "retention_release_exception_summary_closeout_record_id", "release_metrics_snapshot_state", "release_metrics_snapshot_scope"]))
    lines.extend(_format_table("Retention Release Metrics Snapshot Verification Receipts", catalog["retention_release_metrics_snapshot_verification_receipts"], ["retention_release_metrics_snapshot_verification_receipt_id", "retention_release_metrics_snapshot_record_id", "verification_state", "checked_release_metrics_snapshot_refs"]))
    lines.extend(_format_table("Retention Release Metrics Snapshot Closeout Records", catalog["retention_release_metrics_snapshot_closeout_records"], ["retention_release_metrics_snapshot_closeout_record_id", "retention_release_metrics_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Release Terminal Report Records", catalog["retention_release_terminal_report_records"], ["retention_release_terminal_report_record_id", "retention_release_metrics_snapshot_closeout_record_id", "release_terminal_report_state", "release_terminal_report_scope"]))
    lines.extend(_format_table("Retention Release Terminal Report Verification Receipts", catalog["retention_release_terminal_report_verification_receipts"], ["retention_release_terminal_report_verification_receipt_id", "retention_release_terminal_report_record_id", "verification_state", "checked_release_terminal_report_refs"]))
    lines.extend(_format_table("Retention Release Terminal Report Closeout Records", catalog["retention_release_terminal_report_closeout_records"], ["retention_release_terminal_report_closeout_record_id", "retention_release_terminal_report_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Final Publication Notice Records", catalog["retention_final_publication_notice_records"], ["retention_final_publication_notice_record_id", "retention_release_terminal_report_closeout_record_id", "final_publication_notice_state", "final_publication_notice_scope"]))
    lines.extend(_format_table("Retention Final Publication Notice Verification Receipts", catalog["retention_final_publication_notice_verification_receipts"], ["retention_final_publication_notice_verification_receipt_id", "retention_final_publication_notice_record_id", "verification_state", "checked_final_publication_notice_refs"]))
    lines.extend(_format_table("Retention Final Publication Notice Closeout Records", catalog["retention_final_publication_notice_closeout_records"], ["retention_final_publication_notice_closeout_record_id", "retention_final_publication_notice_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Completion Records", catalog["retention_publication_completion_records"], ["retention_publication_completion_record_id", "retention_final_publication_notice_closeout_record_id", "publication_completion_state", "publication_completion_scope"]))
    lines.extend(_format_table("Retention Publication Completion Verification Receipts", catalog["retention_publication_completion_verification_receipts"], ["retention_publication_completion_verification_receipt_id", "retention_publication_completion_record_id", "verification_state", "checked_publication_completion_refs"]))
    lines.extend(_format_table("Retention Publication Completion Closeout Records", catalog["retention_publication_completion_closeout_records"], ["retention_publication_completion_closeout_record_id", "retention_publication_completion_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Closure Certificate Records", catalog["retention_publication_closure_certificate_records"], ["retention_publication_closure_certificate_record_id", "retention_publication_completion_closeout_record_id", "publication_closure_certificate_state", "publication_closure_certificate_scope"]))
    lines.extend(_format_table("Retention Publication Closure Certificate Verification Receipts", catalog["retention_publication_closure_certificate_verification_receipts"], ["retention_publication_closure_certificate_verification_receipt_id", "retention_publication_closure_certificate_record_id", "verification_state", "checked_publication_closure_certificate_refs"]))
    lines.extend(_format_table("Retention Publication Closure Certificate Closeout Records", catalog["retention_publication_closure_certificate_closeout_records"], ["retention_publication_closure_certificate_closeout_record_id", "retention_publication_closure_certificate_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Index Records", catalog["retention_terminal_public_index_records"], ["retention_terminal_public_index_record_id", "retention_publication_closure_certificate_closeout_record_id", "terminal_public_index_state", "terminal_public_index_scope"]))
    lines.extend(_format_table("Retention Terminal Public Index Verification Receipts", catalog["retention_terminal_public_index_verification_receipts"], ["retention_terminal_public_index_verification_receipt_id", "retention_terminal_public_index_record_id", "verification_state", "checked_terminal_public_index_refs"]))
    lines.extend(_format_table("Retention Terminal Public Index Closeout Records", catalog["retention_terminal_public_index_closeout_records"], ["retention_terminal_public_index_closeout_record_id", "retention_terminal_public_index_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Digest Records", catalog["retention_terminal_public_digest_records"], ["retention_terminal_public_digest_record_id", "retention_terminal_public_index_closeout_record_id", "terminal_public_digest_state", "terminal_public_digest_scope"]))
    lines.extend(_format_table("Retention Terminal Public Digest Verification Receipts", catalog["retention_terminal_public_digest_verification_receipts"], ["retention_terminal_public_digest_verification_receipt_id", "retention_terminal_public_digest_record_id", "verification_state", "checked_terminal_public_digest_refs"]))
    lines.extend(_format_table("Retention Terminal Public Digest Closeout Records", catalog["retention_terminal_public_digest_closeout_records"], ["retention_terminal_public_digest_closeout_record_id", "retention_terminal_public_digest_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Archive Copy Records", catalog["retention_terminal_public_archive_copy_records"], ["retention_terminal_public_archive_copy_record_id", "retention_terminal_public_digest_closeout_record_id", "terminal_public_archive_copy_state", "terminal_public_archive_copy_scope"]))
    lines.extend(_format_table("Retention Terminal Public Archive Copy Verification Receipts", catalog["retention_terminal_public_archive_copy_verification_receipts"], ["retention_terminal_public_archive_copy_verification_receipt_id", "retention_terminal_public_archive_copy_record_id", "verification_state", "checked_terminal_public_archive_copy_refs"]))
    lines.extend(_format_table("Retention Terminal Public Archive Copy Closeout Records", catalog["retention_terminal_public_archive_copy_closeout_records"], ["retention_terminal_public_archive_copy_closeout_record_id", "retention_terminal_public_archive_copy_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Audit Snapshot Records", catalog["retention_terminal_public_audit_snapshot_records"], ["retention_terminal_public_audit_snapshot_record_id", "retention_terminal_public_archive_copy_closeout_record_id", "terminal_public_audit_snapshot_state", "terminal_public_audit_snapshot_scope"]))
    lines.extend(_format_table("Retention Terminal Public Audit Snapshot Verification Receipts", catalog["retention_terminal_public_audit_snapshot_verification_receipts"], ["retention_terminal_public_audit_snapshot_verification_receipt_id", "retention_terminal_public_audit_snapshot_record_id", "verification_state", "checked_terminal_public_audit_snapshot_refs"]))
    lines.extend(_format_table("Retention Terminal Public Audit Snapshot Closeout Records", catalog["retention_terminal_public_audit_snapshot_closeout_records"], ["retention_terminal_public_audit_snapshot_closeout_record_id", "retention_terminal_public_audit_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Metrics Snapshot Records", catalog["retention_terminal_public_metrics_snapshot_records"], ["retention_terminal_public_metrics_snapshot_record_id", "retention_terminal_public_audit_snapshot_closeout_record_id", "terminal_public_metrics_snapshot_state", "terminal_public_metrics_snapshot_scope"]))
    lines.extend(_format_table("Retention Terminal Public Metrics Snapshot Verification Receipts", catalog["retention_terminal_public_metrics_snapshot_verification_receipts"], ["retention_terminal_public_metrics_snapshot_verification_receipt_id", "retention_terminal_public_metrics_snapshot_record_id", "verification_state", "checked_terminal_public_metrics_snapshot_refs"]))
    lines.extend(_format_table("Retention Terminal Public Metrics Snapshot Closeout Records", catalog["retention_terminal_public_metrics_snapshot_closeout_records"], ["retention_terminal_public_metrics_snapshot_closeout_record_id", "retention_terminal_public_metrics_snapshot_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Public Release Summary Records", catalog["retention_terminal_public_release_summary_records"], ["retention_terminal_public_release_summary_record_id", "retention_terminal_public_metrics_snapshot_closeout_record_id", "terminal_public_release_summary_state", "terminal_public_release_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Public Release Summary Verification Receipts", catalog["retention_terminal_public_release_summary_verification_receipts"], ["retention_terminal_public_release_summary_verification_receipt_id", "retention_terminal_public_release_summary_record_id", "verification_state", "checked_terminal_public_release_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Public Release Summary Closeout Records", catalog["retention_terminal_public_release_summary_closeout_records"], ["retention_terminal_public_release_summary_closeout_record_id", "retention_terminal_public_release_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Distribution Summary Records", catalog["retention_terminal_distribution_summary_records"], ["retention_terminal_distribution_summary_record_id", "retention_terminal_public_release_summary_closeout_record_id", "terminal_distribution_summary_state", "terminal_distribution_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Distribution Summary Verification Receipts", catalog["retention_terminal_distribution_summary_verification_receipts"], ["retention_terminal_distribution_summary_verification_receipt_id", "retention_terminal_distribution_summary_record_id", "verification_state", "checked_terminal_distribution_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Distribution Summary Closeout Records", catalog["retention_terminal_distribution_summary_closeout_records"], ["retention_terminal_distribution_summary_closeout_record_id", "retention_terminal_distribution_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Access Summary Records", catalog["retention_terminal_access_summary_records"], ["retention_terminal_access_summary_record_id", "retention_terminal_distribution_summary_closeout_record_id", "terminal_access_summary_state", "terminal_access_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Access Summary Verification Receipts", catalog["retention_terminal_access_summary_verification_receipts"], ["retention_terminal_access_summary_verification_receipt_id", "retention_terminal_access_summary_record_id", "verification_state", "checked_terminal_access_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Access Summary Closeout Records", catalog["retention_terminal_access_summary_closeout_records"], ["retention_terminal_access_summary_closeout_record_id", "retention_terminal_access_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Retrieval Summary Records", catalog["retention_terminal_retrieval_summary_records"], ["retention_terminal_retrieval_summary_record_id", "retention_terminal_access_summary_closeout_record_id", "terminal_retrieval_summary_state", "terminal_retrieval_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Retrieval Summary Verification Receipts", catalog["retention_terminal_retrieval_summary_verification_receipts"], ["retention_terminal_retrieval_summary_verification_receipt_id", "retention_terminal_retrieval_summary_record_id", "verification_state", "checked_terminal_retrieval_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Retrieval Summary Closeout Records", catalog["retention_terminal_retrieval_summary_closeout_records"], ["retention_terminal_retrieval_summary_closeout_record_id", "retention_terminal_retrieval_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Consumer Summary Records", catalog["retention_terminal_consumer_summary_records"], ["retention_terminal_consumer_summary_record_id", "retention_terminal_retrieval_summary_closeout_record_id", "terminal_consumer_summary_state", "terminal_consumer_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Consumer Summary Verification Receipts", catalog["retention_terminal_consumer_summary_verification_receipts"], ["retention_terminal_consumer_summary_verification_receipt_id", "retention_terminal_consumer_summary_record_id", "verification_state", "checked_terminal_consumer_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Consumer Summary Closeout Records", catalog["retention_terminal_consumer_summary_closeout_records"], ["retention_terminal_consumer_summary_closeout_record_id", "retention_terminal_consumer_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Exception Summary Records", catalog["retention_terminal_exception_summary_records"], ["retention_terminal_exception_summary_record_id", "retention_terminal_consumer_summary_closeout_record_id", "terminal_exception_summary_state", "terminal_exception_summary_scope"]))
    lines.extend(_format_table("Retention Terminal Exception Summary Verification Receipts", catalog["retention_terminal_exception_summary_verification_receipts"], ["retention_terminal_exception_summary_verification_receipt_id", "retention_terminal_exception_summary_record_id", "verification_state", "checked_terminal_exception_summary_refs"]))
    lines.extend(_format_table("Retention Terminal Exception Summary Closeout Records", catalog["retention_terminal_exception_summary_closeout_records"], ["retention_terminal_exception_summary_closeout_record_id", "retention_terminal_exception_summary_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Archive Report Records", catalog["retention_terminal_archive_report_records"], ["retention_terminal_archive_report_record_id", "retention_terminal_exception_summary_closeout_record_id", "terminal_archive_report_state", "terminal_archive_report_scope"]))
    lines.extend(_format_table("Retention Terminal Archive Report Verification Receipts", catalog["retention_terminal_archive_report_verification_receipts"], ["retention_terminal_archive_report_verification_receipt_id", "retention_terminal_archive_report_record_id", "verification_state", "checked_terminal_archive_report_refs"]))
    lines.extend(_format_table("Retention Terminal Archive Report Closeout Records", catalog["retention_terminal_archive_report_closeout_records"], ["retention_terminal_archive_report_closeout_record_id", "retention_terminal_archive_report_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Release Annex Records", catalog["retention_terminal_release_annex_records"], ["retention_terminal_release_annex_record_id", "retention_terminal_archive_report_closeout_record_id", "terminal_release_annex_state", "terminal_release_annex_scope"]))
    lines.extend(_format_table("Retention Terminal Release Annex Verification Receipts", catalog["retention_terminal_release_annex_verification_receipts"], ["retention_terminal_release_annex_verification_receipt_id", "retention_terminal_release_annex_record_id", "verification_state", "checked_terminal_release_annex_refs"]))
    lines.extend(_format_table("Retention Terminal Release Annex Closeout Records", catalog["retention_terminal_release_annex_closeout_records"], ["retention_terminal_release_annex_closeout_record_id", "retention_terminal_release_annex_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Terminal Publication Bundle Records", catalog["retention_terminal_publication_bundle_records"], ["retention_terminal_publication_bundle_record_id", "retention_terminal_release_annex_closeout_record_id", "terminal_publication_bundle_state", "terminal_publication_bundle_scope"]))
    lines.extend(_format_table("Retention Terminal Publication Bundle Verification Receipts", catalog["retention_terminal_publication_bundle_verification_receipts"], ["retention_terminal_publication_bundle_verification_receipt_id", "retention_terminal_publication_bundle_record_id", "verification_state", "checked_terminal_publication_bundle_refs"]))
    lines.extend(_format_table("Retention Terminal Publication Bundle Closeout Records", catalog["retention_terminal_publication_bundle_closeout_records"], ["retention_terminal_publication_bundle_closeout_record_id", "retention_terminal_publication_bundle_verification_receipt_id", "closeout_state", "outcome"]))
    lines.extend(_format_table("Retention Publication Terminal Status Records", catalog["retention_publication_terminal_status_records"], ["retention_publication_terminal_status_record_id", "retention_terminal_publication_bundle_closeout_record_id", "publication_terminal_status_state", "publication_terminal_status_scope"]))
    lines.extend(_format_table("Merge Decisions", catalog["merge_decisions"], ["merge_decision_id", "import_record_id", "decision", "reason_code"]))
    lines.extend(_format_table("Intake Decisions", catalog["intake_decisions"], ["intake_decision_id", "inbox_item_id", "decision", "reason_code"]))
    lines.extend(_format_table("Routes", catalog["routes"], ["route_id", "route_kind", "enabled", "channels"]))
    lines.extend(_format_table("Delivery Channels", catalog["delivery_channels"], ["channel_id", "channel_kind", "status", "route_kinds"]))
    lines.extend(_format_table("Delivery Jobs", catalog["delivery_jobs"], ["delivery_job_id", "dispatch_rule_id", "job_state", "priority"]))
    lines.extend(_format_table("Transport Adapters", catalog["transport_adapters"], ["transport_adapter_id", "transport_kind", "status", "channels"]))
    lines.extend(_format_table("Transport Receipts", catalog["transport_receipts"], ["transport_receipt_id", "outbox_item_id", "transport_adapter_id", "transport_state"]))
    lines.extend(_format_table("Handling Labels", catalog["handling_labels"], ["label_id", "allowed_sharing_scopes", "requires_redaction"]))
    lines.extend(_format_table("Retention Classes", catalog["retention_classes"], ["retention_class", "default_duration", "allowed_states"]))
    lines.extend(_format_table("Integrity Receipts", catalog["integrity_receipts"], ["path", "digest_algorithm", "purpose"]))
    lines.extend(_format_table("Sharing Scopes", catalog["sharing_scopes"], ["scope_id", "display_name", "path"]))
    lines.extend(_format_table("Review Gates", catalog["review_gates"], ["gate_id", "display_name", "path"]))
    return "\n".join(lines)
