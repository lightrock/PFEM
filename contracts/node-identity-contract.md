# Node Identity Contract

PFEM node identity connects profile shape to federation accountability.

A node manifest should identify:

- `node_id`
- `display_name`
- `profile_id`
- `node_kind`
- `status`

Rollup and federation records should refer to known node ids.
