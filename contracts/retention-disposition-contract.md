# Retention and Disposition Contract

A retained PFEM record should identify:

- `retention_class`
- `disposition_state`

The retention policy maps classes to default durations and allowed disposition states.

Examples:

```text
retention_class: shared-summary
disposition_state: active
```
