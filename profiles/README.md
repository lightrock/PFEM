# Profiles

PFEM profiles configure deployment shape.

Known profiles are indexed in:

```text
profiles/profile-registry.json
```

Profile files remain the source of detail for each profile. The registry is only
an index.
