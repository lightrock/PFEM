# Merge Decisions

Merge decisions record how imported payloads are resolved against local PFEM records.

An import record says a payload was applied, staged, skipped, or failed.
A merge decision says whether incoming records were accepted, skipped, superseded, deferred, or sent to review because of local conflict rules.
