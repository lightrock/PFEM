# Adapters

PFEM adapters translate source-specific input into PFEM contracts.

Known adapters are indexed in:

```text
adapters/adapter-registry.json
```

Adapter manifests remain the source of detail for each adapter. The registry is
only an index.
