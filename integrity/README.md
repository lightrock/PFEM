# Integrity

Integrity receipts record hashes for important PFEM JSON artifacts.

Current file:

```text
integrity/receipt-manifest.json
```

Update receipts after intentional changes:

```bat
pfem_integrity_update.bat
```

Validate receipts:

```bat
pfem_integrity.bat
```
