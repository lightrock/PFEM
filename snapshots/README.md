# Snapshot Manifests

Snapshot manifests list the materialized records behind a PFEM state checkpoint.

A state checkpoint says this is a known-good state.
A snapshot manifest says which records/files make up that state.
