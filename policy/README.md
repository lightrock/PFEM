# Policy

PFEM policy files define review gates and sharing scopes.

This folder does not decide deployment law or organization rules. It gives the
architecture a small set of named policy hooks so profiles, rollups, and
federation messages can be checked for obvious drift.

Current file:

```text
policy/sharing-policy.json
```
