# Infrastructure Site Node Example

This example documents a site-oriented PFEM shape.

Profile:

```text
profiles/infrastructure-site/infrastructure-site.profile.yaml
```

The profile enables source input, normalized observation handling, local memory,
evidence package production, local dashboard use, and federation output.

The current example does not bind to a specific source adapter. A real site
profile would select configured adapters through deployment configuration.
