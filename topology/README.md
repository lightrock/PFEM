# Topology

PFEM topology files describe which nodes are allowed to exchange messages.

Nodes answer:

```text
Who exists?
```

Topology answers:

```text
Who can send what to whom, under what sharing scope?
```

Current file:

```text
topology/federation-topology.json
```
