# Retention Permanent Archive Retention Lock Records

PFEM retention permanent archive retention lock records add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive service level snapshot closeout record = formal closure of retention permanent archive service level snapshot workflow
retention permanent archive retention lock record = permanent-archive-retention-lock layer permanent archive retention lock record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive service level snapshot closeout record:
formal closure of retention permanent archive service level snapshot workflow

retention permanent archive retention lock record:
permanent-archive-retention-lock layer permanent archive retention lock record
```
