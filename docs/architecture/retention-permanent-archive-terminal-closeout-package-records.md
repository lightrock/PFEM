# Retention Permanent Archive Terminal Closeout Package Records

PFEM retention permanent archive terminal closeout package records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal final rollup report closeout record = formal closure of retention permanent archive terminal final rollup report workflow
retention permanent archive terminal closeout package record = permanent-archive-terminal-closeout-package layer permanent archive terminal closeout package record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
