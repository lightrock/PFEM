# Retention Permanent Archive Public Index Records

PFEM retention permanent archive public index records add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive access register closeout record = formal closure of retention permanent archive access register workflow
retention permanent archive public index record = permanent-archive-public-index layer permanent archive public index record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive access register closeout record:
formal closure of retention permanent archive access register workflow

retention permanent archive public index record:
permanent-archive-public-index layer permanent archive public index record
```
