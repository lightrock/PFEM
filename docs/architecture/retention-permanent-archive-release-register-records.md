# Retention Permanent Archive Release Register Records

PFEM retention permanent archive release register records add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive retention lock closeout record = formal closure of retention permanent archive retention lock workflow
retention permanent archive release register record = permanent-archive-release-register layer permanent archive release register record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive retention lock closeout record:
formal closure of retention permanent archive retention lock workflow

retention permanent archive release register record:
permanent-archive-release-register layer permanent archive release register record
```
