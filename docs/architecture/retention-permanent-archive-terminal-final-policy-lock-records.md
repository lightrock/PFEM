# Retention Permanent Archive Terminal Final Policy Lock Records

PFEM retention permanent archive terminal final policy lock records add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final access lock closeout record = formal closure of retention permanent archive terminal final access lock workflow
retention permanent archive terminal final policy lock record = permanent-archive-terminal-final-policy-lock layer permanent archive terminal final policy lock record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
