# Retention Permanent Archive Terminal Availability Certificate Records

PFEM retention permanent archive terminal availability certificate records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal compliance attestation closeout record = formal closure of retention permanent archive terminal compliance attestation workflow
retention permanent archive terminal availability certificate record = permanent-archive-terminal-availability-certificate layer permanent archive terminal availability certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal compliance attestation closeout record:
formal closure of retention permanent archive terminal compliance attestation workflow

retention permanent archive terminal availability certificate record:
permanent-archive-terminal-availability-certificate layer permanent archive terminal availability certificate record
```
