# Retention Permanent Archive Terminal Interop Export Receipt Verification Receipts

PFEM retention permanent archive terminal interop export receipt verification receipts add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal interop export receipt record = permanent-archive-terminal-interop-export-receipt layer permanent archive terminal interop export receipt record
retention permanent archive terminal interop export receipt verification receipt = evidence that permanent archive terminal interop export receipt refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal interop export receipt record:
permanent-archive-terminal-interop-export-receipt layer permanent archive terminal interop export receipt record

retention permanent archive terminal interop export receipt verification receipt:
evidence that permanent archive terminal interop export receipt refs/digest were checked
```
