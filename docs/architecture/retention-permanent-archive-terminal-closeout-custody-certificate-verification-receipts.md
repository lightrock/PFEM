# Retention Permanent Archive Terminal Closeout Custody Certificate Verification Receipts

PFEM retention permanent archive terminal closeout custody certificate verification receipts add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout custody certificate record = permanent-archive-terminal-closeout-custody-certificate layer permanent archive terminal closeout custody certificate record
retention permanent archive terminal closeout custody certificate verification receipt = evidence that permanent archive terminal closeout custody certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
