# Retention Permanent Archive Terminal Exception Register Records

PFEM retention permanent archive terminal exception register records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal service level report closeout record = formal closure of retention permanent archive terminal service level report workflow
retention permanent archive terminal exception register record = permanent-archive-terminal-exception-register layer permanent archive terminal exception register record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal service level report closeout record:
formal closure of retention permanent archive terminal service level report workflow

retention permanent archive terminal exception register record:
permanent-archive-terminal-exception-register layer permanent archive terminal exception register record
```
