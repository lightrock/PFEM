# Retention Permanent Archive Terminal Interop Export Manifest Records

PFEM retention permanent archive terminal interop export manifest records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal public access certificate closeout record = formal closure of retention permanent archive terminal public access certificate workflow
retention permanent archive terminal interop export manifest record = permanent-archive-terminal-interop-export-manifest layer permanent archive terminal interop export manifest record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal public access certificate closeout record:
formal closure of retention permanent archive terminal public access certificate workflow

retention permanent archive terminal interop export manifest record:
permanent-archive-terminal-interop-export-manifest layer permanent archive terminal interop export manifest record
```
