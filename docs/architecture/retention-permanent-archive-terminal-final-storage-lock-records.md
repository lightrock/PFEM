# Retention Permanent Archive Terminal Final Storage Lock Records

PFEM retention permanent archive terminal final storage lock records add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final exception clearance closeout record = formal closure of retention permanent archive terminal final exception clearance workflow
retention permanent archive terminal final storage lock record = permanent-archive-terminal-final-storage-lock layer permanent archive terminal final storage lock record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
