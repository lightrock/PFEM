# Retention Permanent Archive Terminal Custody Snapshot Records

PFEM retention permanent archive terminal custody snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal publication metrics snapshot closeout record = formal closure of retention permanent archive terminal publication metrics snapshot workflow
retention permanent archive terminal custody snapshot record = permanent-archive-terminal-custody-snapshot layer permanent archive terminal custody snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal publication metrics snapshot closeout record:
formal closure of retention permanent archive terminal publication metrics snapshot workflow

retention permanent archive terminal custody snapshot record:
permanent-archive-terminal-custody-snapshot layer permanent archive terminal custody snapshot record
```
