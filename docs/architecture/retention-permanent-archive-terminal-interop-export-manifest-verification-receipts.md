# Retention Permanent Archive Terminal Interop Export Manifest Verification Receipts

PFEM retention permanent archive terminal interop export manifest verification receipts add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal interop export manifest record = permanent-archive-terminal-interop-export-manifest layer permanent archive terminal interop export manifest record
retention permanent archive terminal interop export manifest verification receipt = evidence that permanent archive terminal interop export manifest refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal interop export manifest record:
permanent-archive-terminal-interop-export-manifest layer permanent archive terminal interop export manifest record

retention permanent archive terminal interop export manifest verification receipt:
evidence that permanent archive terminal interop export manifest refs/digest were checked
```
