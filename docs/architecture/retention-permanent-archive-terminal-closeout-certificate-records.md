# Retention Permanent Archive Terminal Closeout Certificate Records

PFEM retention permanent archive terminal closeout certificate records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout digest closeout record = formal closure of retention permanent archive terminal closeout digest workflow
retention permanent archive terminal closeout certificate record = permanent-archive-terminal-closeout-certificate layer permanent archive terminal closeout certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
