# Retention Permanent Archive Terminal Final Retrieval Endpoint Verification Receipts

PFEM retention permanent archive terminal final retrieval endpoint verification receipts add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final retrieval endpoint record = permanent-archive-terminal-final-retrieval-endpoint layer permanent archive terminal final retrieval endpoint record
retention permanent archive terminal final retrieval endpoint verification receipt = evidence that permanent archive terminal final retrieval endpoint refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
