# Retention Permanent Archive Access Register Records

PFEM retention permanent archive access register records add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive integrity certificate closeout record = formal closure of retention permanent archive integrity certificate workflow
retention permanent archive access register record = permanent-archive-access-register layer permanent archive access register record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive integrity certificate closeout record:
formal closure of retention permanent archive integrity certificate workflow

retention permanent archive access register record:
permanent-archive-access-register layer permanent archive access register record
```
