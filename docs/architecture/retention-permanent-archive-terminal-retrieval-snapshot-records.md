# Retention Permanent Archive Terminal Retrieval Snapshot Records

PFEM retention permanent archive terminal retrieval snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal access snapshot closeout record = formal closure of retention permanent archive terminal access snapshot workflow
retention permanent archive terminal retrieval snapshot record = permanent-archive-terminal-retrieval-snapshot layer permanent archive terminal retrieval snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal access snapshot closeout record:
formal closure of retention permanent archive terminal access snapshot workflow

retention permanent archive terminal retrieval snapshot record:
permanent-archive-terminal-retrieval-snapshot layer permanent archive terminal retrieval snapshot record
```
