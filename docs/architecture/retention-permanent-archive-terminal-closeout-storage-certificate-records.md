# Retention Permanent Archive Terminal Closeout Storage Certificate Records

PFEM retention permanent archive terminal closeout storage certificate records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout retrieval certificate closeout record = formal closure of retention permanent archive terminal closeout retrieval certificate workflow
retention permanent archive terminal closeout storage certificate record = permanent-archive-terminal-closeout-storage-certificate layer permanent archive terminal closeout storage certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
