# Retention Terminal Permanent Chain Rollup Closeout Records

PFEM retention terminal permanent chain rollup closeout records add the next permanent-archive retention boundary.

The boundary is:

```text
retention terminal permanent chain rollup verification receipt = evidence that terminal permanent chain rollup refs/digest were checked
retention terminal permanent chain rollup closeout record = formal closure of retention terminal permanent chain rollup workflow
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention terminal permanent chain rollup verification receipt:
evidence that terminal permanent chain rollup refs/digest were checked

retention terminal permanent chain rollup closeout record:
formal closure of retention terminal permanent chain rollup workflow
```
