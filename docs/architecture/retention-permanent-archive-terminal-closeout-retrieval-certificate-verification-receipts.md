# Retention Permanent Archive Terminal Closeout Retrieval Certificate Verification Receipts

PFEM retention permanent archive terminal closeout retrieval certificate verification receipts add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout retrieval certificate record = permanent-archive-terminal-closeout-retrieval-certificate layer permanent archive terminal closeout retrieval certificate record
retention permanent archive terminal closeout retrieval certificate verification receipt = evidence that permanent archive terminal closeout retrieval certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
