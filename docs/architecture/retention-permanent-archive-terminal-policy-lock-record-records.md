# Retention Permanent Archive Terminal Policy Lock Record Records

PFEM retention permanent archive terminal policy lock record records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal storage durability report closeout record = formal closure of retention permanent archive terminal storage durability report workflow
retention permanent archive terminal policy lock record record = permanent-archive-terminal-policy-lock-record layer permanent archive terminal policy lock record record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal storage durability report closeout record:
formal closure of retention permanent archive terminal storage durability report workflow

retention permanent archive terminal policy lock record record:
permanent-archive-terminal-policy-lock-record layer permanent archive terminal policy lock record record
```
