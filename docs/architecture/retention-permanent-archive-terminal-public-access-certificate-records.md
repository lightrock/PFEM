# Retention Permanent Archive Terminal Public Access Certificate Records

PFEM retention permanent archive terminal public access certificate records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal authority notice closeout record = formal closure of retention permanent archive terminal authority notice workflow
retention permanent archive terminal public access certificate record = permanent-archive-terminal-public-access-certificate layer permanent archive terminal public access certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal authority notice closeout record:
formal closure of retention permanent archive terminal authority notice workflow

retention permanent archive terminal public access certificate record:
permanent-archive-terminal-public-access-certificate layer permanent archive terminal public access certificate record
```
