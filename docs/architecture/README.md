# Architecture

PFEM architecture documentation placeholder.

## Contributor command protocol

The project-level command protocol is documented here:

```text
../developer/pfem-contributor-command-protocol.md
../developer/pfem-new-tab-prompt.md
```

When any developer says `start a new tab`, output the canonical new-tab handoff prompt instead of continuing implementation work.
