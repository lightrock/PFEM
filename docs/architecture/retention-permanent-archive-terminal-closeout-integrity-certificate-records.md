# Retention Permanent Archive Terminal Closeout Integrity Certificate Records

PFEM retention permanent archive terminal closeout integrity certificate records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout custody certificate closeout record = formal closure of retention permanent archive terminal closeout custody certificate workflow
retention permanent archive terminal closeout integrity certificate record = permanent-archive-terminal-closeout-integrity-certificate layer permanent archive terminal closeout integrity certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
