# Retention Permanent Archive Terminal Publication Manifest Records

PFEM retention permanent archive terminal publication manifest records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive public release closeout closeout record = formal closure of retention permanent archive public release closeout workflow
retention permanent archive terminal publication manifest record = permanent-archive-terminal-publication-manifest layer permanent archive terminal publication manifest record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive public release closeout closeout record:
formal closure of retention permanent archive public release closeout workflow

retention permanent archive terminal publication manifest record:
permanent-archive-terminal-publication-manifest layer permanent archive terminal publication manifest record
```
