# Retention Terminal Public Audit Snapshot Records

PFEM retention terminal public audit snapshot records add the next retention publication closeout boundary.

The boundary is:

```text
retention terminal public archive copy closeout record = formal closure of retention terminal public archive copy workflow
retention terminal public audit snapshot record = terminal-public-audit-snapshot layer terminal public audit snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention terminal public archive copy closeout record:
formal closure of retention terminal public archive copy workflow

retention terminal public audit snapshot record:
terminal-public-audit-snapshot layer terminal public audit snapshot record
```
