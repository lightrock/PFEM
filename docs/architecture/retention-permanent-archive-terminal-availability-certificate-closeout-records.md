# Retention Permanent Archive Terminal Availability Certificate Closeout Records

PFEM retention permanent archive terminal availability certificate closeout records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal availability certificate verification receipt = evidence that permanent archive terminal availability certificate refs/digest were checked
retention permanent archive terminal availability certificate closeout record = formal closure of retention permanent archive terminal availability certificate workflow
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal availability certificate verification receipt:
evidence that permanent archive terminal availability certificate refs/digest were checked

retention permanent archive terminal availability certificate closeout record:
formal closure of retention permanent archive terminal availability certificate workflow
```
