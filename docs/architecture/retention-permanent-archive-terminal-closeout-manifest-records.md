# Retention Permanent Archive Terminal Closeout Manifest Records

PFEM retention permanent archive terminal closeout manifest records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout package closeout record = formal closure of retention permanent archive terminal closeout package workflow
retention permanent archive terminal closeout manifest record = permanent-archive-terminal-closeout-manifest layer permanent archive terminal closeout manifest record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
