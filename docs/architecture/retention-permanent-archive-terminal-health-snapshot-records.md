# Retention Permanent Archive Terminal Health Snapshot Records

PFEM retention permanent archive terminal health snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal service snapshot closeout record = formal closure of retention permanent archive terminal service snapshot workflow
retention permanent archive terminal health snapshot record = permanent-archive-terminal-health-snapshot layer permanent archive terminal health snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal service snapshot closeout record:
formal closure of retention permanent archive terminal service snapshot workflow

retention permanent archive terminal health snapshot record:
permanent-archive-terminal-health-snapshot layer permanent archive terminal health snapshot record
```
