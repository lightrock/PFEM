# Retention Permanent Archive Public Index Verification Receipts

PFEM retention permanent archive public index verification receipts add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive public index record = permanent-archive-public-index layer permanent archive public index record
retention permanent archive public index verification receipt = evidence that permanent archive public index refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive public index record:
permanent-archive-public-index layer permanent archive public index record

retention permanent archive public index verification receipt:
evidence that permanent archive public index refs/digest were checked
```
