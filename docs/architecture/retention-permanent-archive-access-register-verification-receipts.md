# Retention Permanent Archive Access Register Verification Receipts

PFEM retention permanent archive access register verification receipts add the next permanent-archive continuation boundary.

The boundary is:

```text
retention permanent archive access register record = permanent-archive-access-register layer permanent archive access register record
retention permanent archive access register verification receipt = evidence that permanent archive access register refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive access register record:
permanent-archive-access-register layer permanent archive access register record

retention permanent archive access register verification receipt:
evidence that permanent archive access register refs/digest were checked
```
