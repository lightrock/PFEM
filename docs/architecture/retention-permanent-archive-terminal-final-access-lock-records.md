# Retention Permanent Archive Terminal Final Access Lock Records

PFEM retention permanent archive terminal final access lock records add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final storage lock closeout record = formal closure of retention permanent archive terminal final storage lock workflow
retention permanent archive terminal final access lock record = permanent-archive-terminal-final-access-lock layer permanent archive terminal final access lock record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
