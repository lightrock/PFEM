# Retention Terminal Public Metrics Snapshot Records

PFEM retention terminal public metrics snapshot records add the next retention publication closeout boundary.

The boundary is:

```text
retention terminal public audit snapshot closeout record = formal closure of retention terminal public audit snapshot workflow
retention terminal public metrics snapshot record = terminal-public-metrics-snapshot layer terminal public metrics snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention terminal public audit snapshot closeout record:
formal closure of retention terminal public audit snapshot workflow

retention terminal public metrics snapshot record:
terminal-public-metrics-snapshot layer terminal public metrics snapshot record
```
