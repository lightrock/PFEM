# Retention Permanent Archive Terminal Availability Certificate Verification Receipts

PFEM retention permanent archive terminal availability certificate verification receipts add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal availability certificate record = permanent-archive-terminal-availability-certificate layer permanent archive terminal availability certificate record
retention permanent archive terminal availability certificate verification receipt = evidence that permanent archive terminal availability certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal availability certificate record:
permanent-archive-terminal-availability-certificate layer permanent archive terminal availability certificate record

retention permanent archive terminal availability certificate verification receipt:
evidence that permanent archive terminal availability certificate refs/digest were checked
```
