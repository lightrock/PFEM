# Retention Permanent Archive Terminal Publication Metrics Snapshot Records

PFEM retention permanent archive terminal publication metrics snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal publication audit snapshot closeout record = formal closure of retention permanent archive terminal publication audit snapshot workflow
retention permanent archive terminal publication metrics snapshot record = permanent-archive-terminal-publication-metrics-snapshot layer permanent archive terminal publication metrics snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal publication audit snapshot closeout record:
formal closure of retention permanent archive terminal publication audit snapshot workflow

retention permanent archive terminal publication metrics snapshot record:
permanent-archive-terminal-publication-metrics-snapshot layer permanent archive terminal publication metrics snapshot record
```
