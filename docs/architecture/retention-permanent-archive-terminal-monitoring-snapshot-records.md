# Retention Permanent Archive Terminal Monitoring Snapshot Records

PFEM retention permanent archive terminal monitoring snapshot records add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal fixity certificate closeout record = formal closure of retention permanent archive terminal fixity certificate workflow
retention permanent archive terminal monitoring snapshot record = permanent-archive-terminal-monitoring-snapshot layer permanent archive terminal monitoring snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal fixity certificate closeout record:
formal closure of retention permanent archive terminal fixity certificate workflow

retention permanent archive terminal monitoring snapshot record:
permanent-archive-terminal-monitoring-snapshot layer permanent archive terminal monitoring snapshot record
```
