# Retention Permanent Archive Terminal Public Access Certificate Verification Receipts

PFEM retention permanent archive terminal public access certificate verification receipts add the next permanent-archive assurance boundary.

The boundary is:

```text
retention permanent archive terminal public access certificate record = permanent-archive-terminal-public-access-certificate layer permanent archive terminal public access certificate record
retention permanent archive terminal public access certificate verification receipt = evidence that permanent archive terminal public access certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal public access certificate record:
permanent-archive-terminal-public-access-certificate layer permanent archive terminal public access certificate record

retention permanent archive terminal public access certificate verification receipt:
evidence that permanent archive terminal public access certificate refs/digest were checked
```
