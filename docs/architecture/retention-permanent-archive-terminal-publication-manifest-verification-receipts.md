# Retention Permanent Archive Terminal Publication Manifest Verification Receipts

PFEM retention permanent archive terminal publication manifest verification receipts add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal publication manifest record = permanent-archive-terminal-publication-manifest layer permanent archive terminal publication manifest record
retention permanent archive terminal publication manifest verification receipt = evidence that permanent archive terminal publication manifest refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal publication manifest record:
permanent-archive-terminal-publication-manifest layer permanent archive terminal publication manifest record

retention permanent archive terminal publication manifest verification receipt:
evidence that permanent archive terminal publication manifest refs/digest were checked
```
