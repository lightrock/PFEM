# Retention Permanent Archive Terminal Service Snapshot Records

PFEM retention permanent archive terminal service snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal exception snapshot closeout record = formal closure of retention permanent archive terminal exception snapshot workflow
retention permanent archive terminal service snapshot record = permanent-archive-terminal-service-snapshot layer permanent archive terminal service snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal exception snapshot closeout record:
formal closure of retention permanent archive terminal exception snapshot workflow

retention permanent archive terminal service snapshot record:
permanent-archive-terminal-service-snapshot layer permanent archive terminal service snapshot record
```
