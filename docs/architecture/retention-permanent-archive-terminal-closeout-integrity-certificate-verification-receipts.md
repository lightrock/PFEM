# Retention Permanent Archive Terminal Closeout Integrity Certificate Verification Receipts

PFEM retention permanent archive terminal closeout integrity certificate verification receipts add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout integrity certificate record = permanent-archive-terminal-closeout-integrity-certificate layer permanent archive terminal closeout integrity certificate record
retention permanent archive terminal closeout integrity certificate verification receipt = evidence that permanent archive terminal closeout integrity certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
