# Retention Permanent Archive Terminal Final Retrieval Attestation Records

PFEM retention permanent archive terminal final retrieval attestation records add the next permanent-archive final seal boundary.

The boundary is:

```text
retention permanent archive terminal final access attestation closeout record = formal closure of retention permanent archive terminal final access attestation workflow
retention permanent archive terminal final retrieval attestation record = permanent-archive-terminal-final-retrieval-attestation layer permanent archive terminal final retrieval attestation record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
