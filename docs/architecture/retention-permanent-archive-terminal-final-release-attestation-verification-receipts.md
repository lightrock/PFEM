# Retention Permanent Archive Terminal Final Release Attestation Verification Receipts

PFEM retention permanent archive terminal final release attestation verification receipts add the next permanent-archive final seal boundary.

The boundary is:

```text
retention permanent archive terminal final release attestation record = permanent-archive-terminal-final-release-attestation layer permanent archive terminal final release attestation record
retention permanent archive terminal final release attestation verification receipt = evidence that permanent archive terminal final release attestation refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
