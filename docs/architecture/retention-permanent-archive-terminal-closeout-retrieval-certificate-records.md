# Retention Permanent Archive Terminal Closeout Retrieval Certificate Records

PFEM retention permanent archive terminal closeout retrieval certificate records add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout access certificate closeout record = formal closure of retention permanent archive terminal closeout access certificate workflow
retention permanent archive terminal closeout retrieval certificate record = permanent-archive-terminal-closeout-retrieval-certificate layer permanent archive terminal closeout retrieval certificate record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
