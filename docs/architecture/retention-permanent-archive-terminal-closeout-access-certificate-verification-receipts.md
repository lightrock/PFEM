# Retention Permanent Archive Terminal Closeout Access Certificate Verification Receipts

PFEM retention permanent archive terminal closeout access certificate verification receipts add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout access certificate record = permanent-archive-terminal-closeout-access-certificate layer permanent archive terminal closeout access certificate record
retention permanent archive terminal closeout access certificate verification receipt = evidence that permanent archive terminal closeout access certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
