# Retention Permanent Archive Terminal Closeout Storage Certificate Verification Receipts

PFEM retention permanent archive terminal closeout storage certificate verification receipts add the next permanent-archive terminal closeout boundary.

The boundary is:

```text
retention permanent archive terminal closeout storage certificate record = permanent-archive-terminal-closeout-storage-certificate layer permanent archive terminal closeout storage certificate record
retention permanent archive terminal closeout storage certificate verification receipt = evidence that permanent archive terminal closeout storage certificate refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
