# Retention Permanent Archive Terminal Final Access Lock Verification Receipts

PFEM retention permanent archive terminal final access lock verification receipts add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final access lock record = permanent-archive-terminal-final-access-lock layer permanent archive terminal final access lock record
retention permanent archive terminal final access lock verification receipt = evidence that permanent archive terminal final access lock refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
