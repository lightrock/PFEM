# Retention Terminal Permanent Public Register Verification Receipts

PFEM retention terminal permanent public register verification receipts add the next permanent-archive retention boundary.

The boundary is:

```text
retention terminal permanent public register record = terminal-permanent-public-register layer terminal permanent public register record
retention terminal permanent public register verification receipt = evidence that terminal permanent public register refs/digest were checked
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention terminal permanent public register record:
terminal-permanent-public-register layer terminal permanent public register record

retention terminal permanent public register verification receipt:
evidence that terminal permanent public register refs/digest were checked
```
