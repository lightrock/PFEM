# Retention Permanent Archive Terminal Final Retrieval Endpoint Records

PFEM retention permanent archive terminal final retrieval endpoint records add the next permanent-archive terminal authority boundary.

The boundary is:

```text
retention permanent archive terminal final public availability notice closeout record = formal closure of retention permanent archive terminal final public availability notice workflow
retention permanent archive terminal final retrieval endpoint record = permanent-archive-terminal-final-retrieval-endpoint layer permanent archive terminal final retrieval endpoint record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.
