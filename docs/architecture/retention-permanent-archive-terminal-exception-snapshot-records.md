# Retention Permanent Archive Terminal Exception Snapshot Records

PFEM retention permanent archive terminal exception snapshot records add the next permanent-archive finalization boundary.

The boundary is:

```text
retention permanent archive terminal retrieval snapshot closeout record = formal closure of retention permanent archive terminal retrieval snapshot workflow
retention permanent archive terminal exception snapshot record = permanent-archive-terminal-exception-snapshot layer permanent archive terminal exception snapshot record
```

## Why this exists

This keeps one PFEM responsibility separate from the next.

Plain English:

```text
retention permanent archive terminal retrieval snapshot closeout record:
formal closure of retention permanent archive terminal retrieval snapshot workflow

retention permanent archive terminal exception snapshot record:
permanent-archive-terminal-exception-snapshot layer permanent archive terminal exception snapshot record
```
