# Node Profile Rules

Node profiles configure deployment shape.

A node profile can select capabilities, adapters, dashboard mode, review gates, and offline behavior.

A node profile should not redefine core domain objects or fork the core architecture.
