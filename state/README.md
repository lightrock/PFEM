# State Checkpoints

State checkpoints mark a local known-good PFEM state after apply receipts.

An apply receipt says records were actually created, updated, skipped, or failed.
A state checkpoint says this set of local records is the current known-good state at a point in time.
